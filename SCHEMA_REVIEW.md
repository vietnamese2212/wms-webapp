# Schema Review – WMS Webapp

> Cập nhật lần cuối: 2026-05-10 (rev 13 — Customer table removed, not needed)
> Database: PostgreSQL via Supabase · client: `@supabase/supabase-js` (service role)

---

## Kiến trúc vị trí kho

```
Warehouse  →  Location (embed sub_code / sub_name / sub_type)
BV_TP1_1_T1 = {warehouse.code}_{sub_code}_{row}_{shelf}
```

`location_code` tự sinh trong backend, user không nhập. Sub-group lấy qua `GET /masterdata/locations/sub-groups?warehouse_id=`.

---

## Trạng thái Model

| Model | Mục đích | Trạng thái |
|---|---|---|
| `Warehouse` | Kho lớn (Ba Vì, Bàu Bàng) | ✅ API + seed |
| `Location` | Vị trí kho (embed sub info) | ✅ API + seed |
| `Material` | Danh mục hàng hóa | ✅ API + seed |
| `Manufacturer` | Nhà máy sản xuất (NMSX) | ✅ API + seed |
| `Department` | Phòng ban + module access | ✅ API + seed (rev 11) |
| `JobTitle` | Chức danh — permission template | ✅ API + seed (rev 11) |
| `Employee` | Nhân viên — action_level, categories, scope | ✅ API đầy đủ (rev 11) |
| `UserWarehouseAccess` | Kho được phép của từng nhân viên | ✅ API + migrate (rev 11) |
| `ImportShift` | Ca nhập (Ca 1/2/3, HC) | ✅ seed sẵn |
| `QAStatus` | Tình trạng QA (X, X CQ, X7, OK) | ✅ seed sẵn |
| `ProductionImport` | Phiếu nhập kho (bảng cha) | ✅ API đầy đủ |
| `InventoryEntry` | Pallet tồn kho | ✅ API đầy đủ |
| `ExportHistory` | Lịch sử xuất kho | ⏳ bảng cũ, thay bằng `OutboundScanEntry` |
| `GroupDeliveryOrder` | Chuyến xe (Số xe) — nhóm các DO | ✅ API + UI (rev 12) |
| `DeliveryOrder` | Phiếu giao hàng SAP (Delivery) | ✅ API + UI (rev 12) |
| `DeliveryOrderItem` | Dòng mặt hàng trong DO | ✅ API + UI (rev 12) |
| `OutboundScanEntry` | Log QR scan xuất kho | ✅ API (rev 12) |
| `LocationTransfer` | Chuyển vị trí pallet | ✅ bảng tạo sẵn |
| `Vehicle` / `Driver` | TMS — xe + tài xế | ⏳ bảng tạo, chưa có API |
| `Menu` / `Setting` | Phân quyền menu, cài đặt | ⏳ bảng tạo, chưa dùng |

---

## Schema các bảng chính

### Warehouse
```sql
id TEXT PK, code TEXT UNIQUE, name TEXT, address TEXT,
warehouse_type TEXT, inventory_mode TEXT, nmsx_code TEXT,
shipto_codes TEXT[] DEFAULT '{}',  -- mã ship-to phụ; auto-detect chuyển kho khớp code HOẶC phần tử mảng này
is_active BOOL DEFAULT true, created_at, updated_at
```

### Location
```sql
id TEXT PK, warehouse_id TEXT FK,
sub_code TEXT,        -- "TP1", "NL1"
sub_name TEXT,        -- "Thành phẩm 1"
sub_type TEXT,        -- "THANH_PHAM" | "NGUYEN_LIEU" | "BAN_THANH_PHAM"
location_code TEXT UNIQUE,  -- "BV_TP1_1_T1" (auto-generated)
row TEXT, shelf TEXT, max_pallets INT DEFAULT 1,
is_active BOOL DEFAULT true, created_at, updated_at
```

### Manufacturer
```sql
id TEXT PK, code TEXT UNIQUE, name TEXT,
is_active BOOL DEFAULT true, updated_at
```

### Material
```sql
id TEXT PK, material_code TEXT UNIQUE, material_description TEXT,
short_name TEXT,          -- auto: "{desc} [{3 số cuối code}]"
custom_short_name TEXT,   -- user override (suffix giữ nguyên)
category TEXT,            -- "Thành phẩm" | "NVL" | "POSM" | "Bao bì"
product_type TEXT, unit TEXT,
weight_kg DECIMAL, cartons_per_pallet INT, cartons_per_pallet_mn INT,  -- _mn DEPRECATED (thay bằng warehouse_pallet_overrides)
units_per_carton INT, pallet_per_ea NUMERIC,  -- pallet_per_ea: 1 EA = ? pallet (NVL), quy đổi tồn EA→pallet
shelf_life_days INT, storage_category TEXT,  -- "UHT" | "Fresh" | "Frozen"
old_code TEXT, image_url TEXT,
batch_prefix TEXT,        -- ĐV2 tem `;`: 2 ký tự tắt hàng để sinh mã lô V2 (TA260705A018); null ĐV1
manufacturer_id TEXT FK, notes TEXT,
is_active BOOL DEFAULT true, created_at, updated_at
```
Filter API: `?category=`, `?storage_category=`, `?manufacturer_id=`, `?search=` (code/description/short_name/old_code)

### Employee
```sql
id TEXT PK, warehouse_id TEXT FK,
name TEXT, employee_code TEXT UNIQUE,
role TEXT,   -- "OWN" | "ADMIN" | "WAREHOUSE_MANAGER" | "WAREHOUSE_STAFF" | "DRIVER" | "HR_MANAGER"
department TEXT, phone TEXT, email TEXT UNIQUE, password TEXT (bcrypt),
is_active BOOL DEFAULT true, created_at
```

### ImportShift
```sql
id TEXT PK, code TEXT UNIQUE, name TEXT  -- Ca 1, Ca 2, Ca 3, HC
```

### QAStatus
```sql
id TEXT PK, code TEXT UNIQUE, label TEXT  -- X, X cảm quan, X 7, OK
```

### ProductionImport (phiếu nhập)
```sql
id TEXT PK, import_code TEXT UNIQUE,
warehouse_id TEXT FK, location_id TEXT FK, material_id TEXT FK,
shift_id TEXT FK,        -- Ca nhập
planned_pallets INT, status TEXT,  -- "OPEN" | "COMPLETED" | "CANCELLED"
imported_by TEXT FK(Employee), created_by TEXT FK(Employee), updated_by TEXT FK(Employee),
location_history JSONB DEFAULT '[]',  -- lịch sử đổi vị trí: [{location_code, by_id, by_name, at, source:'scan'|'detail'}] (migration 20260619)
ncc_id UUID FK(TransportCompany),  -- NCC của phiếu (cho HSD ngoại lệ theo NCC); nullable: SX/chuyển kho = HSD mặc định (migration 20260628)
notes TEXT, created_at, updated_at
```
> Mô hình **"1 phiếu = 1 vị trí" = vị trí CHỌN CUỐI CÙNG**: quét/đổi vị trí → persist `location_id` (vị trí hiện tại) + append `location_history`. KHÔNG giới hạn số vị trí (không có "tràn"). UI hiện 1 vị trí (order.location) + **cảnh báo ⚠** nếu có pallet nằm ở vị trí khác (không tự dời dữ liệu).

### InventoryEntry (pallet tồn kho)
```sql
id TEXT PK, pallet_code TEXT UNIQUE,
location_id TEXT FK, material_id TEXT FK, manufacturer_id TEXT FK,
cycle TEXT,              -- chu kỳ sản xuất (bóc từ QR)
machine_code TEXT,       -- mã máy (bóc từ QR)
pallet_sequence_no INT,  -- vị trí 5 trong QR
stack_layer INT DEFAULT 1,  -- 1=sàn (tính slot), 2/3=chồng (không tính slot)
cartons_imported INT, production_date TIMESTAMP,
qa_status_id TEXT FK,
status TEXT DEFAULT "IN_STOCK",  -- "IN_STOCK" | "EXPORTED" | "TRANSFERRED" | "PARTIAL"
import_order_id TEXT FK(ProductionImport),
ncc_id UUID FK(TransportCompany),  -- denormalize từ phiếu khi quét; cho HSD ngoại lệ theo NCC (migration 20260628)
shelf_life_days INT,  -- shelflife chọn theo LÔ khi nhận (1 mã+1 NCC có thể nhiều shelflife); ưu tiên hơn override-theo-NCC (migration 20260628_02)
batch TEXT,          -- tem V2 (`;` ĐV2): mã lô nguyên văn (TA+yymmdd+Máy+SEQ); NULL với tem `_` (migration 20260707)
expiry_date DATE,    -- tem V2: HSD tường minh từ tem; NULL → %Date fallback shelf-life như cũ (migration 20260707)
created_by TEXT FK(Employee), updated_by TEXT FK(Employee),
created_at, updated_at
```

**Logic `stack_layer`:** chỉ đếm `stack_layer = 1` vào `used_slots`. Layer 2/3 chồng lên, không tính capacity.

---

## Lưu ý INSERT bắt buộc

- `id TEXT NOT NULL` — không có `DEFAULT` → phải truyền `id: randomUUID()`
- `created_at TIMESTAMP NOT NULL` — phải truyền `created_at: new Date().toISOString()` để đảm bảo suffix `Z` (UTC rõ ràng)
- `updated_at TIMESTAMP NOT NULL` — không có `DEFAULT` → phải truyền `updated_at: new Date().toISOString()`

---

## Changelog

| Ngày | Rev | Thay đổi |
|---|---|---|
| 2026-05-07 | 1–8 | Setup, Material fields, Location 2-table (bỏ SubWarehouse), Employee |
| 2026-05-07 | 9 | ProductionImport + InventoryEntry refactor đầy đủ, WMS Inbound API |
| 2026-05-08 | 10 | ImportShift, QAStatus, `pallet_sequence_no`, `qa_status_id`, `shift_id` |
| 2026-05-08 | — | Supabase Realtime bật tất cả bảng `public` + event trigger auto-add bảng mới |
| 2026-05-09 | — | Chuẩn INSERT `randomUUID()` + `updated_at` bắt buộc |
| 2026-05-09 | 11 | Permission system: `Department`, `JobTitle`, `UserWarehouseAccess`, mở rộng `Employee` (action_level, allowed_categories, warehouse_scope). Backend `permissions.ts` với `can()` + `loadActor()`. API + UI User Management |
| 2026-05-09 | 12 | Outbound module: `GroupDeliveryOrder`, `DeliveryOrder`, `DeliveryOrderItem`, `OutboundScanEntry`. `InventoryEntry.cartons_imported` → DECIMAL, thêm `cartons_remaining`. Excel upload, QR scan với QA block + tịnh tiến logic. |
| 2026-05-10 | 13 | `GroupDeliveryOrder` thêm `warehouse_type TEXT`. Excel template thêm cột "Kho xuất" (match Warehouse by name/code) và "Loại kho" (lưu vào warehouse_type). |
| 2026-06-13 | — | `PalletLabelPrint` — log truy vết in tem pallet (qr_code, material_code, cycle/machine/seq/nmsx, qty, mode GENERATE/REPRINT, printed_by/printed_by_name, created_at). Migration `20260613_pallet_label_print_log.sql`. Phục vụ module In tem pallet (tra cứu in mấy lần, ai in). |
| 2026-06-14 | — | `PalletLabelPrint.batch_id` (uuid) — gom các tem in cùng 1 lệnh. Migration `20260614_pallet_print_batch_id.sql`. Phục vụ tab Lịch sử in. |
| 2026-06-15 | — | Dồn/Tách pallet: `InventoryEntry.parent_pallet_code` (tem con đã dồn → mã tem đích) + `InventoryEntry.origin` ('IMPORT'/'SPLIT'). Bảng `PalletOperation` (type MERGE/UNGROUP/SPLIT, source_codes[], target_codes[], detail jsonb, operated_by). Migration `20260615_pallet_merge_split.sql`. Tách giữ `cartons_imported` gốc bất biến + con `import_order_id=NULL` → báo cáo nhập không đổi. |
| 2026-06-15 | — | HR Lịch làm việc & Chấm công (nền tảng): `Skill`, `EmployeeSkill` (priority 1=chính), `LeaveRequest`, `WorkAssignmentSheet`/`Demand`/`WorkAssignment`, `Attendance` (kind CA1/CA2/CA3/HC/LEAVE + ot_hours + early_leave_hours). `Department.requires_scheduling`. Migration `20260615_hr_schedule_attendance.sql`. RLS + anon SELECT cho Realtime. |
| 2026-06-15 | — | HR: `Skill` chuyển sang thuộc **Chức danh** (`job_title_id`, bỏ warehouse/department). Migration `20260615_hr_skill_jobtitle.sql`. |
| 2026-06-15 | — | HR Layout: `WorkLayout` (mẫu theo `warehouse_id`, name) + `WorkLayoutSkill` (skill + required_count). `WorkAssignmentSheet` đổi `department_id`→`layout_id`, unique (work_date, layout_id). Migrations `20260615_hr_layout.sql` + `_finalize.sql`. Tạo lịch = chọn Kho+Layout+Ngày → demand tự đổ; auto-assign lấy NV có quyền kho + có skill trong layout. |
| 2026-06-16 | — | HR: `JobTitle.parent_id` + `in_chart` (sơ đồ tổ chức). Skill `EmployeeSkill` scope mở rộng: cấp trên gán được skill của chức danh cấp dưới (walk parent_id). Migrations `_jobtitle_hierarchy.sql`, `_jobtitle_in_chart.sql`. |
| 2026-06-16 | — | HR Layout: `WorkLayoutJobTitle` (layout ↔ chức danh, để gọi đúng pool người). `WorkLayoutSkill.note` + `WorkAssignmentDemand.note` (ghi chú vị trí). Migrations `20260616_layout_jobtitle.sql`, `20260616_layout_demand_note.sql`. |
| 2026-06-28 | — | Ngoại lệ HSD theo NCC: `ProductionImport.ncc_id` + `InventoryEntry.ncc_id` (UUID FK `TransportCompany`, nullable). Lấy NCC ở phiếu nhập (NCC bắt buộc; SX/chuyển kho tùy chọn) → denormalize xuống pallet khi quét → `effShelfLife(material, ncc_id)` áp `supplier_shelf_life_overrides` cho %Date. Migration `20260628_add_ncc_id_for_shelflife.sql`. |
| 2026-07-08 | — | Multi-tenant ĐV2 tem `;`: `Material.batch_prefix` (TEXT nullable) = 2 ký tự tắt hàng để SINH mã lô V2 từ app (tab Sinh tem). Mã lô = `batch_prefix + yymmdd + Máy + SEQ` (TA260705A018), khớp mã lô kế toán. Ô "Mã tắt (mã lô)" hiện trong form Mã hàng chỉ khi cờ `label_format='semicolon'`. Migration `20260707_material_batch_prefix.sql`. |
| 2026-07-20 | — | **Dữ liệu bên ngoài / DO SAP**: `erp_outbound_orders` thêm `sync_status` (ACTIVE/OBSOLETE — SAP live-pull) + `last_synced_at`. Migration `20260720_erp_sync_status.sql`. Module `external_do_sap` (route `/api/external/do-sap` CRUD + list phân trang/filter/facets); trang FE "Dữ liệu bên ngoài" (Báo cáo). `GroupDeliveryOrder` thêm `priority` (20260720_gdo_priority) + `transport_note` (20260720_gdo_transport_note) từ KHVC. |
| 2026-07-20 | — | **ĐỢT 3 BASE UNIT — "Up raw" SAP (2-tier)**: bảng `erp_outbound_orders` = BẢN SAO NGUYÊN VĂN dòng VL06O (`od_number`+`od_item` unique, `qty_base`=Actual delivery qty theo BASE, `base_unit`, `ship_to_*`, `batch`=mã lô kế toán, `raw` jsonb ôm toàn bộ dòng gốc, `source` EXCEL/SAP). Tầng derived (GDO/DO/OutboundItem) sinh khi Up KHVC (join theo DO). Route `POST /wms/outbound/upload-vl06o` (raw) + `/upload-khvc` (sinh chuyến), quyền `outbound.import`. `uploadExcel` tách `processVehicleGroups` để KHVC tái dùng logic re-upload. Số lượng lưu = BASE (cartons_ordered=qty_base, loose_picking=hộp lẻ). Migration `20260720_erp_outbound_orders.sql` (RLS bật, chặn anon). |
| 2026-06-28 | — | Shelflife theo LÔ: `InventoryEntry.shelf_life_days` (INT nullable). 1 mã + 1 NCC khai được nhiều shelflife (100/200 ngày); chọn lúc quét (selector NCC-biến-thể trong InboundScanSheet) → lưu thẳng trên pallet. %Date ưu tiên `shelf_life_days → override-NCC(khi 1 giá trị) → Material.shelf_life_days`. Chuyển kho kế thừa shelf_life_days + ncc_id từ pallet gốc cùng pallet_code. Migration `20260628_02_inventory_shelf_life_days.sql`. |
| 2026-06-16 | — | HR: `ShiftRestRule` (from_shift→to_shift bị cấm) — luật nghỉ giữa ca, KHÔNG hardcode; auto-assign đọc phân công ngày D-1 + bảng này để loại ca vi phạm. Seed: CA3→{CA1,CA3,HC}. Migration `20260616_shift_rest_rule.sql`. |
| 2026-06-17 | — | HR Phân công: UNIQUE index `(work_date, layout_id)` — mỗi layout chỉ 1 phiếu/ngày. Migration `20260617_uniq_assignment_sheet_per_day.sql`. |
| 2026-06-18 | — | Omni-search bỏ dấu server-side: `CREATE EXTENSION unaccent` + RPC `omni_material_ids(term)` / `omni_location_ids(term)` (unaccent ilike, trả id). Migration `20260618_unaccent_search.sql`. Phục vụ ô tìm kiếm Inventory 1-ô (pallet/mã+tên hàng/vị trí, gõ không dấu vẫn ra). Controller fallback ilike nếu chưa apply. |
| 2026-06-23 | — | Đặt lịch TMS — kế toán slot NGUYÊN TỬ chống overbooking/drift khi hàng trăm user đặt cùng lúc. RPC `book_vehicle_slot(vslot,new_slot,plate,status,actor)` (kiểm sức chứa bằng ĐẾM SỐNG biển-distinct dưới row-lock, KHÔNG tin `booked_count`) + `recount_slot(slot)`. `DeliverySlot.booked_count` thành CACHE hiển thị. CHECK `booked_count BETWEEN 0 AND max_vehicles`. Migration `20260623_atomic_slot_booking.sql` (ĐÃ apply). `try_book_slot` cũ còn trong DB nhưng KHÔNG còn code gọi (dead). Verify: 1800 request đồng thời → 0 overbooking, 0 drift. |
| 2026-06-29 | — | `Warehouse.shipto_codes text[]` — 1 kho nhiều mã ship-to. Auto-detect chuyển kho (`maybeAutoCreateTransferOrder`) khớp `shipto_party = code` HOẶC phần tử trong `shipto_codes`. Migration `20260629_warehouse_shipto_codes.sql` (CHỜ apply). Quản ở form Kho + upload cột shipto_codes; chặn 1 ship-to thuộc >1 kho. |
| 2026-06-29 | — | `TransportCompany.alias_codes text[]` — 1 NCC/ĐVVT nhiều mã ERP. Upload (kế hoạch xuất/nhập, tồn, xe) khớp `code` HOẶC `alias_codes` → cùng 1 id; HSD ngoại lệ + báo cáo gộp. Migration `20260629_ncc_alias_codes.sql` (CHỜ apply) — đồng thời GỘP các bản trùng (type, tên): giữ 1 primary, gom code còn lại vào alias_codes, xóa bản phụ. Quản ở form ĐVVT + upload cột alias_codes; chặn trùng mã giữa các NCC. |
| 2026-06-24 | — | Đăng ký cổng — xe "kết hợp" (vừa Nhập vừa Xuất cùng Loại kho): `gate_registrations.visit_group_id uuid` (+ index `idx_gate_reg_visit_group`). 1 lần đăng ký Hướng="Nhập + Xuất" tách thành **2 record 1 chiều** (INBOUND+OUTBOUND) chung `visit_group_id`. KHÔNG lưu record nào là "BOTH" → cỗ máy match/relink booking giữ NGUYÊN, mỗi chân match như đăng ký 1 chiều → không lỗi TMS booking. Migration `20260624_gate_visit_group.sql` (CHỜ apply). |
| 2026-07-01 | — | `InventoryEntry` — partial UNIQUE index `uq_inventory_active_wh_pallet (warehouse_id, pallet_code) NULLS NOT DISTINCT WHERE status active`. Chống tạo pallet TRÙNG khi quét ĐỒNG THỜI (race read-then-insert scanQR/scanManual). Composite THEO KHO (KHÔNG global) vì hàng no-QR (Loscam/POSM) dùng `pallet_code=mã hàng`, cùng mã ở NHIỀU kho là hợp lệ → global sẽ chặn oan kho thứ 2. scanQR nay set `warehouse_id`; NULLS NOT DISTINCT để entry QR cũ (wh null) vẫn dedup theo pallet_code. Handler đã bắt 23505→409. Migration `20260701_inventory_active_pallet_unique.sql` (apply qua pg CONCURRENTLY). Verify: 6-8 quét cùng mã đồng thời → 1 OK + còn lại 409 (trước fix: N pallet ảo). Phát hiện Mốc 3; sửa phạm vi theo góp ý no-QR. |
| 2026-07-01 | — | `InventoryAdjustmentLog` — đổi `delta`/`cartons_before`/`cartons_after` INTEGER → **numeric**. Trước: cột integer nhưng `cartons_remaining` là numeric (thập phân, vd 7004.875) → INSERT log thất bại, `adjustInventory` nuốt lỗi (console.error) → audit log điều chỉnh tồn KHÔNG BAO GIỜ ghi (0 dòng toàn hệ thống). Không đổi code. Migration `20260701_adjustment_log_numeric.sql` (apply qua pg). Verify: adjust +1 pallet thập phân → log ghi đúng before=7004.875/after=7005.875. Phát hiện Mốc 5 test vận hành. |
| 2026-07-02 | — | Kế hoạch VC cho SỐ LẺ Thùng/Pallet: `TmsOrder.planned_boxes/planned_pallets` + `inbound_plan_lines.planned_boxes/planned_pallets` INTEGER → **numeric** (file KH thật có 515.5 → integer chết "invalid input syntax"). Chỉ bảng KẾ HOẠCH — thực xuất/tồn thật vẫn nguyên. Migration `20260702_planned_qty_numeric.sql` (CHỜ apply). |
| 2026-07-01 | — | **PostgREST aggregate** bật lại: `ALTER ROLE authenticator SET pgrst.db_aggregates_enabled='true'` + `NOTIFY pgrst,'reload config'` (mặc định PostgREST v12 = TẮT). Cho phép `.select('col.sum()')` gom phía DB. `listInventory` tổng thùng tồn = 1 query SUM (tái dùng NGUYÊN `applyInventoryFilters` → tổng khớp tuyệt đối list) thay vì kéo ~4000 dòng về Node (**5.2s→~0.3s**). `listFacets` phân trang SONG SONG (trang 1 lấy count exact + bắn các trang còn lại đồng thời) thay tuần tự (**4.9s→~1.3s**). An toàn dữ liệu: RLS đã có policy `anon_select` (qual=true) trên InventoryEntry/Material/Location → anon vốn đọc hết, aggregate KHÔNG mở thêm dữ liệu. Migration `20260701_enable_pgrst_aggregates.sql` (apply qua pg + DIRECT_URL). |
| 2026-07-05 | — | RPC `move_pallets_to_location` — đếm sức chứa LOẠI pallet tồn=0 (`AND cartons_remaining > 0`). Upload tồn nay nhận tồn=0 (~31k bản ghi IN_STOCK remaining=0 còn gắn location) → pallet hết hàng bị đếm chiếm slot → 134 vị trí báo "đầy" oan. Các count JS cùng họ đã sửa cùng commit (inbound suggest/scan LOCATION_FULL, listLocations `used_slots`/`has_same_material`, getLocation, move fallback) + search tồn khả dụng (fetchMaterialInventory, prepare FEFO) bỏ tồn=0. Migration `20260705_move_pallets_exclude_zero.sql` (CHỜ apply — RPC đang deployed nên tới khi apply, riêng luồng DỒN VỊ TRÍ vẫn đếm cả tồn=0; các luồng JS đã đúng ngay). |
| 2026-07-05 | — | Dashboard tổng quan DATA THẬT: RPC `dashboard_stats(p_warehouse_ids text[], p_categories text[], p_today date)` trả jsonb `{inventory: [kho×loại: pallets/cartons/materials], today: {inbound_orders/inbound_cartons/outbound_gdos/outbound_planned/outbound_scanned}}` — aggregate phía DB (InventoryEntry sẽ hàng triệu dòng). Kiểu cột đã verify: `Warehouse.id`/`Material.id` TEXT, `InventoryEntry.warehouse_id` uuid (join cast `::text`), `ProductionImport`/`GDO.warehouse_id` text. BE `GET /wms/dashboard` (auth-only, cắt scope kho+loại JWT) có FALLBACK JS phân trang khi RPC chưa apply → app chạy được ngay, apply xong tự nhanh. Migration `20260704_dashboard_stats.sql` (CHỜ apply; SELECT tương đương đã test khớp 4001 pallet/454256.213 thùng). |
| 2026-07-05 | — | RPC `outbound_shortage_stats(p_warehouse_id text, p_date date)` — cảnh báo thiếu tồn Xuất/Nhặt lẻ theo (kho, ngày giao): demand = còn phải xuất (đặt−quét) MỌI đơn chưa hủy trong ngày theo mã; available = tồn LOẠI QA giữ (`qa_status_id IS NULL`) + LOẠI QUARANTINE (kể entry location NULL qua `ie.warehouse_id::text`); planned = KH nhập (`inbound_plan_lines` ACTIVE, ngày hôm-nay-VN→ngày giao) TRỪ thực đã nhập từng chuyến (`InventoryEntry.cartons_imported` qua ProductionImport→`tms_order_id` hoặc gate→order). BE `GET /wms/outbound/shortages` (level 1 = tồn+KH đủ / 2 = vẫn thiếu) trả rỗng nếu RPC chưa apply. Migration `20260705_outbound_shortage_stats.sql` (CHỜ apply; SELECT tương đương verify Ba Vì 04/07: mã 157 demand 699/avail 420.283). |
| 2026-07-07 | — | **Multi-tenant cờ hệ thống + QR v2 (tem `;` ĐV2)**: bảng `SystemSetting` (key TEXT PK, value jsonb, updated_by, updated_at — realtime; cờ đầu tiên `label_format` 'underscore'/'semicolon' chỉ chiều IN tem) + `InventoryEntry.batch` TEXT (mã lô nguyên văn) + `InventoryEntry.expiry_date` DATE (HSD tường minh từ tem) + partial index `idx_inventory_entry_batch`. Parser `qrParser.ts` 2 nhánh theo delimiter (v2: 7 đoạn trim space, QA 1=OK/khác=X, Máy+SEQ trích từ mã lô, NSX/HSD dd/mm/yyyy parse thành phần); `normalizeQR` áp mọi điểm quét (inbound/outbound/stocktake/pallet-ops). Migration `20260707_systemsetting_qr_v2.sql` (ĐÃ apply STAGING 07/07; production LOF apply khi merge main). |
| 2026-07-10 | — | **Loại kho TÙY BIẾN (multi-tenant)**: `LookupValue.meta` jsonb NOT NULL DEFAULT '{}' — cờ hành vi per-loại (`is_ncc_goods` thay NCC_CATEGORIES hardcode, `requires_shelf_life`/`requires_pallet_per_ea` thay luật form Mã hàng, `batch_char` = ký tự thế chỗ Máy trong mã lô khi Sinh tem V2 (ĐV2: Nguyên liệu='N'), `badge_color`). Seed cờ 5 loại hiện có = đúng hành vi cũ. RPC `rename_warehouse_type(old,new)` SECURITY DEFINER — đổi tên loại kho CASCADE 1 giao dịch 11 cột text: LookupValue + Material/Location/WarehouseZone.category + Employee.allowed_categories[] + SlotTemplate/DeliverySlot.cargo_type + TmsOrder/GDO/gate_registrations/inbound_plan_lines/ProductionImport.warehouse_type (REVOKE anon/authenticated — chỉ service role gọi qua PUT /wms/lookup/:id). Migration `20260710_warehouse_type_options.sql` (CHỜ apply staging → production khi merge; BE listLookup select('*') nên chưa apply vẫn chạy, chỉ lưu cờ/đổi tên cần migration). |
| 2026-07-10 | — | **Chế độ quản tồn thứ 4 QTY_DATE (tồn số lượng theo NSX)**: nới CHECK `warehouse_inventory_mode_check` thêm 'QTY_DATE' + đổi unique index `uq_inventory_active_wh_pallet` từ (warehouse_id, pallet_code) → (warehouse_id, pallet_code, COALESCE(production_date,'1900-01-01')) NULLS NOT DISTINCT (partial active) — kho QTY_DATE có NHIỀU dòng pool active cùng mã khác NSX; QR/QTY giữ nguyên tính chống trùng (QR: trùng code ⇒ trùng NSX; QTY: NSX null ⇒ vẫn 1 dòng). BE: `isQtyLike` (inventoryMode.ts) — QTY_DATE hành xử như QTY mọi nhánh; scanManual bắt `production_date` (422 NSX_REQUIRED) + pool key theo NSX; `applySharedPoolDelta` FEFO (NSX cũ trước) + tham số chọn NSX; `getManualItemStock` trả `date_pools`. Migration `20260710_inventory_mode_qty_date.sql` (CHỜ apply staging → production khi merge; chưa apply thì chọn QTY_DATE khi lưu kho sẽ vướng CHECK cũ). |
| 2026-07-10 | — | **Kho phụ nội bộ (tổ sản xuất tại site)**: `Warehouse.parent_warehouse_id` TEXT FK → Warehouse (Warehouse.id là TEXT — bản uuid đầu bị 42804; NULL = kho thường; BE validate không lồng 2 cấp/không tự trỏ). Kho phụ chỉ giao dịch với kho parent: guard outbound 2 chiều (`internalOrbitError` — đích kho phụ chỉ parent xuất tới; nguồn kho phụ chỉ shipto=parent hoặc tiêu hao không shipto), chặn inbound `createOrder` NCC/FACTORY (chỉ nhận qua chuyển kho từ parent), `maybeAutoCreateTransferOrder` hạ về OTHER khi tên/shipto trùng kho phụ site khác. Cặp parent↔con được nới: biển số TÙY CHỌN (`isInternalPair` — startGDO/quickExport/quickExportExisting) + `confirmTransferReceipt` MIỄN gác booking ĐVVT. Migration `20260710_warehouse_parent.sql` (ĐÃ apply STAGING 10/07; production khi merge). |
| 2026-07-11 | — | **Nhận chuyển kho vào kho QTY_DATE — NSX kế thừa từ tem quét xuất**: `ProductionImport.transfer_production_date date` (NULL = phiếu không gắn NSX). `confirmTransferReceipt`/`createOneInbound`: kho nhận QTY_DATE → tách 1 phiếu / (mã × NSX) từ `OutboundScanEntry.production_date` của GDO nguồn (Σ thùng THỰC QUÉT per NSX, planned_pallets = số pallet để đối chiếu tem; hàng no-QR/GDO không tem → 1 phiếu NSX null, gõ tay lúc nhận). `scanManual`: phiếu có NSX → dùng NSX PHIẾU làm production_date pool (body chỉ là fallback). FE panel Nhận (TMSBookings): dòng NSX per phiếu + nút **"Nhận đủ theo xuất"** 1 chạm (lưu + hoàn thành mọi phiếu OPEN theo đúng số xuất, 409 lành tính bỏ qua); InboundDetail dialog lưu thủ công hiện NSX cố định. Migration `20260711_transfer_nsx_per_import.sql` — ⚠ ORDER_SELECT inbound đã select cột này: PHẢI apply STAGING ngay khi deploy (chưa apply → list/chi tiết phiếu nhập lỗi 400); production khi merge. |
| 2026-07-12 | — | **Sơ đồ xếp xe 3D (Xuất kho)**: `Material.carton_length_cm/carton_width_cm/carton_height_cm` numeric (kích thước thùng carton, cm) + `VehicleType.box_length_cm/box_width_cm/box_height_cm` numeric (lòng thùng xe, cm). Nhập ở form Mã hàng ("Thùng D×R×C") + upload Excel Mã hàng (3 cột CUỐI sau batch_prefix — template luôn xuất đủ cột giữ vị trí M_KEYS) + form Loại xe TMS. Thuật toán xếp `frontend/utils/loadPlan.ts` (cột chồng cùng mã + shelf packing, chạy thuần FE) + viewer Three.js lazy `LoadPlan3DDialog` (nút "Xếp xe 3D" trang chi tiết chuyến xuất, đọc-only). Mã thiếu kích thước → cỡ giả định 40×30×25 + cảnh báo. Migration `20260712_load_plan_dims.sql` — ⚠ getGDO đã select 3 cột Material: PHẢI apply STAGING ngay khi deploy (chưa apply → chi tiết chuyến xuất 404 + tạo/sửa Mã hàng lỗi); production khi merge. |
| 2026-07-12 | — | **Xếp xe 3D đợt 2 — luật xếp chồng (user chốt)**: `Material.max_stack_layers` integer (số lớp tối đa 1 chân, null = theo trần xe) + `Material.stack_on_top` boolean NOT NULL DEFAULT false (hàng nhẹ được xếp TRÊN mã khác, ưu tiên lên nóc). Thuật toán loadPlan.ts viết lại: theo ĐƠN (hết DO1 mới DO2) → chân cùng loại cao đều (chênh ≤1 lớp, thùng dư tự nằm nóc) → 1 dãy chỉ 1 (đơn×kích thước) → hàng nhẹ lên nóc chân đã xếp (cùng đơn trước) → hạ lớp đều để trải HẾT chiều dài xe khi hàng ít. Migration `20260712_load_plan_stack.sql` (ĐÃ apply STAGING qua pg; production khi merge — getGDO select 2 cột này). |
| 2026-07-26 | — | **Map SAP → kho để SIẾT upload VL06O theo kho (user chốt 26/07)**: `Warehouse.sap_plant text` (mã nhà máy SAP — cột **Plant** trong VL06O, vd `1102`) + `Warehouse.sap_storage_locations text[] NOT NULL DEFAULT '{}'` (các **Storage Location** thuộc kho, vd FG01/FG02/PM01; **rỗng = mọi sloc của plant đó thuộc kho này**) + index `idx_warehouse_sap_plant`. Vì sao cần: dòng VL06O mang mã SAP, KHÔNG khớp `Warehouse.code` (`20000016`) nên không thể suy ra kho để guard. Khai ở **form Kho (Cài đặt WMS)** 2 ô Plant SAP + Storage Location. `uploadVl06o` khớp (plant, sloc) trước → fallback theo plant; dòng map ĐƯỢC mà kho ngoài phạm vi user → **403 all-or-nothing** (chưa ghi gì); dòng CHƯA khai map → **cho qua + đếm `sap_unmapped` + cảnh báo nhắc khai** (fail-open CÓ CHỦ ĐÍCH — fail-closed sẽ chặn upload tới khi khai đủ map = chặn vận hành). Verify vai thật 4/4: kho ngoài scope 403 & 0 dòng raw · kho của mình vẫn up được · plant chưa khai cho qua + cảnh báo · file TRỘN 2 kho chặn toàn bộ. Migration `20260726_warehouse_sap_mapping.sql` (ĐÃ apply STAGING qua pg 26/07; production khi merge — cột additive). |
| 2026-07-26 | — | **SỬA HỒI QUY của `20260725_upload_concurrency` + fix 500 ô tìm kiếm (check-app 26/07)**: (1) `uq_tms_order_inbound_group` (unique date+kho+loại+loại xe+NCC cho MỌI lệnh INBOUND) **CHẶN OAN nghiệp vụ hợp lệ** — 1 NCC giao 2 xe cùng ngày/loại xe là bình thường; verify runtime: tạo đơn tay thứ 2 → 409 *"Mã đơn đã tồn tại"* (sai nghĩa), upload KH vận chuyển 2 lệnh cùng nhóm → ghi 0/2. Thay bằng cột **`TmsOrder.plan_group_key text`** (khóa `ngày\|\|kho\|\|loại kho\|\|loại xe\|\|NCC`) **CHỈ luồng upload KH nhập set** + `uq_tms_order_plan_group` UNIQUE(plan_group_key) WHERE plan_group_key IS NOT NULL AND status<>'CANCELLED' ⇒ lệnh tạo tay / KH vận chuyển để NULL nên không bị ràng buộc, chống-đua của luồng gộp vẫn còn (race 14/14 pass). **KHÔNG backfill lệnh cũ (cố ý)**: key chỉ cần cho việc TẠO MỚI; backfill sẽ chặn oan các nhóm cũ vốn có 2+ lệnh. Migration `20260726_plan_group_key.sql`. (2) 3 RPC `omni_narrow_material_ids` / `omni_narrow_location_ids` / `omni_narrow_import_material_ids` (DISTINCT id thực có trong `InventoryEntry`/`ProductionImport`) — omni-search nhét id vào filter `material_id.in.(…)`, term ngắn phổ biến ("51"→371 mã, "-"→453, "_"→374, "a"→500) làm URL >13KB → PostgREST từ chối → **API 500 trắng trang Tồn kho/Nhập kho**; thu hẹp về id thực có dữ liệu (đo: "-" 453→38) KHÔNG mất dòng, vượt trần 300 thì trả **400 kèm thông báo** thay vì cắt âm thầm. Migration `20260726_omni_search_narrow.sql`. (Cả 2 ĐÃ apply STAGING qua pg 26/07; production khi merge — additive + drop index sai, an toàn.) |
| 2026-07-12 | — | **Xếp xe 3D — đổi đơn vị cm → mm**: RENAME `Material.carton_*_cm`→`carton_*_mm` + `VehicleType.box_*_cm`→`box_*_mm`, dữ liệu đã nhập ×10. Toàn tuyến (BE/FE/form/template/3D) dùng mm. Migration `20260712_load_plan_mm.sql` (ĐÃ apply STAGING; production chạy SAU 2 migration load_plan kia khi merge). |
| 2026-07-16 | — | **Control Tower (Giám sát vận hành)**: RPC `control_tower_stats(p_warehouse_ids text[], p_categories text[], p_today date)` trả jsonb {gate: đếm theo status + inside_list top 40 kèm entry_at (dwell tính FE), outbound: đếm GDO theo status + planned/scanned + active top 40 kèm tiến độ, inbound: orders/pallets/cartons hôm nay, weigh: tickets/pending2/net_kg, hourly: thùng xuất + pallet nhập theo giờ VN} — aggregate phía DB, mọi cắt scope null-inclusive. Kèm 2 index MỚI `idx_ose_scanned_at` (OutboundScanEntry.scanned_at) + `idx_ie_created_at` (InventoryEntry.created_at) cho truy vấn theo-ngày. BE `GET /wms/control-tower` (permission MỚI control_tower.view) trả 503 NOT_READY khi RPC chưa apply. Migration `20260716_control_tower_stats.sql` (CHỜ apply staging → production khi merge). |
| 2026-07-16 | — | **Control Tower v2 — chiều hàng hóa (user chốt sau bản 1)**: RPC `control_tower_stats` REPLACE thêm: `outbound.loose_planned/loose_scanned` (nhặt lẻ KH + đã quét lẻ), `out_by_material` + `in_by_material` (top 15 mã theo KH/thùng + n_materials), `out_active[].npp` (string_agg distinct NPP) + `n_materials`. FE: progress strip KH/đã xuất/còn/% + 2 khối hàng theo mã + tile Mã hàng xuất/Nhặt lẻ/Tỷ lệ lẻ. Migration `20260716_control_tower_stats_v2.sql` (chạy đè bản v1 — CHỜ apply staging; production khi merge chỉ cần chạy v2 + 2 index từ file v1). |
| 2026-07-17 | — | **Control Tower v3 — nghìn mã/ngày + filter Loại kho (user 17/07)**: RPC `control_tower_stats` REPLACE — `out_by_material` sort **(ordered−scanned) DESC** (mã còn thiếu nổi lên, mã đủ chìm) top 30 + `n_done`/`n_short`; `in_by_material` top 30 + cắt p_categories theo Material.category (null-inclusive). Controller nhận `?categories=` ∩ scope JWT (403 nếu chọn ngoài scope). FE: filter Loại kho (useScopedWhTypes) + chips "đủ/còn thiếu" trên header khối + footer ghi chú top-30. Migration `20260717_control_tower_stats_v3.sql` (đè v2 — CHỜ apply staging; production khi merge chạy index v1 → v3). |
| 2026-07-17 | — | **FIX lệch giờ Nhịp độ theo giờ (control_tower_stats)**: `OutboundScanEntry.scanned_at` + `InventoryEntry.created_at` = timestamp NAIVE chứa UTC → biểu thức `col AT TIME ZONE VN` hiểu nhầm naive là giờ VN (quét 10:32 VN hiện cột 20h). Chuẩn naive-UTC: `col AT TIME ZONE UTC AT TIME ZONE VN` + biên ngày t0/t1 naive-UTC (không phụ thuộc session TZ). Migration `20260717_control_tower_hourly_tz_fix.sql` (đè v3, cùng chữ ký). BÀI HỌC: cột giờ bảng nghiệp vụ là naive-UTC — RPC mới đụng giờ VN phải dùng chuỗi 2 bước AT TIME ZONE. |
| 2026-07-17 | — | **Control Tower v5 — cột hiển thị (user)**: gate_inside thêm `warehouse_type` + `vehicle_type` (Xe trong cổng hiện Loại kho/Loại xe/Hướng); out_active thêm `warehouse_type` (GDO) + `export_type` (string_agg distinct từ OutboundItem — Loại xe của chuyến). Migration `20260717_control_tower_stats_v5.sql` (đè tz_fix, cùng chữ ký). Production khi merge: index v1 → chạy thẳng v5 (bỏ qua v2/v3/tz_fix). |
| 2026-07-17 | — | **Slotting (Tối ưu vị trí — mục 6 roadmap)**: `WarehouseZone.pick_rank` integer (hạng nhặt khu, 1 = gần cửa xuất nhất, NULL = không tham gia gợi ý — sửa trong tab Khu vực). Bảng MỚI `SlottingPlan` (id/warehouse_id/name/status ACTIVE-COMPLETED-CANCELLED/note/window_days/n_lines/created_by/completed_at-by/updated_at-by) + `SlottingPlanLine` (plan_id FK ON DELETE CASCADE, inventory_entry_id, pallet_code, material_code/name, abc, reason, from/to_location_id+code snapshot) — trạng thái dòng KHÔNG lưu, SUY SỐNG từ InventoryEntry.location_id hiện tại (DONE/PENDING/MOVED_OTHER/GONE). RLS enable + realtime publication 2 bảng. RPC `slotting_stats(p_warehouse_id text, p_categories text[], p_days int)` trả jsonb {materials: ABC theo LƯỢT NHẶT lũy-kế-trước-mã 80/95 + tồn theo khu, placement, zones (capacity/used), locations (used_slots)} — cửa sổ ngày so naive-UTC 2 bước. Migration `20260717_slotting.sql` (CHỜ apply staging → production khi merge; chưa apply: trang Slotting 503 NOT_READY + tab Khu vực lỗi select pick_rank). |
| 2026-07-18 | — | **Slotting v2 (bản CUỐI — user chỉnh 2 vòng)**: `WarehouseZone.flow_type` (SAME_END/FLOW_THROUGH — luồng cửa) + `SlottingPlan.level/principle`. Khu đặc thù (SCA lạnh…) KHÔNG thêm trường — dùng LOẠI KHO có sẵn (user bác slot_group vì khớp chuỗi tay): engine siết khu-có-Loại chỉ nhận mã đúng Loại + cảnh báo WRONG_CATEGORY + checkbox pull_wrong_zone lúc tạo KH; file có DROP COLUMN IF EXISTS slot_group (dọn nếu lỡ apply bản giữa). **DROP + tạo lại `SlottingPlanLine`**: dòng GOM (mã + date_key) — material_id, n_pallets, entry_ids jsonb (tiến độ x/N sống), flow_note. RPC REPLACE cùng chữ ký (+ zones.flow_type). Mức độ Easy/Normal/Hard + FIFO/FEFO/LIFO = FILTER trên trang. Cấu hình khu (pick_rank/flow_type) qua route riêng PATCH /wms/slotting/zone-config/:id (permission MỚI slotting.configure — tab Cài đặt trong trang, gỡ khỏi Cài đặt WMS). Migration `20260718_slotting_v2.sql` — chạy SAU 20260717_slotting.sql (CHỜ apply staging; production khi merge 2 file tuần tự). |
| 2026-07-18 | — | **Slotting — 2 cờ per VỊ TRÍ (user bổ sung)**: `Location.slot_no_in` boolean NOT NULL DEFAULT false (vị trí KHÔNG đưa hàng vào — kho tạm: không làm đích, hàng nằm đó LUÔN sinh lệnh kéo đi ưu tiên P1 sau kéo-sai-loại, kể cả khi không tick pull_wrong_zone) + `Location.slot_no_out` boolean NOT NULL DEFAULT false (vị trí KHÔNG lấy hàng đi — hàng kẹt: loại hoàn toàn khỏi nguồn, vẫn tính chiếm sức chứa). Chỉnh trong tab Cài đặt trang Tối ưu vị trí (2 multi-select + nút Lưu, replace-all per kho qua PUT /wms/slotting/location-config — permission slotting.configure). RPC `slotting_stats` REPLACE cùng chữ ký (+ locations.slot_no_in/slot_no_out). Migration `20260718_slotting_locations.sql` — chạy SAU 2 file slotting trước (CHỜ apply staging; production khi merge chạy 3 file tuần tự). |
| 2026-07-20 | — | **BASE UNIT Đợt 2 — SEMANTIC FLIP (user duyệt 20/07, ĐÃ CHẠY STAGING)**: đổi NGHĨA các cột số lượng sang BASE UNIT (×units_per_carton, CHỈ mã có entry_unit): InventoryEntry (cartons_imported/remaining/reserved/adjustment_qty) · OutboundItem (cartons_ordered/scanned/loose_picking) · OutboundScanEntry.cartons_scanned · InventoryAdjustmentLog (delta/before/after) · ProductionImport (planned_cartons/posm_cartons) · inbound_plan_lines.planned_boxes. KHÔNG nhân: TmsOrder.planned_boxes (cache thùng quy đổi cross-mã) · PalletLabelPrint.qty · pallets/weight/tons. RPC REPLACE cùng migration: dashboard_stats/control_tower_stats/slotting_stats (tổng = thùng quy đổi) + get_outbound_scan_log/search (thêm 3 cột units). Backup x_flip_bak_* + báo cáo round base_unit_flip_round_report (67 giá trị, tổng lệch <5 hộp). Migration `20260719_base_unit_flip.sql` chạy qua `scripts/base-unit-flip/run-flip.mjs` (flip/verify/rollback) — STAGING 20/07 verify 0 lệch; **PRODUCTION: chạy CÙNG CỬA SỔ với merge main** (code base-semantics + guard qty_semantics). |
| 2026-07-19 | — | **BASE UNIT Đợt 0 — khai báo ĐVT trên Mã hàng**: `Material.base_unit` text (đơn vị GỐC — lưu trữ/tính toán sau semantic flip Đợt 2) + `Material.entry_unit` text (đơn vị NHẬP LIỆU, chỉ hiển thị; hệ số 1 Entry = N Base dùng lại `units_per_carton`). Backfill theo rule user: TP+SCA → HOP (41 mã "chai" → BT), entry CAR khi có hệ số (5 mã thiếu hệ số → entry NULL); Pallet Loscam → EA không entry; loại khác → đơn vị hiện tại (Kg/KG chuẩn hóa KG), không entry. Form + upload Mã hàng (2 cột CUỐI template) + cổng integration materials trả thêm 2 field. Kế hoạch tổng: `docs/plans/BASE_UNIT_EXECUTION_PLAN.md`. Migration `20260719_material_base_unit.sql` (ĐÃ apply STAGING qua pg 19/07; production khi merge — cột additive, chưa apply chỉ thiếu field, form vẫn chạy). |
| 2026-07-19 | — | **Sức chứa khu vực kho (Dashboard)**: `WarehouseZone.max_pallets` integer — pallet tối đa KHAI TAY tại khu (user chốt 19/07: không cộng tự động từ Σ Location.max_pallets; NULL = chưa khai). Khai ở form Khu vực (Cài đặt WMS, quyền manage_zone). Card "Sức chứa khu vực kho" trên Dashboard so pallet tồn vs số này; pallet tồn quy đổi: mã có `Material.pallet_per_ea` → Σ tồn × pallet_per_ea, mã không có → đếm entry active gắn vị trí. Migration `20260719_zone_max_pallets.sql` (ĐÃ apply STAGING qua pg 19/07; PRODUCTION: cột đã TỒN TẠI SẴN trên DB LOF — verify sau merge 19/07 select/ghi/hoàn tác OK, KHÔNG phải chạy gì thêm). |
| 2026-07-18 | — | **Slotting — fix lệch sức chứa (test hội tụ của user)**: `slotting_stats.loc_used` đếm chỗ đã dùng CHỈ `stack_layer=1` trong khi RPC `move_pallets_to_location` (gác cổng chuyển vị trí) đếm MỌI tầng → engine tưởng đích còn chỗ, thực hiện dính 400 LOCATION_FULL oan. RPC REPLACE cùng chữ ký, thay đổi DUY NHẤT: bỏ `AND ie.stack_layer = 1` trong loc_used (used_slots = đúng thước đo move RPC). Migration `20260718_slotting_capacity_fix.sql` — chạy SAU 3 file slotting trước (production khi merge: 4 file tuần tự). |
| 2026-07-20 | — | **BỎ ĐVT (Material.unit) khỏi TOÀN APP — thay bằng Base Unit + Entry Unit (user chốt 20/07)**: (1) **DROP COLUMN `Material.unit`** — đơn vị mã hàng nay = `base_unit` (gốc) + `entry_unit` (thùng); mọi nơi cần "đơn vị" suy qua helper TẬP TRUNG `unitCodeOf(m)=entry_unit\|\|base_unit` (BE+FE mirror `utils/qtyUnits.ts`). BE bỏ `unit` khỏi mọi select/ghi (materialController create/update/upload — M_KEYS giữ index 3 làm FILLER vị trí để file Excel cũ không lệch cột; orderController 5 select + 4 output→unitCodeOf; outboundController select; exportController vẫn TRẢ field `unit` cho ERP nhưng suy entry\|\|base — giữ contract). FE Materials (form/pane/bảng/template/validate) + TMS/Inbound/Outbound đọc `.unit`→unitCodeOf. (2) **Danh mục Đơn vị tính** = `LookupValue` type `unit_of_measure`, `meta.role` base/entry/both (+label) — tab MỚI "Đơn vị tính" trong Cài đặt WMS (permission `wms_settings.manage_unit`; routes `/wms/lookup-unit/*` KHÓA type để manage_unit không sửa được loại kho). Materials: Base Unit (role base/both, BẮT BUỘC) + Entry Unit (role entry/both) là SingleSelect lấy từ danh mục; chặn Base=Entry (FE+BE — nếu bằng nhau nhân EA/thùng sai). Migration `20260720_unit_of_measure_seed.sql` (seed CAR=both/HOP/BT/BAG/KG/EA=base — ĐÃ apply STAGING qua pg 20/07) + `20260720_drop_material_unit.sql` (⚠ áp SAU khi deploy code không còn select unit — production khi merge). |
| 2026-07-20 | — | **Mã PHI HÀNG HÓA (chiết khấu/dịch vụ, vd 910000060) — user chốt 20/07**: `Material.is_non_stock` boolean NOT NULL DEFAULT false. Cờ ở ĐẦU form Mã hàng — tích thì CHỈ cần Mã + Tên (validate FE bỏ qua các field vận hành, các field đó ẩn). App LOẠI mã khỏi: **Xuất** (`processVehicleGroups` skip khi build byNpp → không sinh dòng cần quét + không validate dòng; chuyến chỉ toàn mã này → skip không tạo chuyến rỗng; VẪN lưu ở raw `erp_outbound_orders` để đối chiếu SAP) + **picker chọn hàng** Outbound/TMS(plan+reconcile)/Inbound (lọc `!is_non_stock`) + **Tồn** (tự vắng vì không nhập/quét). Badge "phi hàng hóa" ở list + detail. Migration `20260720_material_non_stock.sql` (ĐÃ apply STAGING qua pg 20/07; production khi merge — cột additive). |
| 2026-07-21 | — | **Reconcile SAP↔WMS Đợt 1 — tầng raw "Kế hoạch xuất" + liên kết OD trên dòng**: (1) bảng MỚI `khvc_lines` (id/group_code/do_no UNIQUE(group_code,do_no)/warehouse_code/npp/veh_type/dvvt/priority/cs/note/export_date/source EXCEL·SAP·APP/sync_status ACTIVE·REMOVED/gdo_id/raw jsonb/uploaded_by/created_at/updated_at) + 4 index + RLS + realtime publication — TẦNG 2 raw song song `erp_outbound_orders`, `uploadKhvc` lưu song song (churn-safe id theo (group_code,do_no), upsert chunk 500); tab "Kế hoạch xuất" (module quyền MỚI `external_khvc` — 5 nơi). (2) `OutboundItem.od_refs jsonb NOT NULL DEFAULT '[]'` = `[{od_number,od_item,qty_base}]` — liên kết ngược dòng chuyến↔dòng OD SAP (populate từ luồng KHVC qua mergeNppRows; file gộp trực tiếp → []), nền cho engine reconcile Đợt 2 (Σqty_base=cartons_ordered). (3) `OutboundDelivery.delivery_code` DROP NOT NULL (linkage OD chuyển sang od_refs; delivery_code còn ghi cho tương thích). Kèm code: `uploadVl06o` nạp CÓ SO SÁNH (INSERT/UPDATE/NO-OP theo hash cột nghiệp vụ, GIỮ id cũ khi UPDATE — vá churn PK, KHÔNG auto-OBSOLETE, header guard cột Delivery/Item). 3 migration `20260721_khvc_lines.sql` + `20260721_outbound_item_od_refs.sql` + `20260721_outbound_delivery_code_nullable.sql` (ĐÃ apply STAGING qua pg 21/07, verify E2E 12/12 PASS; production khi merge main chạy 3 file — additive, an toàn). |
| 2026-07-21 | — | **FIX REALTIME "Dữ liệu bên ngoài" + đối chiếu (bug thiếu RLS policy — cùng lớp WeighTicket 16/07)**: 3 bảng `khvc_lines`/`erp_outbound_orders`/`reconcile_tasks` bật RLS nhưng THIẾU `rls_auth_select` → Supabase Realtime (kết nối role=authenticated qua `setRealtimeAuth`) KHÔNG nhận Postgres-changes event → tab DO SAP/Kế hoạch xuất/Cần xử lý KHÔNG tự cập nhật (BE service-role vẫn đọc → REST không lỗi, realtime chết ÂM THẦM). Fix: `CREATE POLICY rls_auth_select FOR SELECT TO authenticated USING(true)` cả 3 bảng (GIỐNG các bảng realtime-OK; anon vẫn chặn; scope dữ liệu thật vẫn ở controller BE). Migration `20260721_external_rls_realtime_policy.sql` (idempotent, ĐÃ apply STAGING qua pg 21/07; verify sống: xóa khvc → cột "Số xe (KH)" DO SAP đổi realtime KHÔNG refresh; INSERT chứng minh qua anon-policy tạm rồi gỡ — local anon vì thiếu SUPABASE_JWT_SECRET, Preview/production authenticated → chạy đủ). **PRODUCTION khi merge: chạy file này.** ⚠️ NỢ NGOÀI PHẠM VI (sweep cùng dấu hiệu): `ApiKey` (in pub, chưa map realtime) + `Employee` (map `['employees']` nhưng KHÔNG trong publication → realtime nhân sự không chạy) — user quyết fix riêng. |
| 2026-07-21 | — | **Reconcile SAP↔WMS Đợt 2 — engine đối chiếu + hàng chờ "Cần xử lý"**: bảng MỚI `reconcile_tasks` (id/item_id/gdo_id/group_code/material_code+name/od_number+od_item/change_type[QTY_INCREASE·QTY_DECREASE·LINE_REMOVED·MATERIAL_CHANGED·ATTR_CHANGED]/zone[Z1-Z4]/action[AUTO_APPLIED·NEEDS_REVIEW·BLOCKED·RECONCILE_ONLY]/status[OPEN·RESOLVED]/old_ordered·new_ordered·scanned/detail/actor/resolution·resolved_by·resolved_at) + 3 index + RLS + realtime publication. Engine `services/outboundReconcile.ts` `reconcileFromSap(changedKeys)`: tìm item qua `od_refs` (jsonb `cs` filter — **`.contains` supabase-js serialize SAI array-of-object → dùng `.filter('od_refs','cs',JSON.stringify(...))`**), phân vùng theo cartons_scanned + GDO.scan_completed_at, TỰ ÁP khi scanned=0 (Z1/Z2) / đẩy hàng chờ khi scanned>0 (Z3: giảm<quét=BLOCKED, còn lại NEEDS_REVIEW) / RECONCILE_ONLY khi đóng (Z4) / ATTR tự áp mọi vùng. Trigger: `uploadVl06o` (updated keys + dòng-thiếu-DO-có-mặt→OBSOLETE) + `updateDoSap`/`deleteDoSap`/bulkDelete — đều bọc try/catch (AUGMENT, không làm hỏng thao tác gốc). API `GET /wms/outbound/reconcile-tasks(+/count)` + `POST .../:id/resolve` (apply chỉ khi new≥scanned, else 422; keep; manual_done) quyền MỚI `outbound.reconcile`. Migration `20260721_reconcile_tasks.sql` (ĐÃ apply STAGING qua pg 21/07, verify E2E 12/12 + UI Playwright PASS; production khi merge — bảng mới, additive). |
| 2026-07-22 | — | **Index GIN `OutboundItem.od_refs`** (`idx_outbound_item_od_refs_gin`, jsonb_path_ops): các truy vấn containment `od_refs @> [{od_number}]` của guard xóa DO SAP (`classifyDoSapDelete`) + engine reconcile đang seq-scan bảng triệu dòng — thêm index cho scale. Kèm code cùng đợt: `classifyDoSapDelete` bỏ N+1 tuần tự (song song lô 8 + chỉ lấy dòng `cartons_scanned>0`), enrich `used` của `listDoSap` chunk 40/khvc chunk 100 (né cap-1000 âm thầm → badge "Chưa dùng" sai), list thêm param `od_number_eq`/`group_code_eq` (editor gom khớp CHÍNH XÁC). Migration `20260722_od_refs_gin_index.sql` (ĐÃ apply STAGING qua pg 22/07; production khi merge — index additive, an toàn). |
| 2026-07-22 | — | **Cột `manual_edited_at timestamptz` trên `erp_outbound_orders` + `khvc_lines`** (user 22/07: đánh dấu dòng bị SỬA TAY bằng symbol ✎ sau số DO ở 2 tab Dữ liệu bên ngoài). SET khi PUT/POST tay (`updateDoSap`/`createDoSap`/`updateKhvc`/`createKhvc`); RESET NULL khi upload đè lại (uploadVl06o dòng UPDATE — dòng NO-OP giữ nguyên cờ; uploadKhvc upsert). Kèm code cùng đợt (không đổi schema): nút Sửa/Bỏ chọn theo selection dời LÊN TOOLBAR (hết layout-shift), BỎ Xóa bulk ngoài list (xóa CHỈ trong editor), editor gom rộng 96vw/1280px + 1 dòng nowrap + LUÔN có cột DO/Số xe + bỏ nút sửa-chi-tiết-dòng (DoSapForm đã xóa), paste block Excel mọi cột 2 editor. Migration `20260722_manual_edited_at.sql` (ĐÃ apply STAGING qua pg 22/07; production khi merge — cột additive, an toàn). |
| 2026-07-22 | — | **Cột `Material.is_pallet_carrier boolean NOT NULL DEFAULT false`** (user 22/07: dòng Pallet Loscam trong đơn xuất CHÍNH LÀ pallet chứa hàng bên trên → cộng vào đếm Pallet của chuyến = DOUBLE). Cờ per-mã (không if theo tên/loại): `listGDOs.palletsOf` bỏ qua mã carrier khi đếm Pallet (tile pane/cột list/SummaryBand); mã VẪN nằm ở Tổng (k QR) để giao nhận đếm tấm. Checkbox trong form Mã hàng (dưới cờ phi hàng hóa) + badge detail; tick cờ → validate BỎ bắt buộc HSD/Pallet-EA (pallet không có HSD). Backfill `short_name ilike '%pallet loscam%'` (mã 810000000). Migration `20260722_material_pallet_carrier.sql` (ĐÃ apply STAGING qua pg 22/07; production khi merge — cột additive). |
| 2026-07-23 | — | **RPC `adjust_inventory_atomic`** (test tải 23/07 phát hiện: `adjustInventory` CAS update tồn RỒI insert `InventoryAdjustmentLog` ở 2 bước riêng → request dính 504 xen giữa = tồn đổi nhưng THIẾU dòng log = audit hụt + retry trừ 2 lần). RPC row-lock `FOR UPDATE` gộp đọc–tính–update tồn/`adjustment_qty`/status + insert log trong 1 transaction, trả `OK|NOT_FOUND|NEGATIVE`; controller gọi RPC trước, fallback CAS+log cũ khi RPC chưa deploy. Migration `20260723_adjust_inventory_atomic.sql` (ĐÃ apply STAGING qua pg 23/07, verify tải ~2.5k adjusts: 80/80 pallet 0 drift log↔adjustment_qty; production khi merge main). |
| 2026-07-23 | ✅ PRODUCTION | **GO-LIVE base-unit cutover (merge dev→main `177eb91a`)** — apply LÊN DB PRODUCTION LOF toàn bộ 20 migration `dev` (Pha A 17 additive theo thứ tự phụ thuộc → sync 47 nhãn base/entry từ staging, upc drift 0 + flip-scope 0 lệch → Pha B `run-flip` production trong 1 transaction 14/14 cột 0 lệch, round 67 dòng tổng lệch ~10 base, giữ `x_flip_bak_*` + `base_unit_flip_round_report` cho rollback → RPC `20260722_dashboard_control_tower_baseunit` → Pha C `drop_material_unit`). Verify 4 tầng: DB (flip đúng) · RPC dashboard (thùng quy đổi hợp lý) · code (route mới 401=live) · app-smoke live 200 (materials/inventory/dashboard/outbound, không 500 sau drop unit). QA suite staging XANH trước cutover. Backup x_flip_bak_* GIỮ tới khi user xác nhận xóa. |
| 2026-07-23 | — | **4 index composite đọc-dưới-tải-ghi** (đo 23/07: nhiều user ghi → user đọc delay 3–10×, Tồn kho p95 10.9s max 14s; EXPLAIN: 3 query/lượt mở trang đều Seq Scan ~36k dòng): `idx_ie_wh_importdate (warehouse_id, import_date desc nulls last, id)` list Tồn chính (21ms/2487buf→1.7ms/11buf Index Only Scan) + `idx_ie_wh_mat_rem (warehouse_id, material_id) include (cartons_remaining)` SUM/COUNT + `idx_gdo_wh_date (warehouse_id, delivery_date desc)` list Xuất (GDO trước đó KHÔNG có index warehouse_id nào) + `idx_pi_wh_importdate` list Nhập. Đo lại cùng tham số: p95 Tồn 10.9s→2.1s, Xuất 8.5s→1.9s, Nhập 4.9s→0.9s, Dashboard 4.5s→1.2s. Migration `20260723_read_perf_indexes.sql` (ĐÃ apply STAGING qua pg 23/07; production khi merge — index additive, bảng càng lớn lợi càng lớn). |
| 2026-07-24 | — | **Bảng `StocktakeLog` (nhật ký kiểm kê append-only)** — user 24/07 hỏi "lưu được dữ liệu quá khứ để xem lại không?": trước đó kiểm kê chỉ lưu 4 cột trên `InventoryEntry` (stocktake_at/by/flagged/flag_note) → **kiểm lại là ĐÈ mất**, pallet xuất đi cũng mất. Thêm bảng ghi 1 dòng MỖI LẦN kiểm (id/entry_id/pallet_code/location snapshot/material snapshot/units/app_qty/physical_qty/diff/is_flagged/note/counted_by(_name)/counted_at + created/updated_at, mọi id TEXT). `stocktakeEntry` append log sau khi lưu (non-fatal). Endpoint `GET /inventory/stocktake-log` (scope kho+loại null-inclusive, cap 2000, lọc ngày/kho/loại/vị trí/search) + tab FE **"Lịch sử kiểm"** (`/wms/stocktake/history`, gate `stocktake.view`). Realtime `StocktakeLog→['stocktake-log']`. Migration `20260724_stocktake_log.sql` (ĐÃ apply STAGING qua pg 24/07 + đã trong publication realtime; production khi merge — bảng mới additive). |
| 2026-07-25 | — | **Chuẩn upload đa-user: dọn di sản trùng + 2 UNIQUE INDEX chống đua** (user chốt 25/07 "nhiều người cùng upload KH các kho"): upload KH nhập upsert theo key (Ngày+Kho+NCC+Mã hàng) nhưng JS check không đỡ được 2 người ghi cùng mili-giây → DB là chốt chặn cuối. Migration `20260725_upload_concurrency.sql`: (1) backup `x_bak_plan_dup_20260725` (90 dòng) + `x_bak_order_dup_20260725` (3 lệnh) → GỘP dòng KH trùng key (giữ dòng cũ nhất, cộng SL, nối PO) + dời dòng về lệnh cũ nhất rồi CANCEL lệnh INBOUND trùng nhóm → recalc cache lệnh ảnh hưởng (mirror qtyEntryDecimal) → gác `RAISE EXCEPTION` nếu còn trùng; (2) `uq_inbound_plan_line_active_key` ON inbound_plan_lines(date, warehouse_id, ncc_id, material_id) NULLS NOT DISTINCT WHERE status<>'CANCELLED' AND material_id IS NOT NULL + `uq_tms_order_inbound_group` ON TmsOrder(date, warehouse_id, warehouse_type, vehicle_type, ncc_id) NULLS NOT DISTINCT WHERE direction='INBOUND' AND status<>'CANCELLED'. BE bắt 23505 → jitter + re-fetch → chuyển insert thành update / dùng chung lệnh người thắng (không 500). Test đồng thời 8/8: 12 upload song song 3 kho 2.8s · 8 request y hệt cùng lúc → 1 insert + 7 update, 0 trùng · 5 request đua ghi đè → last-write-wins · cache khớp 265/265 · dọn 0 sót. (ĐÃ apply STAGING qua pg 25/07; production khi merge — migration idempotent tự dọn trùng production trước khi tạo index.) |
| 2026-07-27 | — | **DỌN DỮ LIỆU DEMO (staging) — KHÔNG đổi schema, chỉ dữ liệu**: `20260727_reset_demo_data.sql` sao lưu 21 bảng sang schema `bak_20260727` rồi TRUNCATE **Material + Tồn (InventoryEntry/AdjustmentLog/StocktakeLog) + Nhập (ProductionImport/gate_registrations) + Xuất (GDO/OutboundDelivery/Item/ScanEntry) + Kế hoạch (TmsOrder/TmsVehicleSlot/inbound_plan_lines + raw SAP erp_outbound_orders/khvc_lines/reconcile_tasks) + phụ trợ bám pallet (PalletLabelPrint/PalletOperation/SlottingPlan+Line)**; GIỮ Kho/Vị trí/Khu vực/NCC-ĐVVT/Xe/Loại xe/Khung giờ/Nhân sự/Phân quyền/QA-Ca/LookupValue/SystemSetting/ApiKey/WeighTicket/HR. **Bắt buộc kèm `UPDATE "DeliverySlot" SET booked_count=0`** — bộ đếm cache này không reset thì khung giờ hiện "đã đầy" → KHÔNG đặt lịch mới được. Đã kiểm: không bảng NGOÀI danh sách nào trỏ vào nhóm bị xoá (TRUNCATE không cần CASCADE); 5 FK tới Material đều nằm trong danh sách nên Material xoá sạch được. Verify: 20/20 bảng về 0, 20/20 bảng giữ nguyên số, booked_count 3→0, smoke 24/24 endpoint 200 (0 lỗi 500) với DB rỗng. **CHỈ CHẠY STAGING — KHÔNG chạy trên production.** File có sẵn 2 khối comment: trả lại 3 cờ Mã hàng từ backup, và `DROP SCHEMA bak_20260727 CASCADE` khi chắc chắn. |
| 2026-07-27 | — | **GỘP Loại kho "Thùng" → "PK01" (dữ liệu, KHÔNG đổi schema)**: user đổi taxonomy sang mã SAP (Thành phẩm→FG01, SCA→FG02, Raw→RM01, Giấy→PK01) rồi muốn xoá "Thùng" nhưng bị chặn. RPC `rename_warehouse_type` KHÔNG gộp được (từ chối khi tên đích đã tồn tại — chỉ hỗ trợ đổi tên). Migration `20260727_merge_warehouse_type_thung_pk01.sql`: sao lưu `bak_20260727c` → gác `RAISE EXCEPTION` (chỉ chạy khi 174 khung giờ + 90 slot "Thùng" **trùng KHÍT** PK01 theo kho/thứ/giờ/loại xe/hướng **và 0 lượt đặt**) → 2 khu K2THUNG/K4THUNG sang PK01 · 39 nhân sự gỡ `Thùng` khỏi `allowed_categories` (cả 39 đã có PK01; nối mảng phải dùng `ARRAY['PK01']::text[]` — viết `\|\| 'PK01'` bị **malformed array literal** vì Postgres chọn toán tử array\|\|array) · **XOÁ** 174 `SlotTemplate` + 90 `DeliverySlot` (bản sao thừa của PK01, đổi sang PK01 sẽ tạo 264 dòng trùng trong lưới đặt lịch) · xoá `LookupValue`. Verify 11/11 XANH; danh mục còn FG01/FG02/POSM/RM01/PK01. **CHỈ STAGING** — production chưa đổi taxonomy nên KHÔNG chạy. |
| 2026-07-27 | — | **KHU VỰC KHO NHIỀU LOẠI (`20260727_zone_location_multi_categories.sql`, ĐÃ apply staging qua pg)**: user chốt "khu chứa cả RM01 + PK01 → cho chọn MULTI, KHÔNG cho để trống". Đổi 3 cột 1-giá-trị → **mảng text[]**: `WarehouseZone.category`→`categories` (**NOT NULL + CHECK cardinality≥1**, gác RAISE nếu còn khu chưa gắn loại — production phải gán loại trước khi apply), `Location.category`→`categories` (nullable — kế thừa từ khu, null di sản = dùng chung), `StocktakeLog.category`→`categories` (snapshot lúc đếm). Chốt so khớp đổi từ "bằng nhau" → "loại phiếu **∈ mảng**" (3 chốt LOCATION_CATEGORY_MISMATCH inbound; slotting zoneAccepts/WRONG_CATEGORY/frozen; filter PostgREST `eq`→`cs`, scope `in`→`ov` — helper `categoriesAllAllowed`/`categoriesOrScopeFilter` trong categoryScope.ts, guard WRITE = MỌI loại trong scope, cắt LIST = giao ≥1). Sửa kèm 2 RPC đọc cột cũ: `rename_warehouse_type` (3 cột sang `array_replace`) + `slotting_stats` (zones trả key `categories`). **Loại của vị trí hết sửa lẻ**: form vị trí bỏ ô Loại kho (kế thừa từ khu, hint rõ), `updateZone` đổi loại → **CASCADE xuống mọi vị trí của khu** (vá luôn lỗ hổng cũ: sửa loại khu để vị trí stale). deleteLookup: 3 checks chuyển `one()`→`arr()`. ⚠️ Thứ tự deploy: apply migration xong deploy code mới ngay (code cũ select cột `category` sẽ 400); production apply ngay trước merge main. |
| 2026-07-27 | — | **Lọc Tồn kho theo kho = cột `warehouse_id` trực tiếp (`20260727_entry_warehouse_id_direct.sql`, ĐÃ apply staging)**: bug filter Kho Bàu Bàng — filter kho cũ liệt kê MỌI `location_id` của kho nhét vào `.or()` → kho 1.517 vị trí = chuỗi ~56KB → PostgREST nghiền hết 60s → **Vercel 504** (kho ≤~350 vị trí như Ba Vì thì thoát; tái hiện: Ba Vì 200/1,2s · Bàu Bàng 504/60s). Fix: (1) **backfill** `InventoryEntry.warehouse_id` = kho của Location (production di sản ~99,8% NULL với pallet QR — cột giờ là nguồn lọc); (2) RPC `move_pallets_to_location` **sync warehouse_id** khi dồn/chuyển vị trí; (3) controller: `applyInventoryFilters` lọc thẳng `eq/in('warehouse_id')` (ăn index `idx_ie_wh_importdate`), chỉ resolve id vị trí khi user chọn vị trí cụ thể; kiểm kê đổi vị trí + `bulkTransferLocation` fallback sync cột; (4) gác RAISE 0 dòng lệch sau backfill. Đo sau fix: Bàu Bàng 8.074 dòng ~1s. **Production apply khi merge** (backfill bắt buộc trước khi code mới chạy — code mới lọc theo cột). |
| 2026-07-27 | — | **RPC `rename_warehouse_type` SÓT 3 CỘT — vá** (`20260727_rename_warehouse_type_full_cover.sql`, ĐÃ apply staging): đổi tên 1 Loại kho cascade 11 bảng nhưng bỏ qua `Warehouse.carton_scan_categories` (text[]), `PalletLabelPrint.category`, `StocktakeLog.category` → sau khi đổi tên, 3 chỗ này còn trỏ tên CŨ = **mồ côi âm thầm**. Đợt đổi sang mã SAP 27/07 không dính vì 3 cột đang rỗng; production có dữ liệu thì hỏng. Thêm 3 `UPDATE` → cascade **14 bảng**; verify round-trip FG02→ZZTMP→FG02 trả đủ 14 khoá + khôi phục nguyên vẹn. **Kèm code** (`lookupController.deleteLookup`): hàng rào xoá soi đủ **14 chỗ** thay vì 3 và **liệt kê từng chỗ bao nhiêu bản ghi**. Bằng chứng mức độ hở: xoá POSM trước đó báo *"1 bản ghi"*, thực tế **488** (1 khu + 37 nhân sự + 270 khung giờ + 180 slot) → xoá xong là 487 bản ghi mồ côi, không cảnh báo gì. |
| 2026-07-27 | — | **Facet Tồn kho: DISTINCT dưới DB (`20260727_inventory_facet_values_rpc.sql`, ĐÃ apply staging)** — `listFacets` trước đây KÉO TOÀN BỘ dòng tồn IN_STOCK/PARTIAL về Node chỉ để gom tập giá trị Chu kỳ/Máy/NCC (12.637 dòng ≈ 13 round-trip hôm nay; **vài triệu dòng/năm = hàng NGHÌN round-trip mỗi lần mở trang Tồn kho**). RPC `inventory_facet_values(p_warehouse_ids uuid[], p_categories text[])` trả `(kind, val)` DISTINCT dưới DB (lọc kho bằng cột `warehouse_id` TRỰC TIẾP — bài học bug 504 Bàu Bàng) + index `idx_ie_facet_wh_status (warehouse_id, status) INCLUDE (cycle, machine_code, ncc_id, material_id)`. **Đồng thời BỎ `materials` + `locations` khỏi response facet** (2.740 mã + 1.753 vị trí = phần lớn của 420KB/lần mở trang) — 3 filter dùng chúng (Tồn kho, In tem ×2) chuyển sang tìm-trên-server 50 dòng. Verify sống: facet **420KB → 3,4KB**, NCC vẫn đủ 52 (khớp oracle SQL), facet 2 kho = hợp của 2. Production apply khi merge (RPC + index additive, an toàn). |
| 2026-07-27 | — | **Tìm kiếm BỎ DẤU cho ô chọn Mã hàng / Vị trí (`20260727_search_norm_unaccent.sql`, ĐÃ apply staging)** — hệ quả trực tiếp của đợt bỏ nạp-cả-danh-mục: mọi ô chọn nay BẮT BUỘC gõ, mà `ilike` phân biệt dấu → gõ "nha dam" ra **0 kết quả** (phải "Nha Đam") ⇒ nhân viên kho tưởng MẤT mã hàng. Thêm `CREATE EXTENSION unaccent + pg_trgm`, hàm `immutable_unaccent()` (unaccent() là STABLE nên KHÔNG dùng thẳng trong cột GENERATED — bọc bằng cách chỉ đích danh `'public.unaccent'::regdictionary`), cột **GENERATED `search_norm`** = `lower(immutable_unaccent(...))` trên **Material** (mã + mô tả + tên ngắn + mã cũ) và **Location** (mã vị trí + mã khu + tên khu) + **index GIN trgm**. BE lọc `.ilike('search_norm', %term%)` với từ khoá chuẩn hoá bằng ĐÚNG công thức đó (`normalizeSearchTerm` trong utils/search.ts — NFD strip + đ/Đ→d/D + lower; Postgres `unaccent` map Đ→D nên khớp). Verify sống 7/7: "nha dam" = "Nha Đam" = "NHA DAM" = 20 mã, tìm theo mã vẫn chạy, từ khoá lạ → rỗng không lỗi. ⚠️ `search_norm` xuất hiện trong `select('*')` nên payload trang Danh mục Mã hàng tăng ~10% — sẽ hết khi trang đó phân trang server. Production apply khi merge (additive; Material ~10k dòng ≈ 1s). |
| 2026-07-28 | — | **PHÂN TRANG SERVER cho list Nhập kho (`20260727_inbound_orders_paged_rpc.sql`, ĐÃ apply staging)** — user xem **CẢ THÁNG trở lên** (~500 phiếu/ngày ⇒ 15.000+ dòng ≈ 22MB) nên trần cứng 10.000 dòng không cứu được: bức tường thật là payload + số ô DOM. 3 hàm cùng bộ lọc: `inbound_orders_page` (trang id + tổng, thứ tự ngày desc → nhóm-theo-chuyến → ca → giờ tạo), `inbound_orders_summary` (tổng SummaryBand + bảng "Vị trí hàng nhập" trên TOÀN BỘ kết quả lọc — cộng trên trang hiện tại là SỐ SAI), `inbound_orders_facets` (option Mã hàng/Chu kỳ/Máy DISTINCT dưới DB). Mọi filter trước đây lọc CLIENT (mã/chu kỳ/máy/ca/nguồn/người nhập) nay xuống SQL; TRANSFER vẫn miễn chu kỳ/máy/ca như logic cũ; quy đổi thùng mirror `qtyEntryDecimal` per-mã trước khi cộng; nhánh pallet POSM dùng chung mirror `computeOrderStats`. ⚠️ **BẮT BUỘC `LANGUAGE plpgsql` + `SET plan_cache_mode = force_custom_plan`**: bản `LANGUAGE sql` bị GENERIC PLAN (không biết giá trị tham số → ước lượng sai trên CTE → nested loop 15k×30k) chạy **>300s** trong khi thân query chỉ 1,6s. Đo sau khi sửa: page 118–162ms · summary 380–449ms · facets 123ms trên 15.000 phiếu / 30.000 pallet. |
| 2026-07-28 | — | **PHÂN TRANG SERVER cho list Xuất kho (`20260728_outbound_gdos_paged_rpc.sql`, ĐÃ apply staging)** — đợt 2 sau Nhập kho. 3 hàm `outbound_gdos_page` / `_summary` / `_facets` cùng bộ lọc; mirror nguyên văn FE cũ: nhãn trạng thái suy diễn (`gdo_status_label`), `export_type` = item đầu tiên theo id, thứ tự ngày giao DESC → nhóm cùng xe → export_type DESC → số cuối mã chuyến, quy đổi thùng per-mã, pallet có ngoại lệ Thùng/Pallet theo kho. **4 bài học hiệu năng đo trên 50.000 chuyến / 200.000 dòng hàng** (ghi rõ trong file để không lặp lại): (1) tách mệnh đề lọc ra hàm `_ids()` dùng chung thì gọn code nhưng plpgsql materialize CẢ tập id và planner mất khả năng dừng sớm → phải INLINE vào từng hàm; (2) tìm kiếm bằng `string_agg` mã/tên hàng rồi so chuỗi = quét toàn bảng → Node resolve từ khoá thành tập id trước; (3) hàm `eff_cartons_per_pallet` gọi per-row không inline được (join+quy đổi 250ms, thêm hàm 1 lần/dòng thành 1.600ms) → bung `warehouse_pallet_overrides` MỘT LẦN thành CTE rồi LEFT JOIN; (4) tách tổng và phân bổ NPP thành 2 truy vấn khiến CTE bị tham chiếu 2 lần → vật chất hoá 200k dòng → gộp bằng `GROUPING SETS` 1 lượt quét, và `count(*) OVER ()` thay `SELECT count(*)` riêng. Kết quả: tổng 90 ngày 6–8s → 0,9s; 1 tháng (17k chuyến) 0,37s; 1 kho 64ms. **Trần an toàn `MAX_ITEMS_FOR_TOTALS = 150.000 dòng hàng`**: role `authenticator` của PostgREST có `statement_timeout = 8s` CỐ ĐỊNH, nên phạm vi khổng lồ (nhiều kho × nhiều tháng) trả `too_wide` — vẫn xem/lật trang bình thường, chỉ không tính tổng, FE hiện "—" kèm hướng dẫn thu hẹp. Kèm `isQueryTimeout`/`QUERY_TIMEOUT_MSG` (utils/pagination) đổi lỗi huỷ-truy-vấn thành 400 có hướng dẫn thay vì 500 trắng. |
| 2026-07-28 | — | **PHÂN TRANG SERVER cho lưới Kế hoạch vận chuyển (`20260728_tms_orders_paged_rpc.sql`, ĐÃ apply staging)** — đợt 3. Khác 2 module trước ở chỗ lưới **merge ô theo cụm (rowspan)**: 1 cụm = đơn CHỦ + mọi đơn GOM CHUNG XE, cắt ngang cụm là vỡ layout ⇒ **đơn vị phân trang = CỤM, không phải dòng**, và `page_from`/`page_to` do SQL trả (FE không tự tính `page × pageSize`). 3 hàm `tms_orders_page` / `_summary` / `_facets` cùng bộ lọc; 5 filter trước đây chạy CLIENT (Hướng/ĐVVT/Loại kho/Loại xe/Khung giờ) nay xuống SQL — kể cả luật **"Loại xe kéo theo cả bạn gom chung xe"** và "Khung giờ = Chưa đặt". **STT xe** phải đánh trên tập NỀN (chưa lọc) rồi mới cắt trang, nếu không số thứ tự sẽ nhảy theo bộ lọc/trang. Facet trả thêm `npp_names` (gợi ý NPP cho form Thêm đơn — trước suy từ toàn bộ đơn đã tải). Band "Tổng hợp mã hàng" đổi sang gửi **CỜ bộ lọc** (`by_filter`) để BE tự resolve đơn NHẬP thay vì nhồi hàng nghìn id; dialog Đặt lịch lấy danh sách đơn gom chung xe qua route mới `GET /tms/orders/consolidatable`. **2 lỗi bắt được khi đối chiếu oracle**: (a) đơn thứ cấp đã bị kéo vào cụm vẫn được đếm thêm 1 "xe" và cấp 1 STT riêng → tổng Xe thừa tới 400/3.500 và STT lệch — vá bằng CTE `sec_base`/`sec_f`; (b) `csv()` chỉ nhận chuỗi nên bộ lọc gửi dạng MẢNG trong body làm material-summary văng 500. Đo trên 20.000 đơn / 22.222 xe: page 145–236ms (kể cả MỌI kho × 90 ngày), summary 101–157ms, facets 92ms. |
| 2026-07-28 | — | **Đăng ký cổng: CÂY LƯỜI (`20260728_gate_tree_lazy_rpc.sql`, ĐÃ apply staging)** — trang này KHÔNG phải list phẳng mà là **cây gập/mở 3 cấp Kho → Loại kho → Loại xe**, nên không áp khuôn phân trang của Nhập/Xuất/Kế hoạch VC được (gập nhóm + phân trang đá nhau: nhóm gập vẫn chiếm chỗ trang → trang trông trống). User chốt hướng "cây lười": `gate_tree` trả **thống kê từng nhóm + tổng SummaryBand** (nhẹ, vài chục dòng nhóm), `gate_leaves_page` trả **dòng chi tiết theo đúng thứ tự cây, cuộn tới đâu lấy tới đó** (200 dòng/lượt, IntersectionObserver). Trước đây tải HẾT lượt đăng ký rồi mới dựng cây ở máy — kéo rộng khoảng ngày là chết ở trần 10.000 dòng. **Điểm thiết kế đáng nhớ: THỨ TỰ CÂY DO FE QUYẾT** (kho theo TÊN · loại kho theo Cài đặt WMS · loại xe theo Cài đặt TMS, "Chỉ trả pallet"/"Khác" xuống cuối) → FE gửi xuống **3 mảng thứ tự đã sắp sẵn** + nhãn nhóm rỗng (`p_wt_null`/`p_vt_null`), SQL chỉ `array_position`; chép quy tắc sắp xếp vào SQL là lệch nhau ngay khi user đổi cài đặt. Nhóm đang GẬP gửi xuống server để bỏ qua dòng của nhóm đó (không thì tải toàn dòng đang bị ẩn). 2 lỗi bắt được khi verify: `gateCsv` chỉ nhận chuỗi nên lọc Loại xe gửi dạng mảng → 500; `company_id` không phải uuid → Postgres 22P02 → 500 (nay lọc bằng `uuidList` → trả rỗng). Đo trên 30.000 lượt: tree 61–162ms, leaves 80–170ms (kể cả offset 20.000). Verify 47/47 XANH (thống kê nhóm từng con số vs đường tính cũ, dòng đủ/không trùng/đúng thứ tự cây ở 5 bộ lọc, 2 cỡ lô cho cùng dãy, gập ở cả 3 cấp, 6 ca biên, 30 request đồng thời). |
| 2026-07-28 | — | **PHÂN TRANG SERVER cho 2 trang DANH MỤC GỐC (`20260728_materials_paged_rpc.sql` + `20260728_locations_paged_rpc.sql`, ĐÃ apply staging)** — chốt lại đợt "bỏ nạp-cả-danh-mục" 27/07: hồi đó mới cắt các Ô CHỌN sang tìm-trên-server, còn chính 2 trang danh mục vẫn kéo hết về máy. **Mã hàng**: 2.789KB → **200KB/trang** (14×). 2 luật chỉ đúng khi soi TOÀN bảng nên nằm trong RPC: *Trùng Tên hàng* (`material_description` trim+lower xuất hiện ≥2 lần trong phạm vi quyền — server gắn cờ `is_dup_name` per dòng vì FE không thể suy từ 200 dòng đang xem) và *Thiếu thông tin* (phụ thuộc cờ theo Loại kho HSD/Pallet-EA → BE đọc `LookupValue.meta` rồi truyền `p_cat_rules` + 2 danh sách legacy xuống, **KHÔNG hardcode tên loại trong SQL** — luật warehouse-type-custom-flags). Tìm kiếm mirror `omniMatch` (AND từng token) trên `search_norm` + loại/ĐVT. **Vị trí kho**: 1 kho tới 1.517 vị trí (Bàu Bàng); `used_slots` giữ đúng định nghĩa cũ (pallet lớp 1, IN_STOCK/PARTIAL, còn hàng) — trang chỉ đếm cho 200 dòng đang xem, còn 4 ô tổng (Vị trí · Pallet đang dùng · Sức chứa · Đầy) tính bằng SQL trên toàn bộ lọc, index mới `idx_ie_loc_used`. **2 chỗ suýt sai âm thầm đã vá**: (a) *Xuất Excel* vốn xuất `filtered` → sau phân trang chỉ còn 200 dòng mà không báo gì → nay tải hết kết quả lọc từ server rồi mới xuất (nút có trạng thái đang tải); (b) *gắn cờ kiểm kê hàng loạt* vốn gửi mảng id của toàn bộ danh sách → nay gửi **CỜ bộ lọc** (`by_filter`) để BE tự resolve. Verify oracle trên DỮ LIỆU THẬT staging: Mã hàng **122/122**, Vị trí **94/94** (tổng từng con số, duyệt hết trang ở 2 cỡ, thứ tự, cờ trùng tên, `used_slots` từng dòng, biên/injection, 24 request đồng thời). Mode CŨ trả mảng vẫn giữ cho picker/typeahead/`codes=`. |

## 2026-07-28 — Phân trang server đợt 2 (Kiểm kê · In tem · Nhặt lẻ)
| Migration | Nội dung | Staging | Production |
|---|---|---|---|
| `20260728_stocktake_paged_rpc.sql` | `stocktake_entries_page` + `stocktake_log_page` — bỏ CAP 2000 của 2 tab Kiểm kê (kho 8.074 pallet chỉ xem được 2.000) | ✅ apply | ✅ apply 15/08 |
| `20260728_pallet_prints_paged_rpc.sql` | `pallet_prints_page` + `pallet_prints_facets` + index `idx_pallet_print_created_batch` — Lịch sử in tem phân trang theo PHIẾU IN (trước: mảng ≤20.000 dòng, cắt âm thầm) | ✅ apply | ✅ apply 15/08 |
| `20260728_loose_picking_paged_rpc.sql` | `qty_entry_decimal` + `loose_picking_page` + `loose_picking_facets` + 2 index — Nhặt lẻ đảo chiều truy vấn (lọc item nhặt lẻ TRƯỚC rồi join lên chuyến) + phân trang theo CHUYẾN | ✅ apply | ✅ apply 15/08 |
| `20260728_hr_paged_rpc.sql` | `hr_employees_page` + `hr_attendance_matrix` + 3 index — phân trang theo NGƯỜI cho Quản lý người dùng & Bảng công ma trận (đo thật: bảng công 3.000 NV/tháng từ 44.665KB/18,9s → 461KB/0,75s) | ✅ apply | ✅ apply 15/08 |
| `20260728_pallet_ops_paged_rpc.sql` | `pallet_op_material_code` + `pallet_ops_page` + 3 index — Lịch sử Dồn/Tách phân trang (trước: `hardCap = 5.000`, mảng trần KHÔNG total/KHÔNG cờ ⇒ 25.000 thao tác chỉ thấy 5.000, cắt ÂM THẦM); lọc Loại kho đưa xuống SQL (bóc mã hàng từ tem V1 `_` và V2 `;`) | ✅ apply | ✅ apply 15/08 |
| `20260728b_pallet_prints_rpc_perf.sql` | Sửa HIỆU NĂNG 2 RPC in tem (đo 250.000 tem): `pallet_prints_page` 6.674ms→2.823ms (thôi vật hoá CTE ghi ~18MB temp, gom thẳng theo phiếu in), `pallet_prints_facets` 9.790ms→668ms (GROUPING SETS thay 5 lần `agg(DISTINCT)` tràn đĩa) | ✅ apply | ✅ apply 15/08 |
| `20260728c_inventory_summary_paged_rpc.sql` | `inventory_summary_page` — Tồn kho view TỔNG HỢP gom + phân trang trong SQL (đo 52.635 pallet → 41.107 nhóm: **18.147KB/12,8s → 87KB/0,5–3,2s**, duyệt hết trang 251s→25,8s). %Date vẫn tính ở `utils/shelfLife.computePctDate` cho đúng 1 trang — KHÔNG viết lại công thức trong SQL | ✅ apply | ✅ apply 15/08 |
| `20260728d_zone_used_pallets_rpc.sql` | `zone_used_pallets` + index `idx_inventory_active_loc_mat` — Dashboard gom "pallet đã dùng" theo KHU trong SQL (trước: aggregate PostgREST bị TẮT nên luôn rơi vào fallback kéo cả bảng tồn về Node ⇒ **8,3s → 0,4s**, 15 khu khớp tuyệt đối số cũ) | ✅ apply | ✅ apply 15/08 |
| `20260728e_leaves_paged_rpc.sql` | `hr_leaves_page` + 2 index — Nghỉ phép phân trang server + 4 ô SummaryBand + lọc CHỨC DANH xuống SQL (mặc định lọc CẢ NĂM = 6.001 đơn ≈ 3,8MB, sát trần 4,5MB ⇒ **3.812KB → 63KB / 0,6s**) | ✅ apply | ✅ apply 15/08 |
| `20260728f_tms_order_fk_indexes.sql` | 2 index cho cột KHOÁ NGOẠI `TmsOrder.transfer_gdo_id` + `destination_warehouse_id` (Postgres không tự tạo) — thiếu index thì mỗi lần XOÁ 1 chuyến phải quét TOÀN BỘ bảng lệnh: xoá 12.000 chuyến bị **statement timeout**; đường nghiệp vụ thật là "Bỏ hoàn thành chuyến chuyển kho" (cascade xoá lệnh) | ✅ apply | ✅ apply 15/08 |
| `20260728g_inventory_band_totals_rpc.sql` | `inventory_band_totals` — 2 ô SummaryBand Tồn kho ("Thùng tồn" + "Pallet") gom trong MỘT lời gọi. Bản cũ là một CHUỖI round-trip (nạp nhóm SUM theo mã ~2.700 dòng → chunk 300 tra `Material` lấy hệ số → quy đổi ở Node → thêm 1 câu đếm). Đo bằng gói QA `06-readload`: trang Tồn kho **19.774ms → 4.542ms** ở 24 luồng ghi, Dashboard **47.875ms → 2.229ms**; số khớp SQL tuyệt đối (có lọc và không lọc) | ✅ apply | ✅ apply 15/08 |
| `20260728h_pallet_prints_page_returns_rows.sql` | `pallet_prints_page` trả LUÔN CÁC DÒNG (`ids` → `rows`, đã sort trong SQL). Trước đây RPC trả id rồi backend nạp lại theo chunk 300 ⇒ 1 trang 100 phiếu (~3.000 tem) = **11 request PostgREST**. Đo thật: 1 trang 6.818ms → 3.795ms (warm 890ms), request **11 → 1**; verify 12/12 vs SQL (tổng phiếu/tem/2 ô, 12 trang không trùng, thứ tự, trang vượt phạm vi, có lọc). Nút thắt là pool ~10 khe NỘI BỘ của PostgREST, không phải máy DB — xem đầu file migration | ✅ apply | ✅ apply 15/08 |
| `20260728i_zone_capacity_one_call.sql` | `zone_capacity_rows` — Dashboard dải "Sức chứa theo khu" gom 3 request (khu phân trang + bảng Kho + `zone_used_pallets`) về **1**. Thân tính pallet-đã-dùng là NGUYÊN VĂN `zone_used_pallets` (cố ý copy: lần đầu viết lại "cho gọn" đã lệch 4 điểm → used sai gấp hàng nghìn lần). Verify 15/15 khu khớp tuyệt đối used/capacity/category/tên/zone_id/**thứ tự**/bộ khoá payload. `zone_used_pallets` giữ lại (không còn dùng, là lưới an toàn cho cửa sổ triển khai) | ✅ apply | ✅ apply 15/08 |
| `20260729_rpc_return_rows_batch.sql` | CẮT ROUND-TRIP đợt 2 — 6 đường chuyển sang "RPC trả DÒNG" + Dashboard gom 1 lời gọi: `hr_attendance_matrix` (+employees/rows, Bảng công 4 request→1) · `hr_leaves_page` (+rows+employee embed, Nghỉ phép 4→1) · `loose_picking_page` (+items đầy đủ material/gdo/loose_scanned, Nhặt lẻ 5→1) · `locations_page` (+p_with_rows → rows+used_slots+_count, Vị trí kho 3→1; ĐỔI CHỮ KÝ — drop cả 2 chữ ký, caller cũ dùng default) · `tms_vehicles_page` MỚI (TMS Xe 5→1; ⚠️ Vehicle.ncc_id/vehicle_type_id là uuid → cast ::text phía cột) · `weigh_tickets_page` MỚI (Phiếu cân 5→1, mirror applyFilters kể cả null-inclusive scope) · `dashboard_all` MỚI (stats+zones 2→1, WITH ORDINALITY giữ thứ tự khu). Verify 31/31 payload Y HỆT đường cũ (deep-compare từng khoá) + 18/18 kích SỐNG nhánh lỗi + tải 24 luồng 0×500 | ✅ apply | ✅ apply 15/08 |
| `20260729b_error_logs.sql` | TAI MẮT PRODUCTION — bảng `error_logs` (id/created_at CÓ DEFAULT — bảng mới; RLS bật KHÔNG policy = chỉ service role): BE ghi mọi 5xx qua `recordServerError` (utils/response — cả 2 kiểu fail + maskServerMessage, fire-and-forget, dọn lười 30 ngày ~1% lượt ghi); FE ghi window.onerror/unhandledrejection qua POST `/api/telemetry/client-error` (public, dedupe + 5 lỗi/phiên + bỏ nhiễu ResizeObserver/Script error/mất mạng, cap 3/phút/instance). Đọc: GET `/api/telemetry/digest` (public, CHỈ SỐ ĐẾM 24h) — workflow keepalive gọi hằng ngày, BE>0 = job đỏ → email. Verify sống local: FE ghi + BE 500 ghi + digest đếm đúng + anon bị RLS 401 + dọn sạch dòng test. Kèm: login chặn 400 payload email/password không phải chuỗi (digest bắt được 500 này ngay lần thử đầu) | ✅ apply | ✅ apply 15/08 |
| `20260729c_control_tower_inbound_fix.sql` | GIÁM SÁT VẬN HÀNH — phần NHẬP (user soi số sau khi upload): (1) `inb_pallets` (ô "Pallet nhập") + (2) `hourly_in` (cột xanh biểu đồ giờ) THIẾU lọc Loại kho trong khi khối "Hàng nhập theo mã" CÓ lọc → chọn PK01: ô vẫn 2.374 pallet còn danh sách chỉ 252 (lệch 9,4×); (3) `in_mat_rows` trả thêm `unit` (entry_unit nếu mã có quy cách, ngược lại base_unit) để FE in ĐÚNG ĐVT từng dòng — cột cũ ghi "Thùng" nhưng mã EA/KG trả base thô (1 dòng hiện 2.816.800 "thùng" cho 22 pallet = 2,8 triệu CÁI). Phần còn lại giữ nguyên bản 20260722. Phép kiểm chống tái phát: gói QA 07 mục 4 (ô tổng ⇄ Σ danh sách khi lọc) + luật ratchet `thung_unit_on_aggregate_pages` (baseline 0) | ✅ apply | ✅ apply 15/08 |
| `20260729d_tms_orders_page_sort.sql` | THỨ TỰ LƯỚI KẾ HOẠCH VC — chỉ thay `tms_orders_page` (chữ ký giữ nguyên). Cũ: cụm xếp `date DESC, created_at` ⇒ trong 1 ngày là THỨ TỰ UPLOAD, trộn Xuất/Nhập + loại kho + loại xe + ĐVVT nên đặt giờ phải nhảy khắp bảng. Mới: `ngày ↓ (gần nhất trên, user chốt 29/07) · ưu tiên (bool_or cả cụm) · Xuất→Nhập · loại kho · loại xe · ĐVVT (join TransportCompany) · mã đơn · created_at,id`; STT xe đánh CÙNG bộ khóa (trên tập nền) để STT tăng theo chiều đọc + màu vằn `groupParity` không vón cục. CỐ Ý không xếp theo GIỜ ĐÃ ĐẶT (đặt xong 1 xe là dòng nhảy chỗ + phân trang kéo dòng trang sau lên, đúng lúc đang đặt hàng loạt) — xem lịch theo giờ dùng "Xem booking" / filter Khung giờ. Verify staging: thứ tự khớp khóa khai báo (collation VN: Đ ngay sau D) · STT 1→53 tăng dần · lật 3 trang 53/53 đơn không trùng/mất · UT lên đầu + Nhập xuống cuối (gắn tạm 4 đơn rồi hoàn nguyên 100%) · RPC 63ms | ✅ apply | ✅ apply 15/08 |
| `20260730_dashboard_stock_by_unit.sql` | DASHBOARD PALLET CHỦ ĐẠO (user chốt 30/07) — chỉ THÊM khóa `by_unit` vào `dashboard_stats` (inventory/today giữ nguyên; `dashboard_all` bọc nên tự có): tồn tách theo ĐVT hiển thị của mã (entry_unit nếu có quy cách, ngược lại base_unit), qty quy đổi per-row cùng công thức CTE `inv` ⇒ bất biến Σby_unit = Σinventory cả qty lẫn pallet (QA gói 07 kiểm mỗi lần chạy). FE: tile đầu = Pallet tồn; tile quy đổi → card "Tồn theo đơn vị" (thùng 1,39tr · cái 131,2tr · kg 785k · SET/M2/BAG); RPC cũ chưa apply → fallback ô quy đổi cũ. ⚠️ PHỤ THUỘC bộ base-unit (như 20260722). Verify staging: Σ khớp 133.433.137/15.010 + per-kho Ba Vì 705.721/5.736 · dashboard_all 189ms | ✅ apply | ✅ apply 15/08 |
| `20260730b_gdo_multi_category_scope.sql` | CHUYẾN CHỞ LẪN nhiều Loại kho phải LỌT bộ lọc phân quyền (bug user báo 30/07). Gốc: upload KH xuất gom loại của các dòng thành CHUỖI GHÉP `'FG01+PM01'` (`loaiKhoSet.join('+')`), mà mọi bộ lọc so khớp NGUYÊN CHUỖI `= ANY(p_categories)` ⇒ chuyến biến mất với MỌI user có scope loại — kể cả người có ĐỦ CẢ HAI (đo staging: 67/122 chuyến toàn hệ, Kho Ba Vì 32/58). Luật user chốt: **giao ≥1 loại là THẤY**. Thêm helper `wt_cats(text)→text[]` (tách `+`, NULL khi rỗng — mirror `splitCategories` phía TS) rồi đổi `= ANY` → `&&` ở **8 RPC** đọc `GroupDeliveryOrder.warehouse_type`: `outbound_gdos_page/_summary/_facets`, `loose_picking_page/_facets`, `dashboard_stats`, `control_tower_stats`, `rename_warehouse_type` (đổi tên loại nay thay ĐÚNG phần tử trong chuỗi ghép — trước đó bỏ sót 67 dòng). Facet Loại kho trả TỪNG loại thay vì chuỗi ghép. Định nghĩa lấy TỪ DB đang chạy, chỉ thay biểu thức so khớp. Bảng khác (TmsOrder/ProductionImport/gate_registrations/inbound_plan_lines) đếm 0 dòng ghép → không đụng. Verify staging: list/band/dashboard/control-tower ĐỒNG LOẠT 54 (scope user) · 32 (chỉ PM01) · 4 (chỉ FG02, không lọt oan) · 58 (admin); rename thử trong transaction rollback = 67 dòng ghép được đổi | ✅ apply | ✅ apply 15/08 |
| `20260731e_forklift_report_dashboard.sql` | Xe nâng đợt 4 (user: "báo cáo là raw data, đánh giá kém" → dashboard): RPC `forklift_report` ĐỔI SHAPE array → object `{rows, issue_items}`; `issue_items` = TOP 10 hạng mục bị đánh LỖI nhiều nhất (bóc `jsonb_array_elements(checklist)` where ok='false', gom theo label snapshot). BE parse shape mới cùng commit — production nhận cả cụm khi merge, không có cửa sổ lệch | ✅ apply | ✅ apply 15/08 |
| `20260731d_forklift_photo.sql` | Xe nâng đợt 3 (user chốt): xe HOẠT ĐỘNG phải CHỤP ẢNH XE mới được lưu check list (xe nghỉ khỏi check an toàn/ảnh). (1) `forklift_daily_logs.photo_path`; (2) **bucket Storage ĐẦU TIÊN của app** `forklift-photos` — riêng tư (public=false, không policy trên storage.objects → chỉ service role): BE nhận `photo_data` (data URL, FE đã nén canvas ≤1280px JPEG 0.75 ~200-400KB) → upload → phát **signed URL 1h** ở board/getLog (batch `createSignedUrls`); sửa lại không gửi ảnh mới = GIỮ ảnh cũ (upsert phải gửi lại photo_path kẻo NULL đè); chụp lại/xóa log → BE tự dọn object cũ (fire-and-forget) | ✅ apply | ✅ apply 15/08 (nhớ bucket theo migration)|
| `20260731c_forklift_item_warehouse.sql` | Xe nâng đợt 2 (user chốt sau nghiệm thu): (1) `forklift_checklist_items.warehouse_id` (FK Warehouse, NULL = dùng chung mọi kho) — check list 1 xe = hạng mục chung + riêng kho xe; guard BE: sửa hạng mục CHUNG cần full scope kho, riêng kho cần kho trong scope; (2) RPC `forklift_report` trả thêm `checked_at` (= updated_at log) — lịch sử biết AI check LÚC NÀO | ✅ apply | ✅ apply 15/08 |
| `20260731b_forklift.sql` | **Module XE NÂNG** — check list an toàn hàng ngày + đồng hồ giờ vận hành (user yêu cầu 31/07). 3 bảng mới (đều có DEFAULT id/updated_at, RLS bật không policy, realtime publication): `forklift_vehicles` (mã xe unique upper(code), gắn kho FK Warehouse) · `forklift_checklist_items` (hạng mục dùng chung, seed 8 dòng khi bảng rỗng) · `forklift_daily_logs` (**mỗi xe mỗi ngày 1 dòng** — UNIQUE (forklift_id, log_date) chống đua, upsert đè; status ACTIVE/IDLE, CHECK ACTIVE bắt buộc `hour_meter`; checklist jsonb snapshot label — xóa/đổi tên hạng mục không phá lịch sử). RPC `forklift_report(from,to,wh_ids[])` (plpgsql + force_custom_plan, chỉ service_role): trả dòng kèm `hours_run` = số đồng hồ lần ghi KẾ TIẾP (lateral, bỏ qua ngày nghỉ) − số hôm đó; IDLE = 0; chưa có lần sau = NULL "chờ chốt" | ✅ apply | ✅ apply 15/08 |
| `20260731_plate_format.sql` | BIỂN SỐ = **chỉ chữ+số, IN HOA, không ngăn cách** (`^[A-Z0-9]+$`, user chốt 30-31/07). Gốc: form FE chuẩn hoá từ lâu mà dữ liệu vẫn bẩn vì còn đường ghi KHÔNG qua form (API tích hợp/upload/script) + chính `vehicleController` cũ chỉ bỏ khoảng trắng, **giữ dấu gạch** nên tự đẻ ra `29E-09404`. Đo staging trước khi chạy: Vehicle 11/953 bẩn · gate 2/3 · GDO 0 · TmsVehicleSlot 0. Migration: backup schema `bak_20260731` → **gộp xe trùng** (`66H 07144` + `66H07144` cùng ĐVVT Khang Gia Tiến, chuẩn hoá xong sẽ đụng UNIQUE `Vehicle_plate_ncc_uidx`; viết tổng quát DISTINCT ON, ưu tiên giữ bản đã đúng dạng; đã kiểm 0 gate trỏ tới bản xoá) → chuẩn hoá 4 cột nghiệp vụ → chuỗi rỗng sau chuẩn hoá về NULL → gác `RAISE` nếu còn bẩn → **CHECK** trên `Vehicle` / `gate_registrations` / `GroupDeliveryOrder` / `TmsVehicleSlot` (NOT VALID + VALIDATE 2 bước). **KHÔNG đụng 2 cột lưu nguyên văn nguồn ngoài**: `WeighTicket.license_plate` (bản sao phiếu cân giấy — dạng chuẩn nằm ở `license_plate_norm`) và `erp_outbound_orders.license_plate` (raw SAP). Kèm BE: util `utils/plate.ts` (mirror `normalizeLicensePlate` của FE) áp tại 7 đường ghi. Verify staging: 4/4 bảng 0 dòng bẩn (Vehicle 953→952 do gộp trùng) · ghi thử biển sai dạng vào cả 4 bảng đều bị **23514** · biển đúng dạng vẫn ghi được | ✅ apply | ✅ apply 15/08 |
| `20260801_weigh_gate.sql` | **GATE CÂN XE khi Bắt đầu chuyến xuất** (user chốt 01/08 — "tránh quên cân trước khi làm việc" + link cân↔xe + đối chiếu KL). (1) `Warehouse.require_weigh_on_start` bool DEFAULT false (checkbox form Kho — chỉ bật ở kho CÓ trạm cân; mặc định TẮT nên mọi kho hành vi cũ nguyên vẹn); (2) `GroupDeliveryOrder.weigh_waived_at/by/reason` — vết DUYỆT BỎ QUA cân (quyền mới `outbound.weigh_waive`, route `POST/DELETE /outbound/:gdoId/weigh-waive` + cờ body `weigh_waive` ở cả 3 đường start); (3) index `idx_weigh_ticket_date_plate (weigh_date, license_plate_norm)`; (4) RPC `gdo_weight_estimates(text[])` jsonb (plpgsql + force_custom_plan, chỉ service_role): KL hàng per chuyến = Σ SL÷units_per_carton×`Material.weight_kg` (kg/thùng), trả kg_planned/kg_actual/items_missing — dùng ở detail chuyến (khối Cân xe) + trang Phiếu cân (2 cột KL tính/Lệch). Gate BE (`checkWeighGate` outboundController): kho bật cờ → biển số chuẩn hoá phải khớp phiếu cân `is_complete=false` HÔM NAY (giờ VN) cùng kho (phiếu chưa gắn kho vẫn tính), chưa gắn chuyến khác → không có = 422 `WEIGH_REQUIRED`; qua cổng thì tự gắn phiếu vào chuyến (`matched_by='auto-start'`, chỉ phiếu chưa gắn — không đè match tay); chuyển nội bộ không biển số = miễn; cột chưa apply = fail-open (không chặn vận hành cửa sổ deploy). Bổ sung cùng ngày (user chốt "phải có xe đăng ký cổng VÀ xe cân", không đổi schema): kho bật cờ còn đòi **ĐĂNG KÝ CỔNG hợp lệ** trước điều kiện cân — đúng kho, chiều XUẤT, đã VÀO cổng, biển khớp; **xe ĐÃ RA vẫn hợp lệ** (có đăng ký thật — nhập bù muộn/bốc thêm đơn); chỉ khóa biển VÃNG LAI không đăng ký → 422 `GATE_REQUIRED`; "Duyệt bỏ qua" phủ CẢ cổng lẫn cân; 2 đường Xuất luôn giữ gate cân-only. Verify: test API 21/21 (4 nhánh cổng: vãng lai/chưa vào/kho khác/biển lệch + xe đã ra OK + chặn/pass+link/phiếu-complete-không-tính/waive 2 chiều/kho tắt cờ/quick-export/RPC khớp tính tay 360÷24×11,875=178,1) + Playwright dialog chặn→duyệt→start (desktop+mobile) | ✅ apply | ✅ apply 15/08 (apply TRƯỚC khi deploy)|
| ~~`20260801b_small_delivery.sql`~~ (BỎ — bị 20260801c thay thế, KHÔNG apply production) | **GIAO LẺ tại kho bật gate cổng/cân** (user sửa thiết kế 01/08: "vãng lai có thể là giao nhân viên, xe máy" — không phải mọi vãng lai là xe tải né quy trình). `GroupDeliveryOrder.small_delivery_at/by` — người bấm Bắt đầu TỰ KHAI "Giao lẻ / xe máy / nhân viên nhận — không qua cổng-cân" (checkbox chỉ hiện ở kho bật cờ, KHÔNG cần quyền duyệt) → miễn cả 2 điều kiện, ghi vết ai khai + badge xanh trên chuyến để quản lý đối soát; TÁCH cột khỏi `weigh_waived_*` để báo cáo phân biệt tự-khai vs được-duyệt; gỡ bắt đầu xóa vết (khai theo lượt). Verify: test API 26/26 (giao lẻ pass không cổng/cân + vết đúng cột + unstart xóa + start lại không tick bị chặn như thường) | ✅ apply | ✅ apply 15/08 (apply TRƯỚC khi deploy)|
| `20260801c_gate_rule_split.sql` | **TÁCH 2 RULE Bắt đầu chuyến xuất + BỎ giao lẻ tự khai** (user chốt lại 01/08: "Việc bắt đầu và chọn là rủi ro… 2 rule: 1 chọn có trong ĐĂNG KÝ, 2 chọn có trong CÂN — chọn cái nào chấp hành cái đó, cả 2 thì phải đủ cả 2"). (1) `Warehouse.require_gate_on_start` — rule 1 tách khỏi rule 2 `require_weigh_on_start` (2 checkbox độc lập form Kho); (2) **DROP** `GroupDeliveryOrder.small_delivery_at/by` (20260801b) — mọi miễn trừ (giao lẻ/xe máy/NV nhận/hỏng cân) đi 1 đường DUY NHẤT: duyệt TRƯỚC trên chuyến (`weigh_waived_*`, quyền outbound.weigh_waive; chuyến đã duyệt → biển số tùy chọn). Kèm code: bỏ toàn bộ cờ body bypass (weigh_waive/small_delivery) + nút duyệt trong dialog; kho bật rule 1 → picker chuyến bỏ ô nhập biển vãng lai (tick đặc biệt = xe đã ra/bốc nhiều đơn). Verify: test API 23/23 ma trận 4 kho (gate-only/weigh-only/both/none) + validate gate chi tiết (kho khác/chưa vào/biển lệch) + cờ bypass vô hiệu + duyệt/hủy duyệt + Playwright (picker không còn ô vãng lai, form Kho 2 checkbox) | ✅ apply | ✅ apply 15/08 (apply TRƯỚC deploy; BỎ QUA 20260801b)|
| `20260801d_gate_waive_split.sql` | **TÁCH DUYỆT MIỄN TRỪ theo TỪNG RULE** (user chốt 01/08: "phân thành 2 tình huống và 2 action riêng — có khi đăng ký cổng nhưng không cân hoặc ngược lại"). `GroupDeliveryOrder.gate_waived_at/by/reason` — vết duyệt bỏ qua RULE 1 (đăng ký cổng), quyền MỚI `outbound.gate_waive`, route `POST/DELETE /outbound/:gdoId/gate-waive`; `weigh_waived_*` thu hẹp nghĩa = CHỈ RULE 2 (cân). Duyệt rule nào thoát rule đó — duyệt cổng KHÔNG thoát cân ké (chuyến không biển ở kho rule cân vẫn cần duyệt cân riêng); duyệt cổng ⇒ biển số tùy chọn. FE: 2 nút "Bỏ qua cổng"/"Bỏ qua cân" ở detail chuyến + banner/badge theo từng vết. Verify: test API 29/29 (thêm khối F duyệt lẻ từng rule: chỉ cổng → vẫn chặn cân cả khi không biển · chỉ cân → vẫn chặn cổng + biển vẫn bắt buộc · đủ 2 vết → start không biển OK · hủy 1 vết không đụng vết kia) | ✅ apply | ✅ apply 15/08 (apply TRƯỚC deploy)|
| `20260802_employee_categories_default.sql` | **BỎ DEFAULT lỗi thời của `Employee.allowed_categories`** (check-app 02/08, user chốt "bỏ giá trị cũ đi"). Cột đang DEFAULT `ARRAY['TP','NVL','POSM','BAO_BI']` = taxonomy CŨ trong khi Loại kho hiện dùng mã SAP (FG01/PM01/RM01/PK01) ⇒ mọi đường ghi thẳng DB không khai loại (script import nhân sự, seed) đẻ scope RÁC → user đó dính 403 "Ngoài phạm vi Loại hàng" ở MỌI thao tác mà không hiểu vì sao (chính bẫy này làm hỏng lượt test phân quyền đầu tiên). DROP DEFAULT (NULL = không giới hạn, đúng nghĩa "chưa cấu hình"; `createEmployee` đã tự điền đủ danh mục HIỆN TẠI từ LookupValue khi form không gửi) + dọn di sản dòng mang TOÀN mã cũ về NULL. Verify staging: default=null, 39/39 nhân viên giữ nguyên mã SAP, 0 dòng mã cũ | ✅ apply | ✅ apply 15/08 |
| `20260802b_gdo_origin.sql` | **NGUỒN GỐC CHUYẾN XUẤT — Xuất/Nhặt lẻ là KẾT QUẢ DẪN XUẤT của VL06O + Kế hoạch xuất** (user chốt 02/08: "bắt buộc SAP và thực xuất khớp nhau, muốn sửa thì sửa ở nguồn trước"). `GroupDeliveryOrder.origin` text: `'SAP'` (sinh từ uploadKhvc, item mang od_refs) → **khóa sửa phần KẾ HOẠCH trên đơn** — updateGDO 422 `SAP_PLAN_LOCKED` khi ĐỔI ngày/kho/ship-to/loại kho/ĐVVT/NPP/Số DO/Loại xe hoặc THÊM dòng tay (so GIÁ TRỊ đổi, không chặn oan FE gửi kèm giá trị cũ; khóa mức DÒNG SL/mã/xóa đã có từ 22/07 qua od_refs); `'EXCEL'` (upload KH xuất kiểu cũ — kho không làm SAP) / `'MANUAL'` (tạo tay + Xuất luôn) / `'LEGACY'` (backfill không suy được nguồn) → sửa như cũ. Phân theo TỪNG CHUYẾN, không theo kho. Backfill: có item od_refs → SAP (staging 121/122). Kèm code cùng đợt (không đổi schema): **`replanKhvcGroups`** — CRUD tab Kế hoạch xuất TỰ DỘI xuống chuyến (thêm dòng → sinh chuyến · sửa → cập nhật qua nguyên derivation processVehicleGroups res-facade · xóa hết dòng → xóa chuyến PENDING sạch · chuyến ĐANG XUẤT/ĐÃ HT → reconcile_task `KHVC_CHANGED` không tự đụng); create/update khvc guard DO phải có trong VL06O (400); derive lọc raw OBSOLETE (vá bug uploadKhvc cộng lại dòng SAP đã bỏ); message Hoàn thành thiếu trên chuyến SAP chỉ đường sửa DO SAP. Gói QA `12-derived-lock.mjs` (10 case, trong run-all) | ✅ apply | ✅ apply 15/08 |
| `20260801e_forklift_rls_policy.sql` | **FIX realtime module Xe nâng** (check-app 01/08): 3 bảng `forklift_vehicles/forklift_checklist_items/forklift_daily_logs` bật RLS (20260731b) nhưng THIẾU policy `rls_auth_select` → role authenticated không SELECT được → Supabase Realtime không phát event (board 2 người cùng check không thấy nhau) — đúng bài học 20260716_weigh_ticket_rls_policy. Migration tạo policy SELECT cho authenticated trên cả 3 bảng (idempotent). Kèm đợt fix check-app cùng ngày (code, không đổi schema): outbound bịt 5 đường lách rule cổng/cân (quét/Lưu thủ công khi chưa Bắt đầu, PATCH status tự do, Sửa xe không re-check, unstart không gỡ phiếu cân auto-start + không trả phiếu cổng, matchWeighTicket không scope/cướp phiếu) + giao lẻ gate-waived sửa được thông tin xe + checkWeighGate hết chặn oan khi start lại + gói QA mới `11-gate-rules.mjs` gác vĩnh viễn | ✅ apply | ✅ apply 15/08 (apply TRƯỚC deploy)|
| `20260803_awaiting_sap_and_events.sql` + `20260803b_gdo_plan_dropped.sql` | **KẾ HOẠCH XUẤT ĐI TRƯỚC DỮ LIỆU SAP + SỔ LỊCH SỬ CHUYẾN** (user chốt 03/08: "điều vận upload Kế hoạch xuất khi chưa có VL06O"; "kế hoạch bị xóa ⇒ chuyến vào trạng thái không hoạt động, chỉ xem info + lịch sử"; "DO trên chuyến phải tracking được — thay đổi thế nào, bởi ai, lúc nào, nguồn nào"). (1) `GroupDeliveryOrder.awaiting_sap` bool + `awaiting_dos text[]` — xe còn DO chưa có VL06O vẫn SINH CHUYẾN nhưng BẤT ĐỘNG (bỏ hẳn 400 `MISSING_DO`); VL06O về → `uploadVl06o` tự replan các xe đang chờ → dựng dòng hàng + tắt cờ (realtime `erp_outbound_orders → gdos-paged/gdo`, không cần F5). (2) `plan_dropped` + `plan_dropped_at` — kế hoạch hết dòng cho Số xe thì **KHÔNG xóa chuyến nữa** (trước đây replan DELETE cứng, mất vết): đánh dấu ngừng hoạt động, nhả tồn nhặt lẻ đã giữ, kế hoạch có lại → tự sống lại. Dùng CỜ chứ không thêm status mới (status len lỏi ~20 chỗ lọc/màu/RPC/QA). Guard `inertError` → 422 `TRIP_INERT` ở 9 cửa ghi (start · 2 đường Xuất luôn · quét · kiểm quét · Lưu thủ công · soạn/xác nhận nhặt lẻ · PATCH status · assign · updateGDO), vẫn cho HỦY để dọn. (3) Bảng **`outbound_events`** (RLS + realtime, index `(gdo_id, created_at DESC)`) = dòng thời gian chỉ-ghi-thêm: PLAN_DO_ADDED/REMOVED · PLAN_DATE_CHANGED · PLAN_VEHICLE_DROPPED/REOPENED · AWAITING_SET/CLEARED — tách khỏi `reconcile_tasks` (bảng đó là HÀNG CHỜ VIỆC, nhét nhật ký vào sẽ loãng); API `GET /wms/outbound/:id/events` GỘP 2 nguồn (sổ + thay đổi từ SAP) cho nút "Thông tin" trên trang chi tiết. Kèm: **CẤM xóa dòng Kế hoạch xuất của chuyến ĐANG XUẤT / ĐÃ HOÀN THÀNH** (409 — chứng từ đã chốt); vá cột+filter "Trong DO SAP" bỏ sót dòng `OBSOLETE`; uploadKhvc ghi tầng raw SAU khi qua validate (trước ghi trước rồi mới 400). Gói QA `13-awaiting-sap.mjs` (29 phép kiểm, trong run-all) + ratchet `replan_hard_deletes_gdo` baseline 0 | ✅ apply | ✅ apply 15/08 |
| `20260803c_tms_order_from_khvc.sql` | **KẾ HOẠCH VC (lệnh booking TMS) = KẾT QUẢ DẪN XUẤT của Kế hoạch xuất** (user chốt 03/08: "khi điều vận upload Kế hoạch xuất, dữ liệu ở module Kế hoạch VC sẽ được tự động chuyển lên — dạng bị động, chạy theo Kế hoạch xuất"). `TmsOrder.origin` (`'KHVC'` = tự sinh) + `plan_dropped`/`plan_dropped_at`. **Khóa = SỐ XE** xuyên 3 tầng `khvc_lines.group_code` = `GroupDeliveryOrder.group_code` = `TmsOrder.order_code` (bên TMS cột "Số xe" vốn đã đổ vào order_code — không bịa khóa mới) ⇒ sửa/thêm/bớt DO trong xe thì lệnh + KHUNG GIỜ ĐÃ ĐẶT **giữ nguyên**, chỉ số liệu cập nhật. Service `services/tmsPlanSync.ts` gọi ở 2 điểm: cuối `processVehicleGroups` khi luồng KHVC (upload + replan) và nhánh xe-bị-bỏ của `replanKhvcGroups`. **Cả xe biến mất khỏi kế hoạch → `plan_dropped=true` + TỰ NHẢ khung giờ** qua RPC `book_vehicle_slot(p_new_slot_id=NULL)` — đi ĐÚNG đường nguyên tử như nút "Trả lại" để `booked_count` không trôi (bài học `tms-slot-booking-atomic`); xe có lại → lệnh sống lại **KHÔNG kèm khung giờ**, phải booking lại. Số xe đã có lệnh tạo tay → **NHẬN NUÔI** (cập nhật + đóng dấu KHVC), không tạo trùng (order_code UNIQUE → 23505 sẽ phá cả lượt upload). Pallet/Thùng suy từ dòng hàng, Tấn dùng RPC `gdo_weight_estimates` (không đẻ công thức KL thứ hai); chuyến đang CHỜ dữ liệu thì để trống, lượt đồng bộ khi kích hoạt sẽ điền. Lệnh tự sinh: sửa/xóa tay → 422 `TMS_PLAN_DERIVED` (ghi chú/ưu tiên/ETA/trạng thái vẫn sửa — đồng bộ không đụng tới); FE badge "Từ KH xuất"/"KH đã bỏ". Gói QA `14-tms-plan-derived.mjs` (22 phép kiểm, trong run-all) — bắt được cả hệ quả phụ: cleaner gói 12 phải dọn TmsOrder trước khi xóa kho test (FK) | ✅ apply | ✅ apply 15/08 |
| `20260804_fill_tasks.sql` | **FILL HÀNG PHỤC VỤ NHẶT LẺ** (user chốt 04/08: "dựa trên lượng hàng nhặt lẻ, tính lượng hàng đang ở vị trí dưới đủ hay thiếu; thiếu thì đề xuất/ra lệnh fill; lệnh gắn cho ai thì người đó thực hiện, có tính kết quả + tỷ lệ hoàn thành"). (1) **`Location.is_pick_face`** bool DEFAULT false + index partial — "vị trí nhặt lẻ" (chỗ với tay lấy hàng được) do KHO TỰ KHAI, không suy từ tên tầng: kho khai bằng nút hàng loạt ở trang Vị trí kho (mở rộng route `bulk-flag` sẵn có — áp theo BỘ LỌC nên không nhồi id qua URL) hoặc tick trong form sửa. Mặc định false ⇒ kho chưa khai thì hành vi cũ KHÔNG đổi, trang Fill cảnh báo rõ thay vì báo thiếu oan. (2) Bảng **`FillTask`** — MỖI DÒNG = 1 PALLET phải hạ (nguồn→đích, người phụ trách, vết ai-làm-lúc-nào), status PENDING/DONE/CANCELLED; **unique index `uq_filltask_pending_entry` (entry_id WHERE status='PENDING')** = hàng rào chống đua ở tầng DB (2 người cùng ra lệnh 1 pallet → 1 thắng, người kia nhận `skipped` có lý do; JS check không đỡ nổi 2 request cùng mili-giây). Hủy = đánh dấu CANCELLED, KHÔNG xóa cứng (giữ vết). (3) RPC **`fill_demand`** (cần/đang có/thiếu + pallet nguồn FEFO + đích gợi ý — MỘT lời gọi thay vì vài request/mã): công thức "cần" LẤY NGUYÊN của `loose_picking_page` (nguồn sự thật duy nhất của nhặt lẻ — chép công thức thứ hai là hai màn hình lệch nhau ngay lần sửa luật sau); loại trừ chuyến `awaiting_sap`/`plan_dropped`; loại hàng theo `wt_cats() &&` (giao ≥1, không so nguyên chuỗi); chọn pallet tham lam theo FEFO tới khi đủ bù thiếu; **định nghĩa "chỗ đã chiếm" KHỚP RPC `move_pallets_to_location`** (IN_STOCK/PARTIAL/QUARANTINE) — đếm khác nơi gác thì hoặc gợi ý chỗ rồi quét báo đầy, hoặc bỏ sót chỗ trống. (4) **`fill_tasks_page`** (dòng + tổng + 4 ô band 1 lời gọi, kèm vị trí HIỆN TẠI của pallet để thấy lệch) + **`fill_report`** (tỷ lệ hoàn thành theo người; tổng SL cross-mã qua `qty_entry_decimal` per-mã trước khi cộng). (5) `locations_page` thêm tham số `p_pick_face` (DROP chữ ký cũ trước — thêm tham số có DEFAULT sẽ tạo overload và PostgREST gọi theo tên sẽ "function is not unique"); controller KHÔNG hạ cấp âm thầm khi RPC cũ mà trả 503 (bỏ tham số lọc = trả danh sách chưa lọc mà người dùng tưởng đã lọc). Quyền: module mới `fill` (view/plan/assign/execute). Gói QA `18-fill-replenish.mjs` (20 phép kiểm, trong run-all) + 3 bất biến gói 00 (1 pallet 1 lệnh treo · DONE phải có mốc · đích vẫn là vị trí nhặt lẻ) + ratchet `location_write_without_move_rpc` | ✅ apply | ✅ apply 15/08 |
| `20260804b_location_flag_tristate.sql` | **BỘ LỌC CỜ VỊ TRÍ THÀNH 3 TRẠNG THÁI** (user báo 04/08: "xem cái filter của nó nhé, thấy đang k đúng"). Chỉ đổi `locations_page` + `locations_summary` (không đổi bảng): `p_flag` từ `DEFAULT false` + `(NOT p_flag OR requires_stocktake)` → `DEFAULT NULL` + `(p_flag IS NULL OR requires_stocktake = p_flag)` — giống hệt `p_pick_face`, nhờ đó lọc được chiều **"CHƯA khai cờ"** (đúng cái cần khi đi khai cờ cho cả kho; trước chỉ có "không lọc / chỉ vị trí đã gắn cờ"). Chữ ký KHÔNG đổi ⇒ `CREATE OR REPLACE` là đủ, không sinh overload. Kèm code cùng đợt: (a) **fix bug lọc không hề cắt** — hook FE `locationsQp` không map `pick_face` xuống query-string nên chip lọc bật mà danh sách vẫn trả đủ 1.517 dòng, KHÔNG exception nào để lần ra; kiểu trả về siết thành `Record<keyof LocationsListParams, …>` để thêm field lọc mà quên map = LỖI BIÊN DỊCH; (b) 2 bộ lọc đổi từ multi-1-lựa-chọn (chip in trùng nhãn "Vị trí nhặt lẻVị trí nhặt lẻ") sang chọn-1 Có/Chưa; (c) bảng Vị trí kho thêm 2 CỘT riêng "Nhặt lẻ" + "Cần check hàng ngày" thay 2 dấu hiệu nhỏ nhét cạnh mã vị trí. QA 18 mục 15 gác: lọc phải khớp oracle · "có" + "chưa" = tổng (cờ bị bỏ rơi thì tổng gấp đôi) · ô tổng đếm cùng tập với danh sách | ✅ apply | ✅ apply 15/08 |
| `20260804c_locations_zone_filter.sql` | **BỘ LỌC "KHU VỰC KHO" cho trang Vị trí kho** (user yêu cầu 05/08). `locations_page` + `locations_summary` thêm `p_subs text[]` (NULL = không lọc; so `sub_code = ANY`; mảng RỖNG trả RỖNG theo ngữ nghĩa parseListParam) — thêm tham số = ĐỔI CHỮ KÝ nên DROP 2 bản cũ trước (bài học 20260804). Controller: `ctx.subs` bóc từ `?zones=` bằng `parseListParam` (không tự split), nhánh dự phòng PGRST202 từ chối hạ cấp khi đang lọc khu. FE: filter multi option từ khu của kho đang chọn, đổi Kho tự reset khu (khu thuộc kho — giữ lại là lọc theo khu của kho CŨ, sai âm thầm nếu trùng mã). Kèm fix cùng đợt: **Xuất Excel trang Vị trí từng BỎ QUÊN bộ lọc `pick_face`** vì `fetchAllFiltered` tự chép tay query-string thay vì dùng `locationsQp` — nay export hàm đó và dùng chung (cùng họ bug chip-lọc-không-cắt 04/08, hết đường chép tay thứ hai). QA 18 mục 16 (khớp oracle · `?zones=` rỗng trả rỗng · ô tổng cùng tập) + fuzz 07 thêm 3 URL zones. Verify sống Preview: LNVL+TP1 = 78+1.050 = 1.128 khớp DB; combo khu × nhặt lẻ = 25; bulk by_filter tôn trọng khu (khu không tồn tại → 400, 0 ghi) | ✅ apply | ✅ apply 15/08 |
| `20260805_fill_dest_category.sql` | **FILL HÀNG: vị trí ĐÍCH phải khớp LOẠI KHO của mã** (user bắt 05/08: "hàng ở FG02 lại được yêu cầu chuyển về FG01 là sai nguyên tắc" — đo thật: Kho Ba Vì 3 mã FG02 bị gợi ý về vị trí nhặt lẻ khu FG01 vì cả 4 vị trí nhặt lẻ của kho đều khai FG01). `fill_demand` chọn `to_location` chỉ theo "còn chỗ", không so Loại — và cả 4 cửa còn lại trong `fillController` cũng vậy. Fix: gợi ý RPC + tự chọn đích khi ra lệnh (`takePickFace`) + đích user chỉ định (`createFillTasks`, sai loại = báo rõ và bỏ dòng, KHÔNG âm thầm đổi chỗ) + đổi đích (PATCH → 400 `CATEGORY_MISMATCH`) + ô chọn đích (`pick-face-locations?material_id`) đều đi qua luật `locAcceptsCat` — ĐÚNG khuôn picker vị trí toàn app: `categories` chứa loại của mã HOẶC vị trí chưa khai loại (NULL) HOẶC mã chưa khai loại (null-inclusive 2 chiều). "Đang có ở vị trí nhặt lẻ" vẫn đếm THỰC TẾ vật lý (pallet lệch loại di sản vẫn nhặt được) — luật loại chỉ áp khi chọn ĐÍCH MỚI. FE: cột "Đề xuất hạ về" ghi "hết chỗ nhận loại này" kèm tooltip chỉ đường khai thêm. Chữ ký `fill_demand` KHÔNG đổi (CREATE OR REPLACE). QA 18 cụm 17 (5 phép kiểm, đủ 5 cửa; fixture locBad CHỨA SẴN mã + trống nhất kho — luật vắng mặt thì mọi cửa đều chọn nó, không vacuous) — 41/41 PASS | ✅ apply | ✅ apply 15/08 |
| `20260805b_fill_demand_v2.sql` | **FILL HÀNG v2** (user chốt 05/08, 3 việc): (1) **Kho KHÔNG có vị trí nhặt lẻ nào NHẬN LOẠI của mã = kho không nhặt lẻ loại đó → LOẠI HẲN khỏi bảng Đề xuất** (đo 04/08: Ba Vì 3 mã FG02 biến mất, còn 10 mã FG01 — cả 4 vị trí nhặt lẻ đều FG01); xét trên MỌI vị trí đang hoạt động kể cả đầy (đầy = tạm thời ≠ không phục vụ). (2) `fill_demand` trả thêm `category` + `production_date` per pallet gợi ý → FE tab Đề xuất thêm cột + filter **Loại kho**, cột **"Vị trí lấy hàng"** (nguồn của pallet gợi ý), cột **"SL hạ (chẵn pallet)"** (xe nâng hạ NGUYÊN pallet — tổng hạ ≥ thiếu, làm tròn theo pallet). (3) RPC MỚI **`fill_candidates`** (pallet ứng viên 1 mã, FEFO, CÙNG điều kiện nguồn với fill_demand: ngoài vị trí nhặt lẻ · không QUARANTINE/hàng block · trừ phần đang giữ · chưa có lệnh treo) → dialog **"Chọn date"** per mã: người nhặt lẻ chọn NSX từ tồn thật, không chọn = FEFO (date xa nhất); pallet tick sẵn FEFO tới khi đủ bù thiếu; route `GET /wms/fill/candidates` (fill.view). Kèm fix 2 chip toggle in trùng nhãn ("Chỉ mã đang thiếu" ×2 — cùng họ bug 04/08). Verify oracle 04/08 Ba Vì: 16 pallet gợi ý — 0 ở vị trí nhặt lẻ, 0 QUARANTINE, 0 hết khả dụng, 0 trùng lệnh treo. QA 18 cụm 18 (loại trừ, kỳ vọng động theo DB) + 19 (candidates) — 45/45 PASS; fuzz 41/41 | ✅ apply | ✅ apply 15/08 |
| `20260805c_rls_close_remaining.sql` | **ĐÓNG RLS các bảng sót** (email cảnh báo Supabase 03/08 `rls_disabled_in_public` — CẢ 2 project). Audit 12/07 đã đóng toàn bộ; sót SAU đó 11 bảng trên staging: `StocktakeLog` (nghiệp vụ, đang phát realtime màn Kiểm kê), `base_unit_flip_round_report`, 9 bảng backup `x_bak_*`/`x_flip_bak_*` — ai có URL + anon key đọc/sửa được. Fix: vòng lặp ĐỘNG bật RLS trên MỌI bảng public còn tắt (production bộ bảng sót có thể khác staging, 1 file chạy đúng cả 2); BE dùng service role bypass nên hành vi app KHÔNG đổi; bảng không policy = đóng kín. NGOẠI LỆ: `StocktakeLog` thêm `rls_auth_select` (nằm trong TABLE_QUERY_MAP — bật RLS không policy là realtime Kiểm kê chết CÂM, bài học weigh_ticket/forklift). Gác RAISE trong migration + **RPC `rls_gap_tables`** (SECURITY DEFINER, revoke anon/authenticated) + bất biến gói 00 "mọi bảng public bật RLS" — bảng mới quên RLS đỏ trong ngày. Verify: anon probe 4 bảng = 0 dòng, realtime invariant 19/19. **⚠️ PRODUCTION: chạy NGAY file này trong Supabase Dashboard → SQL Editor của project `svicyfquresxaigfxsdb` — KHÔNG cần chờ merge main** (an toàn: không đổi hành vi app, chỉ đóng cửa anon) | ✅ apply | ⚠️ chạy NGAY qua Dashboard |
| `20260805d_fill_orders.sql` | **LỆNH FILL v3 — GOM THÀNH 1 LỆNH + CHỈ ĐỊNH THEO DATE** (user chốt 05/08 chiều: "vẫn đang yêu cầu đúng tem pallet nào đưa về là không cần thiết — thể hiện date và %date yêu cầu; lệnh fill gom lại thành 1 lệnh, mở ra mới thấy chi tiết; quét QR trong lệnh + trong chi tiết, màn quét cho đổi vị trí đến; chi tiết multi-select đổi vị trí/xóa/giao người theo quyền"). (1) Bảng **`FillOrder`** (1 lần Ra lệnh = 1 lệnh, order_code `F<yymmdd>-NN` unique, status rollup từ dòng) — realtime publication + policy `rls_auth_select` NGAY TRONG migration (bài học RLS-chết-câm 04/08). (2) **`FillTask` thành DÒNG của lệnh, KHÔNG ghim pallet**: thêm `fill_order_id NOT NULL` (cascade), `required_date`+`required_expiry` (NSX yêu cầu + HSD để tính %Date), `required_pallets`/`scanned_pallets`/`qty_done_base` (tiến độ hạ DẦN); `entry_id`/`pallet_code` DROP NOT NULL (chỉ còn dòng di sản); dòng PENDING di sản bị HỦY kèm lý do (mô hình cũ không diễn dịch được), mọi dòng gom vào lệnh di sản `F…-LN`, gác RAISE nếu còn mồ côi. **Unique chống đua ĐỔI KHÓA: `uq_filltask_pending_matdate` (kho, ngày xuất, mã, COALESCE(date)) WHERE PENDING** — 2 người cùng ra lệnh 1 (mã,date) thì 1 thắng. (3) Bảng **`FillTaskScan`** = vết từng pallet quét (ai/tem/NSX/từ đâu về đâu/bao nhiêu), unique (task_id, entry_id) chống ghi trùng, RLS bật. (4) RPC **`fill_scan_apply`** — CẢ bước quét trong MỘT transaction: khoá dòng lệnh FOR UPDATE → kiểm mã/date/nguồn-không-phải-nhặt-lẻ/khả dụng → gọi `move_pallets_to_location` (khoá sức chứa) CÙNG transaction → ghi vết → cộng tiến độ → DONE dòng khi đủ pallet HOẶC đủ SL → `fill_order_rollup` chốt lệnh (tách 2 câu PostgREST = 2 transaction — đúng lớp lỗi adjust 23/07). (5) `fill_orders_page` thay `fill_tasks_page` (DROP — trang Lệnh giờ là danh sách LỆNH GOM: dòng + tổng + 4 ô band + tổng quy đổi per-mã, search unaccent trên mã lệnh/mã hàng/người); `fill_demand` CTE `pend` đổi thành `Σ(qty_base − qty_done_base)` (dòng hạ dần — trừ nguyên qty_base là đếm trùng phần đã nằm ở vị trí nhặt lẻ); `fill_report` "đã hạ" = `qty_done_base` (phần THẬT SỰ chuyển); `fill_candidates` BỎ điều kiện "chưa có lệnh treo" (lệnh không ghim tem nữa). API: `POST/GET/DELETE /wms/fill/orders(+/:id)` thay `/fill/tasks` (PATCH/DELETE dòng giữ); `POST /fill/scan` thành 2 bước preview→commit, nhận `to_location_id` đổi đích ngay màn quét (gate `fill.execute`, phạm vi hẹp: vị trí nhặt lẻ + nhận đúng Loại kho — tiền lệ `leftover_location_id`); sai date → 409 `DATE_MISMATCH` nêu rõ NSX+%Date yêu cầu. FE: tab Lệnh fill = danh sách lệnh, trang mới `/wms/fill/orders/:id` (multi-select Giao cho `fill.assign` · Đổi vị trí đến/Hủy dòng `fill.plan` · Quét `fill.execute`), màn quét confirm-flow có ô "Vị trí đến". QA 18 viết lại (đua theo (mã,date) · DATE_MISMATCH · preview-không-ghi · đua 2 người cùng tem 1 ăn · rollup lệnh) | ✅ apply | ✅ apply 15/08 |
| `20260805e_fill_orders_hints.sql` | **fill_orders_page trả thêm `src_hints` + `dest_codes`** (gộp DISTINCT vị trí lấy/về của các dòng CÒN TREO per lệnh) — phục vụ yêu cầu 05/08 "user dùng điện thoại cần thông tin và thao tác Ở VỊ TRÍ NÀO ngay ở view đầu tiên": tab Lệnh fill + trang chi tiết trên mobile đổi sang THẺ VIỆC (card) — vị trí **LẤY → VỀ** chữ to, nút Quét ngay trên thẻ, toggle "Việc của tôi" nổi trên mobile; bảng đầy đủ cột giữ nguyên cho desktop (breakpoint sm). Kèm FE: người KHÔNG có `fill.plan` mở trang tự vào thẳng tab Lệnh fill (tab Đề xuất là màn của người lập kế hoạch). jsonb output, chữ ký không đổi → CREATE OR REPLACE | ✅ apply | ✅ apply 15/08 |
| `20260805f_fill_scan_wh_direct.sql` | **fill_scan_apply: kho của pallet = cột `InventoryEntry.warehouse_id` TRỰC TIẾP** (user báo 05/08 tối: quét pallet CÓ TỒN mà bị chối "không có tồn trong kho"). Bản cũ suy kho qua `JOIN Location` nên pallet **CHƯA GÁN VỊ TRÍ** (`location_id` NULL — phổ biến ngay sau quét nhập) có `loc_wh` NULL → `IS DISTINCT FROM` = `WRONG_WAREHOUSE` oan; controller `scanFill` cùng lỗi (lọc `loc.warehouse_id`) → 404 `PALLET_NOT_FOUND`. Đổi 2 chỗ: SELECT thêm `e2.warehouse_id AS ent_wh` + check `COALESCE(loc_wh, ent_wh)`; controller lọc kho bằng cột trực tiếp + thông báo TỰ GIẢI THÍCH (pallet ở kho khác → nêu TÊN kho `WRONG_WAREHOUSE` 409; còn tồn nhưng bị GIỮ cho đơn xuất/nhặt lẻ → `RESERVED` nêu số giữ, tách khỏi `GONE` "đã xuất hết"). Gói 18 thêm cụm 20 (2 phép kiểm pallet null-location preview+commit). Kèm FE cùng đợt: màn quét Fill có CHẾ ĐỘ SÚNG PDA (useWedgeScanner, bắn 1 phát = tắt camera, bắn lại đúng tem đang chờ = Xác nhận hạ; bóp cò ngay trên tab Lệnh fill / trang chi tiết = tự mở màn quét chế độ súng) | ✅ apply | ✅ apply 15/08 |
| `20260806e_alerts_expiry_fix.sql` | **VÁ RULE "TỒN CẬN DATE" CHẾT CÂM** (check-app 06/08 bắt ngay lượt khảo sát đầu): `InventoryEntry.production_date` là **timestamp** (không phải `date` như `expiry_date`) ⇒ `production_date + integer` không có toán tử ⇒ RPC `alerts_expiry_candidates` nổ 42883, scanner nuốt qua try/catch per-rule ⇒ rule EXPIRY **KHÔNG BAO GIỜ chạy** (đo: 120 nhóm kho×mã = 6,43 triệu base cận date mà 0 cảnh báo). Fix `::date` trước khi cộng ngày. Kèm: scanner ghi `error_logs` code `ALERT_RULE_FAILED` khi rule lỗi (không chỉ console.error — serverless không ai đọc) + QA gói 20 check RPC nền chạy được & không rule nào lỗi sau lượt quét. Sau vá: 208 ứng viên → 40 cảnh báo thật | ✅ apply | ✅ apply 15/08 |
| `20260806f_user_notifications_rls_own.sql` | **VÁ RÒ FEED CÁ NHÂN** (check-app 06/08, mức nghiêm trọng): policy `USING (true)` + vé realtime (`role=authenticated`, `sub=employee_id`) ⇒ BẤT KỲ user nào cầm anon key (có sẵn trong bundle FE) đọc được **toàn bộ thông báo cá nhân của người khác** qua PostgREST — đo thật: user B đọc được thông báo riêng của A. API backend vốn khoá đúng, chỉ đường Supabase trực tiếp hở. Đổi policy sang `employee_id = auth.uid()`; verify 2 CHIỀU: người lạ đọc 0 dòng **và** chính chủ vẫn đọc + vẫn nhận realtime INSERT (chống lớp lỗi "siết RLS làm realtime chết câm" 04/08). QA gói 19 check policy ≠ USING(true) | ✅ apply | ✅ apply 15/08 |
| `20260806g_user_notif_dedupe.sql` | **GỘP THÔNG BÁO Ở TẦNG DB** (check-app 06/08: giao 6 dòng lệnh fill SONG SONG sinh 4 thông báo — dedupe cũ là ĐỌC-RỒI-GHI trong JS, thua đua). Unique `uq_user_notif_target (employee_id, kind, url) NULLS NOT DISTINCT` + `notifyEmployees` chuyển sang `upsert(onConflict)` ⇒ 1 người + 1 loại việc + 1 đối tượng = ĐÚNG 1 dòng feed; báo lại thì LÀM MỚI dòng đó (created_at mới, read_at=null → nổi lên đầu, đếm chưa đọc lại). Migration tự dọn trùng di sản trước khi tạo index. QA gói 19 mục 10 gác | ✅ apply | ✅ apply 15/08 |
| `20260810d_outbound_pool_atomic.sql` | **BUG #6 — HOÀN TỒN KHỐNG dưới bão 504** (check-app 10/08, đo thật: pool 15→23, remaining>imported, adjustment 0): 3 đường ghi-nhận/hoàn pool no-QR (manualCompleteItem · quickExportGDO · quickExportExistingGDO) là CHUỖI request rời (đổi pool → update cartons_scanned → upsert vết quét) — Vercel kill ở 60s GIỮA chừng ⇒ lượt hoàn sau tính delta từ scanned CŨ → hoàn ĐÔI; chiều xuôi kill giữa chừng → MẤT tồn âm thầm. Fix = RPC **`outbound_pool_apply`**: TRỌN chu trình 1 transaction (khóa OutboundItem FOR UPDATE → delta tính TRONG transaction từ chính cartons_scanned → trừ/hoàn pool có khóa dòng, FEFO/chọn NSX cho QTY_DATE, gộp đa-dòng cùng pallet_code → update item + vết quét). **Số truyền vào TUYỆT ĐỐI, không phải delta** ⇒ gọi lại lần 2 delta=0, hoàn đôi bất khả thi theo cấu trúc; kill = rollback trọn. `p_claim_only_pending` thay cú CAS-claim + hoàn-bù của "Xuất luôn" (thua đua = CLAIM_LOST, không đụng tồn). Gỡ `applySharedPoolDelta`/`applyToPoolRow` (dead). Lưới gác = check "Pool về baseline" gói 05-rush (chính nó bắt bug). Verify: oracle 8 phép + đua + lặp-retry + TÁI HIỆN bão 40 luồng → pool về baseline | ✅ apply | ✅ apply 15/08 |
| `20260810c_employee_fk_indexes.sql` | **XÓA NHÂN VIÊN TIMEOUT 8s ở quy mô thật** (bug #4 check-app 10/08, lộ khi gói QA 12 dọn fixture trên DB 390k dòng): DELETE 1 dòng Employee kích RI-check trên TỪNG FK trỏ Employee — 10 cột (`InventoryEntry.created_by/updated_by/stocktake_by` 175k · `OutboundScanEntry.scanned_by/loose_confirmed_by` 150k · GDO.forklift_driver_id · StocktakeLog.counted_by · ProductionImport ×3) KHÔNG có index ⇒ mỗi FK 1 seq scan ⇒ 57014. Ảnh hưởng cả nút Xóa tài khoản (user_admin) khi production tích đủ dữ liệu. Fix = 10 index partial `WHERE ... IS NOT NULL` (RI tra giá trị non-null nên dùng được; đa số dòng NULL nên index nhỏ). Đo: DELETE 8.000ms timeout → **308ms** | ✅ apply | ✅ apply 15/08 |
| `20260810b_scanlog_search_perf.sql` | **Ô TRUY CỨU Lịch sử quét chết 500 ở 150k dòng** (bug #3 check-app 10/08 — bắt qua UI thật: gõ tem → skeleton treo vô hạn): RPC `search_outbound_scan_log` = 13 phép `ILIKE '%q%'` trên 8 bảng ĐÃ JOIN + `carton_scans::text` cast jsonb TỪNG DÒNG + `COUNT(*) OVER()` ⇒ vật chất hóa 10-join × 150k mỗi lần gõ. Fix theo khuôn **"THU HẸP ID"** (tiền lệ omni-search 26/07): 6 nhánh UNION, mỗi CHIỀU tìm chạy trên bảng GỐC của nó (ose đơn bảng · dimension nhỏ dò trước rồi lần index về scan id) → gom `array_agg(DISTINCT)` vào MẢNG (hàm STABLE không CREATE TEMP TABLE được) → count qua 5 join tối thiểu + page 10 join CHỈ trên tập khớp, `= ANY(mảng)` (PG14+ hash SAOP). Ngữ nghĩa OR 13 chiều + scope kho/loại + luật nhặt-lẻ-đã-xác-nhận GIỮ NGUYÊN; sửa kèm delivery_date naive-UTC đồng bộ 20260810. Đo cùng 150k: tem cụ thể 0,6s ấm · NPP 2,2s · tìm CỰC RỘNG khớp 120k dòng 4,5s — 0×500 | ✅ apply | ✅ apply 15/08 |
| `20260811b_packing_runs.sql` | **TRANG SỔ ĐÓNG GÓI (11/08 chiều — user chốt "mở sổ trước mới quét")** — bảng `packing_runs` (1 dòng = 1 TRANG SẢN PHẨM trong sổ viết tay: Kho + Ngày + Ca SX + Chu kỳ + Mã + Máy + Giờ bắt đầu/kết thúc + Tổng sản lượng) + cột `packing_logs.run_id` FK. Quét tem CHỈ được khi có trang đang MỞ khớp MÃ (422 `RUN_REQUIRED`; nhiều trang cùng mã → 409 `RUN_AMBIGUOUS`, FE chọn trang); pallet KẾ THỪA Máy+Kho của trang (tem in "AP" hết mơ hồ). Bấm "Giờ kết thúc" → CLOSED + chốt `qty_total` = Σ thùng pallet sống + `pallet_count` (CAS trên status). Unique `uq_packing_run_open` (kho, mã, máy) WHERE OPEN chống 2 người mở trùng (23505→409 RUN_DUP); hủy trang chỉ khi CHƯA có pallet (409 RUN_HAS_PALLETS). Quyền MỚI `packing.open_run` (mở/đóng/sửa/hủy trang — tách khỏi `record`). RLS SELECT authenticated + publication (realtime board). QA gói 22 viết lại 24 mục | ✅ apply | ✅ apply 15/08 |
| `20260811_packing_logs.sql` | **SỔ ĐÓNG GÓI ĐIỆN TỬ (11/08)** — bảng `packing_logs` (1 pallet = 1 dòng sổ): số hóa sổ đóng gói viết tay tại xưởng. Quét tem in sẵn lúc BẮT ĐẦU xếp (OPEN, `open_scan_at`) → pallet đầy đóng sổ (CLOSED). **Giờ SX CHÍNH = chữ in phun trên thùng đầu/cuối** (`prod_start_at`/`prod_end_at` + nguồn `OCR`/`MANUAL` — FE chụp ảnh + Tesseract bậc 0 miễn phí đọc, trượt thì gõ tay; `ocr_*_raw` giữ nguyên văn để khai thác số đếm thùng 587 sau); giờ bấm nút chỉ là giờ THAO TÁC. Ảnh bằng chứng: bucket riêng tư `packing-photos` (mẫu forklift-photos, signed URL 1h — ⚠️ CHƯA có job dọn ảnh, chờ user chốt thời gian giữ). Unique `uq_packing_pallet_alive` (pallet_code WHERE status<>CANCELLED) chống 2 người cùng quét mở; đóng = CAS trên status. RLS: policy SELECT authenticated (realtime board — bài học RLS-chết-câm) + publication. Module quyền MỚI `packing` (view/record/edit/cancel/export) + trang /wms/packing (board theo máy + sổ + quét). QA gói 22-packing | ✅ apply | ✅ apply 15/08 |
| `20260810_scanlog_rpc_perf.sql` | **LỊCH SỬ QUÉT CHẾT Ở QUY MÔ THẬT + LỆCH NGÀY −1** (check-app 10/08, test với 150k lượt quét/3 tháng): RPC `get_outbound_scan_log` (a) LANGUAGE sql + lọc ngày NON-SARGABLE (`AT TIME ZONE` trên cột) + `COUNT(*) OVER()` qua 10 JOIN ⇒ lọc 90 ngày **500/8,2s** (trần PostgREST), "hôm nay" 0 dòng cũng 4s; (b) **BUG NGÀY**: thiếu bước `AT TIME ZONE 'UTC'` trên cột naive-UTC (bẫy memory `naive-utc-timestamp-rpc-trap`) ⇒ MỌI lượt quét trước 14:00 VN bị xếp/lọc về NGÀY HÔM TRƯỚC (chứng minh sống: quét 09:00 sáng 12/05 → RPC cũ trả 11/05) — **production cùng lỗi ngày này**. Fix theo khuôn 28/07: plpgsql + `SET plan_cache_mode=force_custom_plan`; ngày VN → cận [from 00:00, to+1 00:00) tính 1 lần ở biến rồi so cột thô (2 nhánh OR: scanned_at naive cho quét thường · loose_confirmed_at timestamptz cho nhặt lẻ — index dùng được); COUNT tách riêng qua 7 join tối thiểu; trang chỉ join 10 bảng cho ≤ p_limit dòng; + index `idx_ose_loose_confirmed_at` partial. ⚠️ plpgsql đòi kiểu khớp TUYỆT ĐỐI: `gdo.assigned_at/started_at` naive phải `::timestamptz` (LANGUAGE sql tự ép, plpgsql nổ "structure of query does not match"). Đo sau fix cùng 150k dòng: 90 ngày 3,3s/200 · hôm nay 0,2s · total khớp oracle · ngày đúng. Gác: smoke thêm GET scan-log 90 ngày + 06-readload thêm đường 90 ngày | ✅ apply | ✅ apply 15/08 |
| `20260806d_notifications.sql` | **NÚT CHUÔNG = TRUNG TÂM THÔNG BÁO (user chốt 06/08)** — (1) `alert_events.warehouse_name` (thông báo chung phải nêu RÕ KHO — scanner resolve tên 1 lần/lượt, list/push đọc thẳng); (2) **`user_notifications`** = FEED CÁ NHÂN (việc đích danh: được giao lệnh fill… — feed là LỊCH SỬ luôn ghi, dọn lười 30 ngày; realtime + policy select cho badge chuông nhảy ngay); (3) **`notification_prefs`** (jsonb key→bool per user, THIẾU KEY = BẬT, RLS đóng — sổ khóa `PREF_KEYS` trong pushService: assign/reconcile/EXPIRY/GATE_DWELL/TRIP_LATE/WEIGH_DIFF/BE_ERRORS) — cài đặt CHỈ tắt CHUÔNG (push), danh sách vẫn đủ. BE: `notifyEmployees` (feed+push+prefs, dedupe cửa sổ 2p cho multi-select) thay sendPush trần ở 2 trigger fill; scanner push gộp per (kho, rule) + lọc prefs; API `/notify/feed|feed/read|prefs`. FE: `NotificationBell` thay chuông mock Header — 3 tab Cá nhân/Chung/Cài đặt; trang Cảnh báo thêm cột Kho + multi-select "Đã biết hàng loạt". QA gói 19 mở rộng (feed+prefs, 21 phép kiểm) + gói 20 thêm check tên kho | ✅ apply | ✅ apply 15/08 |
| `20260806c_cycle_count.sql` | **KIỂM KÊ LUÂN PHIÊN ABC (Đợt 3 roadmap 06/08)** — index `idx_stocktakelog_wh_mat_counted` (warehouse, material, counted_at DESC — StocktakeLog vài trăm nghìn dòng/năm) + RPC **`cycle_count_info(p_warehouse_id)`**: (1) lần kiểm GẦN NHẤT per mã (MAX counted_at từ StocktakeLog append-only — pallet xuất đi không mất dấu), (2) vị trí đang chứa từng mã (cap 200 id + 5 code mẫu/mã, cầu chì 10k mã). Hạng ABC KHÔNG tính lại — BE `cycleCountController.getCycleCount` gọi `slotting_stats` (MỘT nguồn công thức 80/95%) rồi ghép, chu kỳ A7/B30/C90 hardcode `CYCLE_DAYS`. Route GET `/wms/stocktake/cycle` (stocktake.view — màn chỉ đọc, nút "Kiểm N mã" chỉ prefill bộ lọc Tổng hợp KK). FE tab 4 "Luân phiên ABC" trong Kiểm kê (table-format, LOC_ID_CAP 500). QA gói 21 (oracle due_in tự tính lại) | ✅ apply | ✅ apply 15/08 |
| `20260806b_alerts.sql` | **TRUNG TÂM CẢNH BÁO (Đợt 2 roadmap 06/08)** — (1) bảng **`alert_events`**: vòng đời cảnh báo (dedup_key unique = khóa nghiệp vụ; first/last_seen; pushed_at; ack; resolved_at TỰ ĐÓNG khi điều kiện hết) — điều kiện TÍNH SỐNG ở `services/alertScanner.ts` (5 rule: EXPIRY %Date≤20/10 qua `computePctDate` · GATE_DWELL ≥90/180p · TRIP_LATE trễ ngày + bắt đầu >6h · WEIGH_DIFF >5/15% qua `gdo_weight_estimates`, bỏ qua khi có mã thiếu KL · BE_ERRORS 24h), quét lười throttle 10p/instance tại GET /wms/alerts, rule lỗi thì bỏ vòng đó (không resolve oan); cảnh báo MỚI/tái mở → Web Push gộp per kho tới người có `alerts.view` (∩ scope kho). RLS bật + policy `rls_auth_select` + realtime publication (bài học RLS-chết-câm). (2) RPC **`alerts_expiry_candidates`**: prefilter SIÊU TẬP nhóm tồn cận date (HSD tường minh/suy NSX+shelflife trong 120 ngày HOẶC mã có override NCC — không dựng lại công thức %Date trong SQL, Node quyết bằng computePctDate; cầu chì 2000 nhóm). Module quyền MỚI `alerts` (view/ack) + trang `/wms/alerts` (table-format) + menu Tổng quan. QA gói 20 | ✅ apply | ✅ apply 15/08 |
| `20260806_push_notifications.sql` | **WEB PUSH (Đợt 1 roadmap 06/08)** — 2 bảng RLS ĐÓNG HẲN (service role only, KHÔNG realtime): (1) **`push_subscriptions`** — mỗi dòng = 1 thiết bị (trình duyệt/PDA/PWA) của 1 nhân viên đã bật thông báo đẩy (endpoint unique = khóa thiết bị, đổi user trên cùng máy = chuyển chủ dòng; `failed_n` đếm lỗi gửi — 404/410 từ push service = xóa ngay, lỗi khác ≥10 lần = coi như chết); (2) **`push_config`** — 1 dòng duy nhất (PK CHECK id=1) chứa cặp khóa VAPID **TỰ SINH lần gửi đầu**, mỗi silo 1 cặp riêng, không cần khai Vercel env. ⚠️ private key KHÔNG được để `SystemSetting` (GET /wms/settings hở đọc mọi user). BE: `services/pushService.ts` (sendPushToEmployees đích danh + sendPushToPerm theo quyền∩scope kho — không bao giờ throw) + route mới `/api/notify` (vapid-key/subscribe/unsubscribe/test — auth-only, thao tác thiết bị CHÍNH MÌNH). Trigger đợt 1: giao lệnh fill cho người khác (create + PATCH dòng) → push người được giao; task Cần xử lý OPEN từ reconcile SAP → push người có `outbound.reconcile` per kho. FE: SW nạp `push-sw.js` qua workbox importScripts; card "Thông báo đẩy" THẬT trong Cài đặt (thay 5 switch mock) + nút Gửi thử; `?tab=reconcile` deep-link. QA gói 19-push-notify | ✅ apply | ✅ apply 15/08 |
| `20260805g_fill_topup.sql` | **RPC `fill_task_topup` — ĐƠN PHÁT SINH cộng dồn vào dòng lệnh fill đang treo** (user chốt 05/08: ra lệnh trùng (kho, ngày xuất, mã, date) với dòng PENDING thì CỘNG THÊM SL + số pallet vào dòng cũ thay vì bắt hủy-tạo-lại). MỘT câu UPDATE nguyên tử `qty = qty + delta` dưới row-lock (không đọc-rồi-ghi — 5 người cộng cùng lúc đều ăn đủ, QA 18 cụm 21b); điều kiện `status='PENDING'` nên KHÔNG hồi sinh dòng vừa DONE/hủy — trả NULL thì controller thử INSERT lại (unique đã nhả, tối đa 3 vòng jitter). Response `POST /fill/orders` thêm mảng `merged` (mã, date, +SL, +pallet, mã lệnh đích) — FE hiện khối "CỘNG DỒN vào lệnh đang treo". Kèm realtime: `OutboundItem`+`GroupDeliveryOrder` nối thêm key `fill-demand` (đơn phát sinh/đổi ngày → tab Đề xuất nhảy số ngay, không chờ F5) | ✅ apply | ✅ apply 15/08 |
| `20260813_packing_multi_material.sql` | **SỔ ĐÓNG GÓI: NHIỀU MÃ / 1 TRANG + ĐỐI CHIẾU SX↔KHO** (user 13/08: "1 số loại hàng có 2-3 mã — SX chung 1 chu kỳ và 1 máy → mở sổ cho phép nhiều mã"; "quét sổ = xác nhận SX đã sinh pallet, kho quét nhập = xác nhận lần 2 → tìm pallet SX tạo mà kho chưa nhận"). (1) `packing_runs.material_codes text[]` (backfill = `ARRAY[material_code]`; `material_code` giữ = mã ĐẦU cho hiển thị cũ + unique index cũ thành lưới phụ) + GIN index partial WHERE OPEN. (2) RPC **`packing_open_run(p jsonb)`** — advisory xact lock per (kho, máy) chặn 2 trang MỞ có mã GIAO NHAU (unique index cũ chỉ bắt mã đầu; đua 2 mở khác mã đầu chỉ RPC bắt được); lỗi RAISE prefix `PACKDUP:`→409 / `PACKOPEN:`→422. (3) RPC **`packing_logs_recon(...)`** — trang Sổ pallet: rows + total + đếm ĐÃ/CHƯA kho nhận CÙNG MỘT WHERE (received = EXISTS InventoryEntry cùng pallet_code — 2 chiều đều normalizeQR; loại dòng CANCELLED khỏi đếm; plpgsql + force_custom_plan). (4) `idx_inventory_pallet_code` trên `"InventoryEntry"(pallet_code)` phục vụ lookup đối chiếu (index cũ (kho,pallet) không phục vụ tra theo pallet đơn). Gói QA 22 mục [16] gác | ✅ apply | ✅ apply 15/08 |
| `20260813b_packing_recon_qty_alert.sql` | **ĐỐI CHIẾU SX↔KHO đợt 2 (user duyệt 3 đề xuất 13/08):** (1) recon v2 — `packing_logs_recon` trả thêm `received_qty` (SL kho nhập, entry đầu tiên) + `is_qty_diff` (kho đã nhận + 2 bên có số + KHÁC nhau) + `diff_count`; `p_received` nhận thêm `'DIFF'` → filter "Đã nhận nhưng LỆCH số lượng". (2) RPC **`alerts_packing_unreceived(p_hours, p_window_days)`** — gộp PER KHO pallet SX ghi sổ quá N giờ kho chưa nhận (NOT EXISTS InventoryEntry, cửa sổ 7 ngày) cho rule cảnh báo `PACKING_UNRECEIVED` (ngưỡng `PACKING_UNRECV_WARN_H`/`CRIT_H` tùy biến qua alert_thresholds, deep-link `/wms/packing?tab=log&received=NO`). (BE từng kèm cảnh báo mềm ở inbound scanQR — ĐÃ GỠ 13/08 vòng 3: rule chốt = chỉ cảnh báo ở phía SỔ) | ✅ apply | ✅ apply 15/08 |
| `20260813e_packing_perf.sql` | **2 index packing_logs từ check-app dữ liệu lớn 13/08** (seed 72.000 dòng đo thật): `idx_packing_logs_wh_scan (warehouse_id, open_scan_at DESC)` — recon/list đang SEQ SCAN 1,1s/72k, tăng tuyến tính theo năm; `idx_packing_logs_pallet (pallet_code)` — gate quét tem 1-tem-1-dòng + đối chiếu tra theo tem cũng seq scan. Kèm BE cùng commit: list/board trang sổ BỎ mảng pallets (2,2MB → 28KB — trang thật 100-150 pallet sẽ vượt trần 4,5MB Vercel), detail giữ full | ✅ apply | ✅ apply 15/08 |
| `20260813d_warehouse_machines.sql` | **Danh mục MÁY theo KHO** (user 13/08 tối: "Máy thuộc Kho — Sổ đóng gói validate máy ở đây; In tem validate theo NMSX; kho có setup thì phải chọn, không thì điền tự do"): bảng `warehouse_machines` (id, warehouse_id text, code UPPERCASE, note, is_active) + unique `(warehouse_id, upper(code))` + RLS SELECT authenticated + publication realtime. Quản trị tab "Máy" Cài đặt WMS (`wms_settings.manage_machine`); openRun/updateRun packing 422 `MACHINE_INVALID` khi máy ngoài danh mục sống. QA 22 [19] gác | ✅ apply | ✅ apply 15/08 |
| `20260813c_packing_recon_cycle.sql` | **Recon v3 — filter CHU KỲ cho tab Sổ pallet** (user 13/08 tối: "bộ filter đầy đủ"): DROP bản 10 tham số (tránh 2 overload ambiguous khi PostgREST rpc gọi theo tên) + tạo lại `packing_logs_recon` thêm `p_cycle` (JOIN `packing_runs` theo `run_id`, ILIKE partial; dòng không gắn trang bị loại khi lọc) + rows trả kèm `run_cycle`. QA 22 [18] gác hit/miss cả 2 tab | ✅ apply | ✅ apply 15/08 |
| `20260813f_employee_is_superadmin.sql` | **Superadmin theo CỘT thay so TÊN** (audit hardcode 13/08, đợt 1 — nợ kỹ thuật CLAUDE.md): `Employee.is_superadmin boolean NOT NULL DEFAULT false` + set true cho `employee_code='ADMIN' OR name='Admin'` + gác RAISE nếu 0 superadmin active. BE cùng commit: authController resolve quyền + nhét JWT từ CỘT (hết so tên ở ~18 chỗ BE + FE `isAdmin` đọc cờ trên user); pushService nhắm admin bằng `.eq('is_superadmin', true)`. Ratchet `superadmin_by_name` baseline 0 gác tái sinh. ⚠️ **PHẢI apply TRƯỚC khi deploy code main** (login select cột này — thiếu là 503) | ✅ apply | ✅ apply 15/08 (apply TRƯỚC deploy)|
| `20260813g_scanlog_pctdate_fields.sql` | **Lịch sử quét: %Date đúng nguồn** (audit 13/08 mục A2 — FE tự tính bằng `m.shelf_life_days` nên bỏ qua HSD tem V2 + shelf-life theo LÔ/NCC → số KHÁC trang Tồn): DROP + tạo lại `get_outbound_scan_log` (16 tham số) + `search_outbound_scan_log` (5 tham số) trả thêm 4 cột cuối `entry_shelf_life_days, expiry_date, ncc_id, supplier_shelf_life_overrides`; FE bỏ `calcPctAtScan` → `computePctDate` CHUNG với nowMs = thời điểm quét (cả bảng lẫn export) | ✅ apply | ✅ apply 15/08 |
| `20260814c_rotation_strategy.sql` | **NGUYÊN TẮC LUÂN CHUYỂN theo kho** (user chốt 14/08 "chiến thuật phải modify được"): `Warehouse.rotation_principle` (FEFO/FIFO/LIFO, CHECK, mặc định FEFO) + `Warehouse.rotation_required` (bool, mặc định **false** = chỉ cảnh báo như hành vi cũ ⇒ apply KHÔNG đổi cách làm việc của kho nào) · `OutboundScanEntry` +4 cột NULLABLE `rotation_principle / rotation_violation / rotation_best_date / rotation_override_reason` + index partial trên lượt vi phạm. Cột `best_available_date` CŨ giữ cho dữ liệu lịch sử nhưng **KHÔNG ghi nữa** (nghĩa cũ = MIN(NSX) chỉ đếm IN_STOCK/PARTIAL — lệch với tập pallet thật sự lấy được). Dòng cũ để `rotation_violation` NULL = "chưa đo", KHÔNG vào mẫu số báo cáo tuân thủ | ✅ apply | ✅ apply 15/08 |
| `20260814d_scanlog_rotation.sql` | **Lịch sử quét: cột + bộ lọc + số liệu tuân thủ luân chuyển**: DROP chữ ký 16 tham số rồi tạo lại `get_outbound_scan_log` **17 tham số** (thêm `p_rotation` = NULL/'BAD'/'OK') trả thêm 6 cột (`rotation_*` ×4 + `viol_count` + `measured_count`). Bộ lọc rotation áp ở DANH SÁCH, KHÔNG áp ở khối đếm — ô band "% tuân thủ" phải luôn nói về cả dải đang xem chứ không phải phần đã lọc. Đổi KIỂU TRẢ VỀ nên bắt buộc DROP trước (CREATE OR REPLACE báo "cannot change return type"); BE có fallback bỏ `p_rotation` nếu RPC chưa lên bản mới | ✅ apply | ✅ apply 15/08 (apply SAU 20260814c)|
| `20260814e_weigh_warehouses_rpc.sql` | **Kho THỰC CÓ phiếu cân = 1 lời gọi** (cùng đợt dọn N+1 14/08): RPC `weigh_ticket_warehouses()` DISTINCT + index `idx_weighticket_warehouse`. Trước đó `listWeighWarehouses` hỏi từng kho một — đo staging **153 kho hoạt động = 153 request PostgREST** mỗi lần mở bộ lọc trang Phiếu cân, qua pool ~10 khe ⇒ làm chậm CẢ APP. DISTINCT chạy trong DB nên không dính cap-1000 (số dòng về bị chặn bởi số KHO, không phải số phiếu) | ✅ apply | ✅ apply 15/08 |
| `20260815_packing_run_received.sql` | **Đối chiếu SX↔Kho ở CẤP TRANG SỔ** (user 15/08: "giao diện quản lý sổ cần thể hiện sổ nào kho đã nhập hết, sổ nào chưa — dạng symbol"): RPC `packing_runs_received(uuid[])` trả `recv_count` (pallet kho ĐÃ quét nhập — EXISTS InventoryEntry cùng pallet_code, nhập rồi xuất vẫn tính) + `diff_count` (đã nhận nhưng SL kho ≠ sổ, chỉ kết luận khi CẢ HAI bên có số); dòng CANCELLED không tính, khớp `aggRuns`. **Đếm trong SQL = 1 round-trip** — list/board cố ý KHÔNG trả mảng pallet về client (check-app 13/08 đo 2,2MB @60 pallet/trang), nên kéo ~5.000 `pallet_code` về backend rồi tra `InventoryEntry` chunk 300 sẽ là ~17 round-trip PostgREST MỖI LẦN MỞ TRANG. Không cần index mới (`idx_packing_logs_*` + `idx_inventory_pallet_code` đã phủ). QA 22 mục [22] gác | ✅ apply | ✅ apply 15/08 |
| `20260815b_warehouse_type_rename_full.sql` | **ĐỔI TÊN Loại kho phủ ĐỦ + RPC gác** (15/08): quét SỐNG 603 cột text/text[] của DB production ra **18 cột** đang mang giá trị Loại kho, trong khi `rename_warehouse_type` cascade theo bản đồ "14 chỗ" GHI TAY hồi 27/07 ⇒ bỏ sót 2 cột có dữ liệu thật — `OutboundItem.material_type` (280 dòng, = snapshot `Material.category` lúc tạo dòng đơn) và `alert_events.category` (14 dòng, = `warehouse_type` của chuyến/tồn). Sót = đổi tên xong 294 dòng trỏ vào loại KHÔNG CÒN TỒN TẠI, **không lỗi không cảnh báo**. Hàm nay cascade **17 bảng** (thêm 2 cột trên + `TmsOrder.booking_category` + `khvc_lines.booking_category` — cửa đặt lịch sinh 03/08, cũng sau bản đồ). Kèm RPC **`warehouse_type_column_coverage()`** quét lại mỗi lần gọi, trả cột nào mang Loại kho mà cascade bỏ sót (miễn trừ có lý do: `LookupValue.value` = chính danh mục · `Material.product_type` = nhãn "Loại SP" người dùng tự gõ, trùng chữ khác nghĩa · `WarehouseZone.code`/`Location.sub_code` = MÃ KHU đặt tay, staging có khu code 'PK01'="Kho Bao Bì" · bảng `x_bak_*`). Gói QA `00-invariant` mục 11 gọi RPC này ⇒ thêm cột mới mang Loại kho mà quên cascade = **ĐỎ ngay**. Round-trip verify staging: FG01→ZZTMP→FG01 khớp tuyệt đối 2.911 dòng / 13 bảng | ✅ apply 15/08 | ✅ apply 15/08 |
| `20260815c_prod_taxonomy_sap.sql` | **PRODUCTION: Loại kho tiếng Việt → MÃ SAP + gộp "Thùng"** (user chốt 15/08). Staging đổi sang mã SAP từ 27/07 qua GIAO DIỆN (không qua migration) nên production vẫn giữ tên tiếng Việt ⇒ hai môi trường lệch taxonomy, mọi đợt QA trên staging không còn phản ánh production. Ánh xạ (đo sống 13.479 dòng/18 cột): `Thành phẩm→FG01` (9.158) · `POSM→PM01` (1.258) · `Raw→RM01` (639) · `Giấy→PK01` (696) · `Thùng→GỘP vào PK01` (746, trong đó **311 mã hàng** đổi loại). **FG02 (SCA) KHÔNG tạo** — production không có kho nào dùng; danh mục còn 4 giá trị, staging 5, khác biệt ĐÚNG THỰC TẾ. Không tái dùng được `20260727_merge_..._thung_pk01.sql` (viết cho cột `category` DẠNG ĐƠN, nay đã là `categories text[]`, và `StocktakeLog.category` không còn) ⇒ viết lại theo cột MẢNG. An toàn: idempotent · gác chặn nếu chưa apply 20260815b · backup schema `bak_20260815` · gác RAISE trước khi xoá khung giờ (đòi trùng KHÍT PK01 + 0 lượt đặt — production cho 270/270 và 90/90, 0 đặt) · gác cuối gọi coverage. **Diễn tập trên staging trong transaction ROLLBACK (dựng lại tên tiếng Việt + loại "Thùng" rồi chạy thật): 9/9 XANH.** Verify production 14/14: tồn 35.828 · lệnh 7.999 · chuyến 121 · mã 1.806 nguyên vẹn; PK01 = 195+311 = 506 mã; khung giờ 1.296→1.026, slot 763→673, lượt đặt giữ nguyên 3 | ⏳ chỉ production | ✅ apply 15/08 |
| `20260815d_putaway_rules.sql` | **QUY TẮC CẤT HÀNG theo kho — đợt A** (nửa còn lại của luân chuyển 14/08: `rotation` trả lời "lấy pallet nào trước", đây trả lời "cất pallet vào ô nào"). `Warehouse` +8 cột, MẶC ĐỊNH = ĐÚNG hành vi trước 15/08 nên apply không kho nào đổi cách chạy: `putaway_priority` (CHECK CONSOLIDATE/SPREAD/ABC, mặc định CONSOLIDATE = ★ "gom cùng mã" đang có) · `putaway_required` (khai sẵn cho đợt B) · `putaway_max_materials` (NULL = không giới hạn) · `putaway_date_mix` (CHECK ANY/SAME/NEWER_ONLY/OLDER_ONLY) · 4 cờ chặn `block_pick_face` / `block_qa_hold` / `block_full` / `single_ncc`. **RPC `putaway_slot_facts(text[], text, boolean)`** gom "sự thật của ô" trong SQL — MỘT dòng/ô thay vì kéo dòng tồn về đếm (Bàu Bàng 1.517 vị trí = 15.009 dòng tồn nếu kéo về; 50 ô nặng nhất staging = 2.951 pallet). `lots` KHÔNG phải danh sách pallet mà là các NHÓM (mã, NCC, shelf-life, có-HSD-tường-minh) kèm min/max ngày: trong 1 nhóm shelf-life là hằng số nên 2 mốc đủ dựng khoảng ngày cả ô (2.951 pallet → 578 nhóm = 100KB), và **chỉ trả khi kho bật luật trộn date** (`p_with_lots`, mặc định false ⇒ đường chạy thường 0 byte). Hàm CHỦ ĐÍCH không tự tính shelf-life — luật đó ở `utils/shelfLife.ts`, chép xuống SQL là đẻ bản thứ hai. Thay luôn 2 vòng quét `InventoryEntry` cũ trong `listLocations` + `usedSlotsFor` (comment "định nghĩa khớp listLocations" trước đây chỉ là lời hứa, không ai gác). Ratchet `putaway_rule_hand_rolled` baseline 0 | ✅ apply 15/08 | ⏳ chờ merge |
| `20260815e_putaway_enforce.sql` | **QUY TẮC CẤT HÀNG — đợt B: VẾT của từng lượt cất.** `InventoryEntry` +3 cột: `putaway_checked` (bool, mẫu số của % tuân thủ) · `putaway_violation` (mã lý do vi phạm, NULL+checked = cất đúng) · `putaway_override_reason` (danh sách CỐ ĐỊNH ở `utils/putaway.ts`, không gõ tự do — để báo cáo gom nhóm nguyên nhân; biết 70% lượt vượt rào là "khu đúng đã hết chỗ" thì vấn đề là SỨC CHỨA chứ không phải người cất). Dòng cũ để `putaway_checked=false` = **chưa đo**, KHÔNG vào mẫu số (coi dòng cũ là "đạt" sẽ thổi tỷ lệ tuân thủ ngay ngày đầu — bài học 20260814c). Index PARTIAL chỉ trên phần vi phạm `(warehouse_id, import_date) WHERE putaway_violation IS NOT NULL` (báo cáo luôn hỏi "lượt nào sai"). Cột `putaway_required` (khai từ 20260815d) nay có tác dụng: **chặn ở 3 cửa GHI** `createOrder` · `setOrderLocation` · `scanQR` + 1 cửa PREVIEW `checkScanQR` (báo trước, không chặn) — lọc ở dropdown chỉ là gợi ý, gọi thẳng API vẫn cất được. Quyền mới `inbound.putaway_override` (4 nơi). Kho CHƯA bật bắt buộc = hành vi cũ (vẫn cất được) nhưng ghi vết + trả cảnh báo | ✅ apply 15/08 | ⏳ chờ merge |
| `20260815f_scan_insert_putaway.sql` | **RPC `scan_insert_pallet` nhận 3 cột vết cất hàng + RPC `rpc_source` cho bộ QA.** BUG THẬT bắt được lúc kiểm sống đợt B: RPC insert bằng **DANH SÁCH CỘT GHI TAY**, nên khoá mới thêm vào `entryObj` phía backend **rơi ÂM THẦM** — `tsc` xanh, build xanh, API trả **200**, màn quét báo "đã lưu", chỉ có 3 cột vết là không tới nơi (đo: `putaway_checked=false` trên cả 3 lượt quét test). Không exception, không cảnh báo, không dòng nào trong `error_logs`. Sửa: thêm 3 cột vào INSERT (`COALESCE(...,false)` cho boolean — đường ghi khác không gửi khoá vẫn giữ nghĩa "chưa đo"). **Gác chống tái phát (luật bug-chết-hai-lần):** RPC `rpc_source(text)` trả `pg_get_functiondef` + **bất biến QA 00 mục 12** đối chiếu SỐNG khoá của `entryObj` trong source với danh sách cột trong INSERT của RPC — thêm cột mà quên sửa RPC = ĐỎ ngay. Đã thử ngược: dựng lại RPC bản cũ ⇒ bất biến báo đúng 3 cột rơi | ✅ apply 15/08 | ⏳ chờ merge |
| `20260815g_putaway_maxmat_atomic.sql` | **Luật "tối đa N mã / vị trí" chốt DƯỚI ROW-LOCK.** ĐO THẬT 15/08: kho đặt 1 mã/ô, bắn **6 lượt quét ĐỒNG THỜI** 6 mã khác nhau vào cùng ô trống → **3 mã lọt vào**. Luật kiểm ở backend theo kiểu đọc-rồi-ghi nên nhiều lượt cùng đọc "ô đang có 0 mã" rồi cùng ghi — đúng lớp lỗi CLAUDE.md gọi tên: bộ đếm trên tài nguyên DÙNG CHUNG phải nguyên tử. Sửa: `scan_insert_pallet` thêm tham số `p_max_materials` (mặc định NULL = không ràng buộc), đếm `count(DISTINCT material_id)` **dưới cùng row-lock đã có cho sức chứa**, trả `MAXMAT\|<đang có>\|<giới hạn>` → BE map thành 422 `PUTAWAY_VIOLATION`. **KHÔNG chép bộ quy tắc xuống SQL** (tránh bản luật thứ hai) — chỉ đưa DUY NHẤT ràng buộc mang tính "đếm dưới khoá"; backend vẫn quyết định, truyền NULL khi luật tắt hoặc đã duyệt vượt rào. Mã đã có sẵn trong ô không làm tăng số mã ⇒ không chặn (khớp `putawayBlock`) | ✅ apply 15/08 | ⏳ chờ merge |
| `20260815h_material_abc_extract.sql` | **QUY TẮC CẤT HÀNG đợt C: bóc luật ABC thành MỘT NGUỒN.** Luật "mã nào hạng A/B/C" (ngưỡng luỹ kế 80%/95% theo lượt nhặt) đang nằm TRONG SQL của `slotting_stats`; chiến thuật cất hàng "Theo ABC" cần đúng luật đó — chép sang chỗ khác là đẻ bản thứ hai, tái phạm đúng thứ chiến dịch 14–15/08 vừa dọn. Nay: hàm `material_abc(text, text[], int)` trả từng mã kèm `abc`/`cum_picks`/`total_picks`, `slotting_stats` GỌI LẠI nó (giữ `stock_by_zone` riêng vì khối `placement` tách theo sub_code — đó không phải luật ABC). **ABC là hạng TƯƠNG ĐỐI trong (kho, loại hàng, cửa sổ ngày)** nên 3 tham số phải đi cùng nhau ở mọi nơi gọi. An toàn: `slotting_stats` đang chạy thật (Tối ưu vị trí + Kiểm kê luân phiên ABC) nên giữ NGUYÊN TỪNG BIỂU THỨC và nghiệm bằng **DIFF NGUYÊN KHỐI jsonb trước/sau: 18/18 tổ hợp (2 kho × 4 cửa sổ ngày × từng Loại kho) KHỚP TUYỆT ĐỐI, 0 lệch**. Kèm `utils/slottingBands.ts` (zoneAccepts/zonesOverlap/eligibleRankedZones/bandOfIndex/targetZoneCodes) dời khỏi `slottingController` để Slotting và Putaway dùng chung luật xếp band | ✅ apply 15/08 | ⏳ chờ merge |
| `20260815i_rpc_source_revoke_public.sql` + `20260815j_secdef_revoke_public.sql` | **BẢO MẬT — đóng các RPC đang mở cho ANON.** Đo thật bằng chính anon key (khoá này nằm **CÔNG KHAI trong bundle FE**) qua PostgREST: `packing_logs_recon` → **HTTP 200 + dữ liệu nghiệp vụ THẬT** (id pallet, id trang sổ, **id NGƯỜI đóng gói**, trạng thái) · `alerts_packing_unreceived` → 200 + id kho/số liệu · `packing_open_run` → 400 **"PACKOPEN:Nhập Máy"** = lỗi NGHIỆP VỤ, tức hàm ĐÃ CHẠY QUA tầng quyền ⇒ payload đầy đủ là **anon TẠO ĐƯỢC trang sổ** · `realtime_readiness` → 200 + bản đồ TOÀN BỘ bảng kèm trạng thái RLS · `rpc_source` → nguyên văn mã MỌI hàm. **Gốc rễ:** Postgres mặc định cấp EXECUTE cho **PUBLIC** trên hàm mới, `GRANT ... TO service_role` chỉ THÊM chứ không thu hồi mặc định; và **SECURITY DEFINER chạy bằng quyền CHỦ SỞ HỮU nên RLS — lá chắn của cả app — vô hiệu ngay tại đó**. Sửa: REVOKE PUBLIC/anon/authenticated trên MỌI hàm SECURITY DEFINER + 3 hàm đọc catalog, GRANT lại cho service_role. An toàn vì **frontend KHÔNG gọi `supabase.rpc(` ở bất kỳ đâu** (đã grep) — mọi thứ đi qua API app bằng service_role. Gác: RPC `secdef_public_grants()` + **bất biến QA 00 mục 13** — thêm hàm SECURITY DEFINER mà quên REVOKE = ĐỎ ngay. ⚠️ **PRODUCTION ĐANG DÍNH y hệt** (module Sổ đóng gói đã live) — cần apply gấp | ✅ apply 15/08 | ⏳ **GẤP** |
| `20260815k_putaway_bulk_move.sql` | **QUY TẮC CẤT HÀNG — đợt D: bịt cửa "Chuyển vị trí hàng loạt".** LỖ HỔNG THẬT: đợt A+B gác 4 cửa của luồng NHẬP rồi coi như xong, trong khi `PATCH /wms/inventory/bulk-location` — cửa công nhân kho dùng nhiều thứ hai — đi THẲNG xuống RPC move, **không hỏi luật một câu nào** ⇒ kho bật "bắt buộc cất đúng quy tắc" vẫn dồn được pallet vào ô đánh dấu CẤM ĐƯA HÀNG VÀO / ô nhặt lẻ / vượt số mã. Công tắc người dùng yêu cầu chỉ gác được một nửa số cửa. Hai thay đổi ở tầng SQL (luật vẫn nằm nguyên `utils/putaway.ts`, KHÔNG chép xuống đây): (1) `putaway_slot_facts` +`p_with_mats`/`mats` = **TẬP mã** đang có trong ô — cất một LÔ thì ràng buộc "tối đa N mã/ô" phải tính trên HỢP (mã đang có ∪ mã cả lô), chỉ có số ĐẾM thì không biết mã nào của lô đã có sẵn; trả theo cờ như `p_with_lots` để đường chạy thường không gánh thêm byte (ô nặng nhất staging 69 mã × 50 ô ≈ 128KB mỗi lần gõ phím ở ô tìm vị trí). (2) `move_pallets_to_location` +`p_max_materials` + 3 cột vết, đối xứng `scan_insert_pallet` (20260815g) — bộ lọc dòng KHỚP `putaway_slot_facts` (stack_layer=1, IN_STOCK/PARTIAL, còn tồn), lệch bộ lọc là backend chấm một đằng RPC chốt một nẻo. Vết ghi theo **LẦN CẤT NÀY** (pallet dời chỗ thì lý do "vì sao nó nằm đây" là của lần dời, không phải lần nhập đầu). Phải DROP bản 5 tham số: thêm tham số có DEFAULT mà giữ bản cũ = lời gọi 5 đối số NHẬP NHẰNG. Kèm `putawayBlockBatch` (gộp ràng buộc trên TẬP: số mã + một NCC; KHÔNG gộp trộn date vì đó là ràng buộc theo THỨ TỰ chồng hàng, cả lô cất cùng lượt nên người cất tự xếp được — gộp sẽ chặn oan). Ratchet `putaway_door_unreviewed` baseline 0 + QA 26 mục [25]–[30] gác | ✅ apply 15/08 | ⏳ chờ merge |
| `20260816_putaway_enforce_per_rule.sql` | **QUY TẮC CẤT HÀNG — mức xử lý theo TỪNG LUẬT** (user chốt 16/08). `putaway_required` là MỘT công tắc chung cho cả 7 luật ⇒ không diễn đạt được ý định thật: user muốn "Cấm đưa hàng vào" (ngoài đường/mặt đất) **chỉ cấm GỢI Ý + cấm LÊN KẾ HOẠCH**, hết chỗ thì vẫn để tạm — trong khi luật trộn date thì đáng **chặn cứng** (chạy ngược 60 ngày ở Ba Vì: **1.724/5.542 = 31,1% lượt cất đang chôn hàng phải lấy trước**). Bật công tắc chung là biến khu ngoài đường đang chứa 70 pallet thành khu NGOẠI LỆ: mỗi lượt để hàng phải có người cầm quyền duyệt chọn lý do, làm vài chục lần/ngày thì người ta bấm "Khu đúng đã hết chỗ" theo phản xạ ⇒ được vỏ thủ tục, mất ruột. Nay mỗi luật 3 mức: **Tắt** (khai như cũ: bỏ tick / để trống / date_mix='ANY') · **Cảnh báo** (loại khỏi gợi ý + khỏi kế hoạch Slotting, cất vẫn được + ghi vết) · **Bắt buộc** (422, cần `inbound.putaway_override` + lý do). Mức Bắt buộc khai bằng MẢNG mã luật `putaway_enforced text[]` + CHECK `<@` danh sách mã hợp lệ. **Backfill giữ nguyên hành vi**: kho đang bật công tắc chung → ép TẤT CẢ mã luật, rồi `putaway_required=false`; kèm `DO $$ RAISE EXCEPTION` nếu còn sót (backfill trượt = kho đang "bắt buộc" tự thành "chỉ cảnh báo" mà KHÔNG AI BIẾT). Cột `putaway_required` giữ lại (không đọc nữa) — `putawayRulesOf` chỉ rơi về nó khi cột mới VẮNG MẶT (DB chưa apply), còn cột CÓ mà RỖNG là ý định thật của user, không suy diễn. Ràng buộc KIỂU: bỏ hẳn trường `required` khỏi `PutawayRules` ⇒ mọi chỗ còn đọc thành **lỗi biên dịch** (bắt đúng 2 chỗ truyền `p_max_materials`). QA 26 mục [31]–[34] gác: cùng MỘT kho vừa có luật chỉ cảnh báo vừa có luật chặn cứng | ✅ apply 16/08 | ⏳ chờ merge |
| `20260817_locations_filter_noin.sql` | **Filter "Không đưa hàng vào" trên trang danh mục Vị trí kho** (user yêu cầu 17/08, sau khi dời đường khai cờ về trang này). `locations_page` + `locations_summary` thêm `p_slot_no_in boolean DEFAULT NULL` (3 trạng thái: NULL = không lọc), predicate `COALESCE(l.slot_no_in,false) = p_slot_no_in` — cột NULL coi như "chưa cấm". **Phải DROP 2 bản cũ trước khi CREATE**: thêm tham số DEFAULT bằng CREATE OR REPLACE là tạo OVERLOAD thứ hai cùng tên → PostgREST thấy 2 ứng viên cùng khớp là PGRST203 (ambiguous) cho MỌI lời gọi cũ. Cửa sổ triển khai: BE có `rpcLocationsWithNoInFallback` — PGRST202 thì bỏ tham số mới gọi lại, TRỪ khi user đang lọc bằng chính cờ đó (bỏ = trả danh sách chưa lọc âm thầm) → 503 NOT_READY | ✅ apply 17/08 | ⏳ chờ merge |
| `20260817b_slotting_stats_pickface.sql` | **`slotting_stats` trả thêm `is_pick_face` cho từng vị trí — chặn 2 tính năng GIẰNG NHAU.** Engine Slotting coi MỌI ô `slot_no_in` là "kho tạm" và xếp hàng ở đó vào diện KÉO ĐI (P1, chạy mỗi lượt), trong khi vị trí NHẶT LẺ chính là nơi lệnh **Fill hàng** chủ động hạ hàng xuống cho công nhân với tay lấy. Người dùng đánh dấu ô nhặt lẻ "không đưa hàng vào" (ý họ: cấm cất PALLET NGUYÊN) ⇒ Fill đẩy hàng xuống, Slotting lên kế hoạch bốc chính số hàng đó đi, dọn xong Fill lại báo thiếu — vòng lặp vô tận, không lỗi nào để lần ra. Đo thật staging 17/08 (Ba Vì, cấu hình user vừa đặt): 13 ô `slot_no_in`, **3 ô trong đó LÀ vị trí nhặt lẻ = 207 pallet** vào diện dọn mỗi lượt lập kế hoạch. Sửa: RPC trả `is_pick_face` + P1 chừa ô nhặt lẻ ra (ý "cấm cất pallet nguyên" đã có luật riêng `putaway_block_pick_face` phục vụ). An toàn: `slotting_stats` đang chạy thật (Tối ưu vị trí + Kiểm kê ABC) nên nghiệm bằng **DIFF NGUYÊN KHỐI jsonb trước/sau: 12/12 tổ hợp (4 kho × 3 cửa sổ ngày) KHỚP TUYỆT ĐỐI** sau khi bỏ khoá mới ra. Kèm cảnh báo tại form Sửa vị trí khi tick cả 2 cờ + QA 26 mục [37] | ✅ apply 17/08 | ⏳ chờ merge |
| `20260818_locations_filter_noout.sql` | **Filter "Không lấy hàng đi" (`slot_no_out`) — làm ĐỐI XỨNG với `p_slot_no_in` để trang Vị trí kho phủ ĐỦ cả 2 cờ, rồi mới gỡ khối "Vị trí đặc biệt" ở tab Cài đặt của Tối ưu vị trí** (đường khai THỨ HAI cho cùng 2 cờ — user chốt 18/08: thay thế xong mới gỡ). `locations_page` + `locations_summary` thêm `p_slot_no_out boolean DEFAULT NULL`, predicate `COALESCE(l.slot_no_out,false) = p_slot_no_out`. **DROP phải liệt kê CẢ chữ ký đang sống (bản 20260817, kết thúc bằng `p_slot_no_in`) LẪN chữ ký mới** — bỏ sót bản đang sống là CREATE đẻ overload thứ hai, đúng bẫy PGRST203 mà migration trước đã dặn; verify sau apply: `pg_proc` đúng **2 bản ghi** (13 + 9 tham số). BE `rpcLocationsWithNoInFallback` đổi thành `rpcLocationsWithFlagFallback`: PGRST202 thì bỏ DẦN tham số mới (out → in), chỉ bỏ tham số user KHÔNG đang lọc; đang lọc mà RPC chưa có ⇒ 503 NOT_READY. QA 26 mục [38]+[38b] gác cả nhánh mảng lẫn nhánh phân trang | ✅ apply 18/08 | ⏳ chờ merge |
| `20260819_qa_coverage_bigdata.sql` | **RPC chẩn đoán `warehouse_type_column_coverage` TIMEOUT trên dữ liệu lớn** — staging từ 18/08 giữ ~150k dòng nghiệp vụ VĨNH VIỄN (seed dữ liệu test, memory `seed-bigdata-staging`) ⇒ `count(*)` từng cột text của MỌI bảng vượt statement_timeout, gói QA 00-invariant đỏ oan 57014 (đúng lớp lỗi mà môi trường dữ liệu lớn sinh ra để bắt). Sửa 2 lớp: phép kiểm chỉ cần CÓ/KHÔNG → `EXISTS(... LIMIT theo dòng đầu)` thay đếm; hàm chẩn đoán chạy 1 lần/lượt QA → `SET statement_timeout '120s'` riêng hàm. Đo sau sửa: 10,5s / 0 cột sót, invariant 22/22 xanh lại. Thêm miễn trừ `x_seed_manifest` (bảng sổ seed, không phải nghiệp vụ) | ✅ apply 19/08 | ⏳ chờ merge |
| `20260820_stocktake_log_move_from.sql` | **Tab Lịch sử màn Chuyển vị trí quét QR:** StocktakeLog thêm 2 cột snapshot ô NGUỒN `location_from_id`/`location_from_code` (dòng cũ null — FE hiện "—") + partial index `idx_stocktakelog_moves` (WHERE location_changed_to IS NOT NULL) để tab lịch sử chuyển không quét cả bảng kiểm kê. 2 điểm ghi cùng đổ về: `bulkTransferLocation` (cờ `count_as_stocktake` — màn Chuyển vị trí, 1 lần chuyển = 1 lượt kiểm kê) + `stocktakeEntry` (kiểm kê đổi vị trí). Endpoint đọc: `GET /wms/inventory/move-log` (scope kho + loại, range-pagination) | ✅ apply 20/08 | ⏳ chờ merge |
| `20260820b_control_tower_resources.sql` | **Control Tower kiểu Manhattan Facility Console:** RPC MỚI cộng thêm `control_tower_resources(p_warehouse_ids, p_today)` — nhân sự theo khâu hôm nay (quét xuất distinct+top 5 · nhập · kiểm/chuyển) + xe nâng ACTIVE/IDLE/chưa check/lỗi + tồn sống vs bị giữ (QA/QUARANTINE) + chu trình cổng (đăng ký→vào TB, vào→ra TB). Không đụng `control_tower_stats` đang chạy (controller gọi song song, lỗi → FE ẩn khối). Warm ~80ms. Kèm REVOKE PUBLIC/anon/authenticated + GRANT service_role (bài học 20260815i) | ✅ apply 20/08 | ⏳ chờ merge |
| `20260821_warehouse_type_configs.sql` | **Loại kho theo TỪNG kho + chiến thuật xuất/nhập 2 TẦNG** (plan `docs/plans/WH_TYPE_STRATEGY_PLAN.md`). Bảng mới `warehouse_type_configs(warehouse_id text FK, type_code, 12 cột chiến thuật NULLABLE, unique (kho,loại))`: **sự tồn tại của dòng = "kho này vận hành loại này"**, cột NULL = **kế thừa mặc định kho** ⇒ sau backfill mọi cột NULL nên hành vi y hệt trước (tiêu chí số 1). Backfill suy từ khu ∪ vị trí ∪ loại của mã đang tồn; kho không dò ra loại nào → gán ĐỦ mọi loại (kho 0 loại sẽ bị Đợt 2 chặn oan) + `RAISE` nếu còn kho active trống. Đo staging: 760 dòng, Ba Vì 5 loại · Bàu Bàng 4 · Bluestar 1. Kèm **2 cột mới trên `Warehouse`** cho thang ưu tiên CẤT tường minh: `putaway_same_mat_date_pref` (Bước 2 — trong ô cùng mã ưu tiên date nào, default `NONE`) + `putaway_fallback` (Bước 3 — ô ngoài nhóm ưu tiên xếp theo gì, default `BY_CODE`); 2 default = đúng thang cứng đang chạy. **Cascade `rename_warehouse_type` thêm cột thứ 19** (`warehouse_type_configs.type_code`) — sót là đổi tên loại xong tập gán + chiến thuật riêng trỏ mã chết ÂM THẦM; verify diễn tập BEGIN…ROLLBACK 5/5 và RPC gác `warehouse_type_column_coverage` vẫn 0 khoảng trống | ✅ apply 21/08 | ⏳ chờ merge |
| `20260821b_wh_type_sort_order.sql` | **Thứ tự Loại kho theo TỪNG kho** — thêm cột `warehouse_type_configs.sort_order integer NULL`. Trước đó thứ tự chỉ có ở `LookupValue.sort_order` (danh mục DÙNG CHUNG) nên kéo FG02 lên đầu ở kho A thì MỌI kho đổi theo (user phát hiện 21/08). NULL = chưa sắp riêng → rơi về thứ tự danh mục chung; backfill = đúng thứ tự đang hiển thị (760/760 dòng) nên mở màn không xáo trộn. BE: GET/PUT type-configs sắp theo cột này; **PUT không gửi `sort_order` thì GIỮ NGUYÊN** (khác các cột chiến thuật "vắng mặt = bỏ khai riêng" — thứ tự là cách bày, đừng để client cũ xoá trắng công sắp xếp). Gói QA 29 mục [10] gác | ✅ apply 21/08 | ⏳ chờ merge |
| `20260821c_wh_type_shared_catalogue.sql` | **Loại kho = DANH MỤC CHUNG + setting riêng từng kho** (user chốt vòng cuối 21/08). Tạo loại kho một lần ⇒ **MỌI kho đều có** — bỏ hẳn khái niệm "kho vận hành loại nào": migration bù đủ cặp (kho × loại) còn thiếu (153 kho × 5 loại = 765 dòng, 151 kho đã đủ nên chỉ bù 5) + `DO 950 RAISE` nếu còn thiếu. Thêm **3 cột cờ VẬN HÀNH** vào `warehouse_type_configs`: `is_ncc_goods` · `requires_ncc` · `batch_char` (NULL = theo danh mục chung ⇒ hành vi y hệt trước) — 3 cờ này app đọc khi ĐANG ĐỨNG Ở MỘT KHO (quét tem nhập / sinh tem / upload tồn) nên tách theo kho được; còn **Tên · Màu · Bắt buộc HSD · Bắt buộc Pallet/EA giữ DÙNG CHUNG** vì chúng ràng buộc HỒ SƠ MÃ HÀNG, mà mã hàng là một dòng dùng chung toàn hệ thống (form Mã hàng không có ô Kho để mà chọn luật nào). CHECK `batch_char ~ ^[A-Z0-9]$`. Kèm: `deleteLookup` **thôi đếm** `warehouse_type_configs` (mọi kho đều có mọi loại ⇒ đếm vào là không xoá được loại nào) và **dọn cascade** dòng gán khi xoá loại; `addLookup` tự seed loại mới cho mọi kho. Gói QA 29 mục [8]/[9b]/[11] gác | ✅ apply 21/08 | ⏳ chờ merge |
| `20260821d_pallet_prints_page_warehouse.sql` | **Lịch sử in tem trả `warehouse_id` của TỪNG TEM** — `CREATE OR REPLACE pallet_prints_page` thêm `p.warehouse_id` vào CTE `t` (khác bản `20260728h` duy nhất chỗ này: WHERE/khóa gom/shape các key khác không đổi). Lý do: cờ `is_ncc_goods` từ 21/08 khai riêng theo kho, mà một LỆNH IN gộp tem của nhiều kho (màn Lịch sử in không có filter kho) ⇒ component tem không biết hỏi cờ của kho nào; mỗi tem phải MANG kho của nó. Kèm (không phải schema): FE bắt đầu **gửi** `warehouse_id` khi ghi log in — trước đó cột này LUÔN NULL nên guard scope kho ở `logPrints` (`if (l.warehouse_id && …)`) không bao giờ nổ. Tem SINH MỚI vẫn để NULL chủ đích (pallet chưa nhập kho; NMSX chỉ là nơi SX và được chọn ngoài kho mình được gán). Gói QA 29 mục [13] gác | ✅ apply 21/08 | ⏳ chờ merge |
| `20260821e_warehouse_scan_code_types.sql` | **Loại mã camera đọc, đặt THEO TỪNG KHO** — thêm `Warehouse.scan_code_types TEXT NOT NULL DEFAULT 'BOTH'` + CHECK `(QR|BARCODE|BOTH)` (user chốt 21/08: "kho nào chỉ bắt QR, kho nào chỉ bắt barcode, kho nào bắt cả 2"). Lý do: mã vạch 1D KHÔNG có mã sửa lỗi ⇒ vạch mờ đọc ra số không có thật mà vẫn thoả checksum (user báo "quét 14 ra 17") — cách chặn TẬN GỐC ở kho chỉ dùng tem QR là **đừng giải 1D ở đó**, không giải thì không thể đọc sai (và đỡ CPU/pin mỗi khung). Default `BOTH` = đúng hành vi đang chạy nên apply KHÔNG đổi kho nào (153/153 kho = BOTH sau migration). FE: tập format lọc ở `utils/scanEngine` (`nativeFormatsFor`/`zxingFormatsFor` — một nguồn), hook `useScanCodeTypes(warehouseId)` tra theo **kho của NGHIỆP VỤ** (phiếu/chuyến/trang sổ), không phải kho của người đăng nhập; tra trượt ⇒ nới về BOTH (siết thì người quét "không ăn" mà không hiểu vì sao). Prop `codeTypes` của `QRScanner`/`CartonScanSheet` **BẮT BUỘC** (màn quét mới quên = lỗi biên dịch). Gói QA 30 mục [25]–[30] gác bằng ĐỌC THẬT | ✅ apply 21/08 | ⏳ chờ merge |
| `20260821f_dashboard_cache.sql` | **Cache số liệu Dashboard** (user chốt 21/08: 5 phút, đưa vào Cài đặt › Hệ thống). Bảng `dashboard_cache(key,payload,computed_at)` + RPC `dashboard_all_cached(…, p_ttl_seconds)` bọc `dashboard_all`: còn tươi thì trả lại, hết hạn thì tính rồi lưu; `p_ttl_seconds<=0` BỎ QUA cache (cờ = 0 → hành vi cũ nguyên vẹn). Lý do: gói QA 06-readload dưới 8 luồng ghi đồng thời đo Dashboard **p50 28.285ms · max 35.954ms · trả 500** — mà đã LOẠI TRỪ query là nguyên nhân (chạy ấm chỉ 64ms; thử index giả định `(warehouse_id,material_id) INCLUDE(cartons_remaining) WHERE rem>0` trong BEGIN…ROLLBACK: cost 3811→2330 nhưng KHÔNG nhanh hơn ⇒ không tạo; gộp 2 CTE quét trùng chỉ lợi ~30ms ⇒ không đáng rủi ro đổi số trang chủ). Nút thắt = xếp hàng ở pool ~10 khe PostgREST ⇒ cách duy nhất có tác dụng là ĐỪNG chạy tổng hợp nặng trong mọi request. Cache đặt Ở DB chứ không trong RAM lambda: tải cao Vercel bung thêm instance = đúng lúc cache RAM vô dụng nhất. Khoá cache SẮP mảng trước khi ghép (2 user cùng phạm vi khác thứ tự id vẫn chung 1 dòng). Verify: ttl=0 khớp TUYỆT ĐỐI `dashboard_all`, bản cache khớp bản tính sống, 261ms→60ms | ✅ apply 21/08 | ⏳ chờ merge |
| `20260821g_inventory_band_by_unit.sql` | **Ô tổng Tồn kho TÁCH THEO ĐƠN VỊ** (user chốt 21/08). `inventory_band_totals` trả thêm `by_unit` (mảng {unit,qty} giảm dần) — TỔNG GIỮ NGUYÊN, chỉ nói rõ nó gồm gì. Lý do: ô "SL (quy đổi)" là MỘT con số cộng gộp nhiều đơn vị vật lý; đo Bàu Bàng **132.762.662** trong đó **131.209.050 là EA** (2.169 pallet mã không khai quy cách thùng) + 784.654 KG + 722.892 thùng + 27.648 SET + 18.304 M2 + 114 BAG. Công thức ĐÚNG (đã đối chiếu oracle độc lập, lệch 0,008 do làm tròn) nhưng bắt người đọc tra tooltip mới hiểu con số thì con số chưa dùng được. Thêm khoá vào jsonb là ADDITIVE (bundle FE cũ bỏ qua). ⚠️ Nhánh by_unit phải KHỚP cách phân đơn vị của `dashboard_stats`, lệch là Dashboard và Tồn kho đá nhau. Verify: Σby_unit = tổng ở cả 3 phạm vi (Bàu Bàng / Ba Vì / tất cả) | ✅ apply 21/08 | ⏳ chờ merge |
| `20260821h_warehouse_id_type_align.sql` | **DỌN LỆCH KIỂU: `InventoryEntry.warehouse_id` + `PalletOperation.warehouse_id` uuid → text + đặt FK tới `Warehouse(id)`.** `Warehouse.id` là text và **21 bảng khác đã có FK vào nó**; đúng 2 bảng này lạc kiểu ⇒ (a) không đặt được FK nên không gì chặn warehouse_id trỏ vào kho không tồn tại, (b) mọi phép nối buộc cast (`dashboard_stats` có 3 chỗ `w.id = ie.warehouse_id::text` — cast trên CỘT làm index không phục vụ được phép nối), (c) RPC mới viết theo phản xạ `w.id = ie.warehouse_id` là **42883 lúc CHẠY**, không phải lúc biên dịch. Kiểm trước khi đổi: 0 orphan cả 2 bảng · 0 view phụ thuộc · 0 hàm dùng `::uuid` trên cột. FK theo đúng khuôn `Location_warehouse_id_fkey` (ON UPDATE CASCADE ON DELETE RESTRICT — không cho xoá kho còn tồn). ⚠️ **APPLY PRODUCTION TRONG CỬA SỔ NGHỈ**: ALTER TYPE ghi lại TOÀN BẢNG + ACCESS EXCLUSIVE (staging 55.779 dòng/65MB = 6,5s). Verify sau apply: 3 cột đều text · 2 FK có · **5 index chứa cột còn đủ** · số dòng không đổi · phép nối KHÔNG cast chạy được | ✅ apply 21/08 | ⏳ chờ merge |
| `20260821i_inventory_summary_by_unit.sql` | **Nhánh "Tổng hợp" của trang Tồn kho cũng tách đơn vị** (đi kèm 20260821g). Trang Tồn kho có 2 chế độ xem dùng 2 RPC khác nhau ⇒ sửa 1 bên là CÙNG một trang lúc hiện phần tách lúc không. `inventory_summary_page` trả thêm `by_unit`, công thức KHỚP TUYỆT ĐỐI 20260821g + `dashboard_stats`. Verify: mỗi view Σby_unit = tổng CỦA CHÍNH NÓ (3/3 phạm vi); 2 view lệch nhau ≤0,014 trên 133 triệu = làm tròn numeric có sẵn từ trước (band gom theo mã, summary gom theo mã×ngày×NCC) | ✅ apply 21/08 | ⏳ chờ merge |
| `20260821j_warehouse_id_uuid_callers.sql` | **VÁ 4 hàm còn so `warehouse_id` với uuid sau 20260821h + BẤT BIẾN chặn tái sinh.** Tôi đã quét chỗ phụ thuộc trước khi đổi kiểu nhưng **quét SAI CÁCH**: chỉ tìm token `::uuid` NẰM CÙNG DÒNG với `warehouse_id` ⇒ bỏ sót hàm khai THAM SỐ kiểu uuid (`p_warehouse_ids uuid[]`, `p_wh uuid`) vì dòng chữ ký không có 2 thứ đó cạnh nhau. Hậu quả đúng như 20260821h đã tự cảnh báo — **42883 LÚC CHẠY**: gói QA đỏ ở `GET /wms/inventory/facets` (500) và gói RACE 7/14 (quick-export + manual-complete 500 `operator does not exist: text = uuid`). Vá: `inventory_facet_values` uuid[]→text[] · `pallet_ops_page` p_wh uuid→text (2 hàm này ĐỔI KIỂU THAM SỐ nên phải **DROP trước** — CREATE OR REPLACE là đẻ overload → PGRST203, bẫy đã ghi ở 20260817) · `outbound_pool_apply` bỏ `p_warehouse_id::uuid` · `scan_insert_pallet` bỏ `::uuid` khi GHI. Kèm RPC `warehouse_id_uuid_mismatch()` soi 2 chiều (dòng có cả warehouse_id lẫn uuid **+** tham số tên kho khai uuid) → **bất biến 14 của gói QA 00** ⇒ ai viết RPC mới theo phản xạ cũ là đỏ ngay, không chờ tới lúc user bấm | ✅ apply 21/08 | ⏳ chờ merge |
| `20260824_booking_sequence.sql` | **RPC `booking_sequence(p_warehouse_ids text[], p_from, p_to)` — STT chuẩn bị hàng theo booking khung giờ** (user chốt 24/08, brainstorm chọn phương án "số TÍNH SỐNG"). ROW_NUMBER partition (kho, ngày, chiều XUẤT/NHẬP) sort `(DeliverySlot.time_from, TmsVehicleSlot.created_at, id)` — kho nhìn số 1,2,3… biết xe nào soạn trước; đổi/hủy booking là số TỰ DỒN (không lưu cột, không có gì phải đánh lại). Tie-break cố ý dùng `created_at` chứ KHÔNG `updated_at` (sự kiện cổng ghi lên cùng dòng TmsVehicleSlot sẽ xáo thứ tự giữa ngày). Loại `plan_dropped` (slot vốn tự nhả, gác thêm). Trả dòng jsonb 1 lời gọi (luật pool PostgREST). Consumer: `GET /wms/outbound/booking-sequence` (view\|prepare, cắt scope kho, chặn range >190 ngày) → list Xuất kho cột "STT booking" + board Chuẩn bị hàng sort xe theo STT. Gói QA 32 gác bằng oracle tự tính | ✅ apply 24/08 | ⏳ chờ merge |
| `20260824b_loose_settings.sql` | **Setting NHẶT LẺ 2 tầng** (user chốt 24/08 — luật tự sinh nhặt lẻ hết hardcode): `Warehouse` + `warehouse_type_configs` thêm `loose_mode` (CHECK `REMAINDER\|ALL\|OFF`; NULL tầng kho = REMAINDER = hành vi cũ, NULL tầng loại = theo kho) + `loose_max_cartons` (CHECK 1–100000; trần THÙNG chỉ áp REMAINDER — ALL/OFF không so được đơn vị, user lưu ý "trần liên quan đơn vị tính"). REMAINDER = phần lẻ dưới 1 pallet (gốc 20/07) · ALL = TOÀN BỘ SL vào nhặt lẻ (dùng cho POSM CÁI/EA — soạn full trước qua luồng nhặt lẻ sẵn có) · OFF = ép 0 MỌI đường kể cả cột "Nhặt lẻ" ghi tay file upload cũ. Resolve tại `resolveLoosePolicy` (utils/putaway, cùng khuôn resolveRotation); công thức 1 chỗ `loosePalletRemainder` nhận policy Ở CHỮ KÝ — 7 call site bị ép sửa khi biên dịch. Backfill: KHÔNG (NULL = nguyên trạng, 153 kho không đổi). Gói QA 33 gác oracle | ✅ apply 24/08 | ⏳ chờ merge |
| `20260824c_loosepage_wh_types_by_item.sql` | **Trang Nhặt lẻ: filter "Loại kho" theo LOẠI CỦA MÃ ĐANG NHẶT LẺ** (bug user bắt 24/08 khi nghiệm thu setting nhặt lẻ): `loose_picking_page` + `loose_picking_facets` trước đó lọc `wt_cats(GDO.warehouse_type) && p_wh_types` — chuỗi ghép hàng xe CHỞ ('FG01+PM01') ⇒ chuyến có FG lẻ = 0 (vượt trần) chỉ còn POSM vẫn hiện dưới filter FG01. Nay page gom `array_agg(DISTINCT Material.category)` của item `loose_picking>0` per chuyến rồi `cats && p_wh_types`; facets liệt kê category của item nhặt lẻ. **Scope quyền `p_cat_scope` GIỮ NGUYÊN cấp chuyến giao ≥1** (luật 30/07 — chuyến là 1 xe vật lý), chỉ filter user chọn đổi ngữ nghĩa. QA 33 [5d][5e] gác | ✅ apply 24/08 | ⏳ chờ merge |
| `20260825_wh_type_enforced_per_rule.sql` | **Mức xử lý (Bắt buộc / Chỉ cảnh báo) của TỪNG luật cất kế thừa PER-LUẬT** (user chốt 25/08: "nếu không có giá trị gì thì để bao nhiêu thì để, nếu có thì cũng cho theo rule"). Trước đó `warehouse_type_configs.putaway_enforced` THAY THẾ NGUYÊN MẢNG của kho ⇒ loại khai 1 luật là lặng lẽ gỡ mọi luật bắt buộc còn lại — đo thật trên staging: Ba Vì bắt buộc [MAX_MATERIALS, FULL] nhưng loại PM01 khai [FULL] khiến hàng POSM THOÁT luật "tối đa 2 mã/vị trí", nhìn form không đoán ra. Thêm cột `putaway_enforced_off text[]`; hiệu lực = **(kho ∪ loại.bật) \ loại.tắt** (`mergedConfig` trong utils/putaway.ts, FE `resolveStrategy` mirror). 3 trạng thái/luật ở tầng loại — đồng bộ với các cờ tri-state khác của tầng này. KHÔNG backfill: dòng cũ giữ nguyên và nay được hiểu là "bật thêm" — CHÍNH LÀ hành vi user yêu cầu; tính năng 2 tầng chưa merge production nên chỉ staging đổi. QA 29 [5f][5h][5i] gác | ✅ apply 25/08 | ⏳ chờ merge |
| `20260826_location_max_materials.sql` | **Trần "số mã tối đa / vị trí" ĐỔI TRỤC: Kho + Loại kho → TỪNG VỊ TRÍ** (user chốt 26/08: "1 số vị trí như Ngoài đường, mặt đất hiện tại là nơi để chứa chung, nếu setup theo loại kho thì hỏng"). Trục cũ sai từ gốc vì "loại kho" là loại HÀNG: đo staging `B_TP1_NGOÀI ĐƯỜNG SCA` giữ **29 mã** và `B_TP2_MẶT ĐẤT` giữ **28 mã**, cả hai **cùng khu TP1/TP2 + cùng loại hàng thành phẩm** với các kệ 1-2 mã ngay cạnh ⇒ không tầng nào của kho tách được chúng; "chứa chung" là thuộc tính VẬT LÝ của cái ô. Thêm `Location.max_materials integer` + CHECK `1..1000`. **Ngữ nghĩa cố ý CHỈ 2 trạng thái, KHÔNG kế thừa tầng nào** (user chốt "để trống = không giới hạn, có số thì theo rule của nó, mặc định bỏ trống"): NULL = không giới hạn · N ≥ 1 = ô tối đa N mã — nhìn vào ô là biết luật của ô. KHÔNG mượn quy ước `0 = không giới hạn` của `max_pallets` vì mặc định phải là ô TRỐNG, mà trống không được mang nghĩa "0 mã = cấm sạch". Kho chỉ còn quyết định **MỨC XỬ LÝ** (`putaway_enforced` — chặn cứng hay chỉ cảnh báo). **KHÔNG backfill**: Kho Ba Vì đang khai 2 ở tầng kho sẽ MẤT giới hạn tới khi khai lại bằng nút "Số mã tối đa" khai hàng loạt trên trang Vị trí kho (đã báo trước user). Đọc TƯƠI mỗi lượt (dòng Location) nên khai xong ăn NGAY — khác cấu hình kho cache 30s. RPC `scan_insert_pallet`/bulk-move **không đổi** (BE vẫn quyết định con số rồi truyền `p_max_materials`). Gỡ khỏi `PUTAWAY_WH_COLS` + `WH_TYPE_CFG_COLS` — 2 danh sách này sinh ra KIỂU của tầng 2 nên dùng lại là **LỖI BIÊN DỊCH** (bug chết hai lần, không cần thêm cổng tĩnh). QA 26 [18b-18e] gác: 2 ô cùng kho cùng loại hàng, ô khai trần CHẶN / ô để trống QUA · câu chữ nói "vị trí này" không đổ cho kho · gỡ trần có hiệu lực ngay | ✅ apply 26/08 | ⏳ chờ merge |
| `20260826b_drop_wh_max_materials.sql` | **DỌN tàn dư trục cũ** — drop `Warehouse.putaway_max_materials` + `warehouse_type_configs.putaway_max_materials` (user 26/08: "loại bỏ hết những gì liên quan tới trần kho trước đó"). ⚠️ **CHẠY SAU KHI CODE MỚI ĐÃ LIVE** (không gộp vào file trên): code cũ vẫn `select putaway_max_materials` ⇒ drop sớm là **42703 → 500 hàng loạt ở mọi lượt quét nhập**. Đúng khuôn "migration additive trước, DROP sau khi code live" của CLAUDE.md. Giá trị cũ chụp vào `x_bak_20260826_max_materials` trước khi drop | ⏳ chờ code live | ⏳ chờ merge |
| `20260826c_pallet_truck_loadplan.sql` | **Sơ đồ xếp xe: XE PALLET vs XE THƯỜNG** (user chốt 26/08). Phân vai: `VehicleType.is_pallet_truck` (default false = hành vi cũ) = CỜ quyết định CÁCH VẼ — không đọc tên tiếng Việt dù danh mục có sẵn 'XE PALLET (16-17 PALLET)'/'XE XÁ' (ratchet `role_by_vietnamese_name`); `Vehicle.box_*_mm` (CHECK 0<x≤30000) = lòng thùng THẬT của từng biển số — chuyến gắn biển ⇒ màn 3D tra đúng 1 biển trên server (không nạp cả đội 952 xe) rồi tự điền cả loại lẫn kích thước. Validator MỘT nguồn `utils/boxDims.ts` cho cả 2 bảng (thay `Number()` trần từng nhận NaN/0; số nhầm đơn vị >30m bị 400 kèm nhắc "tính bằng MILIMÉT"). FE: `palletizeGroups` (loadPlan.ts) gom TOÀN BỘ hàng lên pallet — pallet đầy per mã + PHẦN DƯ cộng dồn THEO ĐƠN làm tròn lên = pallet gộp (không trộn dư 2 NPP); mã `is_pallet_carrier` = pallet rỗng chồng theo chiều cao; pallet mặc định **1200×1000×1650** (user chốt, áp hàng lẻ); `palletFitError` báo đỏ khi pallet không vừa xe (thử cả xoay); xe thường bỏ khối pallet + banner nêu tên. Oracle 8 ca pass + verify sống Preview (chuyến 29K06125 XE PALLET: 7 mã → 11 pallet, sàn 14 chỗ, xếp 11/11) | ✅ apply 26/08 | ⏳ chờ merge |
