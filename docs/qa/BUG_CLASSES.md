# Sổ LỚP LỖI — lưới nào gác lớp nào, lần tái phát cuối

> **Vì sao có sổ này (11/09/2026):** user — "lần nào yêu cầu test kiểm lỗi cũng ra nhiều, không có hồi kết".
> Đo 30 ngày trước đó: 45 % commit là fix, và 5 lớp lỗi chiếm đa số, **lớp nào cũng đã gặp ≥ 3 lần**.
> Kiểm app trên bề mặt đang lớn 100–160 commit/tuần thì luôn ra lỗi; điều đo được là **lỗi thuộc lớp
> ĐÃ CÓ LƯỚI phải về 0**. Mỗi lần kiểm app (skill `check-app`) BẮT BUỘC gắn nhãn từng lỗi:
>
> - **LẶP** — lớp đã có trong sổ ⇒ lưới của lớp đó HỎNG hoặc CÓ LỖ. Việc phải làm: sửa **lưới** (không chỉ sửa bug),
>   ghi ngày tái phát vào cột cuối. Lặp ≥ 2 lần cùng lớp = lưới sai tầng, phải đổi tầng (vd ratchet regex → kiểu tsc).
> - **MỚI** — chưa có lớp ⇒ thêm dòng mới + chọn tầng lưới theo bảng tầng bên dưới. Không có lưới = CHƯA fix xong
>   (luật "bug chết hai lần", CLAUDE.md).
>
> **Bảng tầng lưới (rẻ → đắt; chọn tầng RẺ NHẤT bắt được lớp đó):**
> 1. **tsc** — ràng buộc kiểu (`db` có kiểu, overload bắt tham số, union phân biệt). Bắt lúc gõ code, 0 giây.
> 2. **Test đơn vị / mirror** — `backend/tests`, `npm test`, vài giây, không cần DB. Helper thuần + "hai bản phải ra cùng số".
> 3. **Cổng tĩnh ratchet** — `scripts/qa/09-static-gate.mjs`, regex/AST trên code, không cần server.
> 4. **Ratchet độ phủ** — `coverage-surface.mjs --ratchet`: route/quyền mới phải có gói QA chạm.
> 5. **Gói QA nghiệp vụ** — `scripts/qa/NN-*.mjs` trên Preview + staging (bậc `fast` mỗi push · `full` đêm).
> 6. **Tai mắt production** — `error_logs` + digest hằng ngày.

| # | Lớp lỗi | Lưới gác (tầng) | Bằng chứng lưới đỏ trên bản lỗi | Tái phát cuối |
|---|---|---|---|---|
| C1 | **Input xấu → 500 thay 4xx** (body sai kiểu `.trim()` nổ · id rác 22P02 · ngày không có thật 22008 · thiếu field 23502 · `.single()` coerce) | (1) `validate({…})` tại route (`middlewares/validate.ts`) · (3) `write_route_without_validate` — route write mới không schema = đỏ · (5) gói 07/37/38 fuzz · `fail(res, err)` dịch mã Postgres · **15/09**: lưới ngày-không-có-thật ở `app.ts` nay soi CẢ `req.body` (trước chỉ `req.query` ⇒ mọi route POST/PUT nhận ngày trong thân đều lọt) + ratchet **`date_regex_without_calendar_check`** (baseline 25) — ratchet này `utils/dates.ts` TỰ NHẬN là có từ 30/08 mà chưa ai viết, nên file mới chép regex vẫn qua cổng. **Khuôn: lưới chặn input phải hỏi "đã phủ CẢ query lẫn body chưa?", cùng câu hỏi với "đã phủ đủ mọi ĐỘNG TỪ chưa?" (07/09)** | gói 37 bodyfuzz 31/08: 61 ca; gói 53: 12 ca · **15/09 tái phát ở CHIỀU BODY**: POST /wms/fill/auto với date "2026-13-99" → 22008 → 500, bắt được khi kiểm tính năng tự ra lệnh fill | 15/09 |
| C2 | **Luật chép tay / MỘT NHÁNH thiếu dữ kiện, bản sau in sai đơn vị** (BASE⇄thùng · số VN `1.234` · ô gộp Excel · isDay · %Date · biển số · mã lô tem). **Dạng 14/09:** endpoint trả HAI nhánh dòng hàng (điểm ghé ↔ "chưa có tồn"), nhánh phụ quên `units` ⇒ FE mặc định "mã không entry" và in số BASE dán nhãn "thùng", ô tổng cộng base thô — không lỗi nào nổ, chỉ số trên màn sai. Hỏi ở mọi endpoint trả số lượng: *"MỌI nhánh đều mang quy cách chứ?"* | (2) `tests/mirror/*.mirror.test.ts` — cùng input, BE và FE ra cùng số · (3) `mirror_helper_without_test` — helper BE ghi "mirror" mà không có test = đỏ · (3) `*_hand_rolled` ratchet | 11/09 qrParser.mirror ĐỎ trên qr.ts cũ (mã lô ngày 32: BE nhận, FE loại) — chứng minh bằng `git stash` · 14/09 gói 57 [25e] ĐỎ trên bản lỗi đang chạy Preview | **18/09** (đơn vị ĐẾM, không phải đơn vị hàng: switch Phạm vi ở Việc cần làm đếm DÒNG trong khi badge tab và ô band đếm VIỆC — màn in "Cần hạ 21" ngay trên "Tất cả 16", hai số cho một thứ, cách nhau 40 px. Một dòng gom nhiều việc cùng ô: đo Ba Vì 21 việc / 15 dòng + 1 dòng fill. Khuôn mở rộng: **hai chỗ trên CÙNG MỘT MÀN nói về cùng một tập thì phải đếm CÙNG đơn vị** — hỏi "ô này đếm cái gì?" trước khi tin số. Trước đó 17/09: Sổ pallet đọc TÊN cột `packing_logs.qty_cartons` để suy ra đơn vị — cột tên "thùng", giá trị BASE ⇒ in "6.720 thùng" cho pallet 140 thùng, sai ×48; lưới gói 22 `[7b]`; khuôn **đơn vị đọc ở chỗ GHI vào cột, không đọc ở tên cột**) · **19/09** (soi màn bằng VAI thủ kho: cột "Vị trí lấy" in "76,438th" cho pallet mà bảng Tối ưu tuyến in "76 thùng + 21 hộp"; dialog tra tồn in "16.345,667 thùng" — 0,438 thùng là 21 hộp, người kho không đọc ra. 10 chỗ cùng khuôn `qtyEntryText(...)` đứng cạnh nhãn đơn vị của MỘT mã. Lưới mới tầng 3: ratchet `entry_decimal_beside_unit_label` baseline 0 — ĐỎ 10 trên bản cũ qua `git stash`) · **20/09 TÁI PHÁT (ca đêm mô phỏng)**: Sổ đóng gói — cột + band "SL (quy đổi)" in thẳng `qty_total` (Σ BASE) ⇒ pallet 190 thùng × 24 hộp hiện **4.560**; detail 1 mã dùng `qtyLabel` thì đúng, list/band/export không. Vá: BE `aggRuns` trả thêm `qty_conv` = Σ `qtyEntryDecimal` per mã, FE in `qty_conv`; lưới gói 22 **[9b]** (qty_conv = qty_total ÷ quy cách mã). Khuôn: nhãn "quy đổi" mà cộng cột base là cùng bệnh "đơn vị đọc ở TÊN" — soi mọi chỗ dán `QTY_CONVERTED_LABEL` |
| C3 | **Phạm vi kho lệch giữa ĐỌC và GHI** (controller không đọc `warehouse_ids`; cửa đọc hở, cửa ghi gác hoặc ngược lại) | (5) gói 41 idor-scope + 51/53 · (4) ratchet độ phủ (route mới không gói nào chạm = đỏ) | gói 53: sổ Dồn/Tách đọc trọn kho khác | 07/09 |
| C4 | **UI: hook sau return sớm · thiếu key invalidate · tràn ngang mobile · nút không có** | (3) `hook_after_early_return` (0) · `mutation_hook_without_button` · `filterbar_without_sheetbutton` · (5) Playwright 3 khổ trong check-app | 10/09 trang chuyến trắng | 10/09 |
| C5 | **Tính năng ra máy thiếu công tắc / trả 200 im lặng** (đợt 1c không có ô `work_mode`; sửa DO SAP không nói "chờ duyệt") | (3) `mutation_hook_without_button` · **bảng công tắc** trong plan (skill `brainstorm-plan`) · (5) gói QA theo bề mặt | 10/09 | 10/09 |
| C6 | **Sai tên bảng/cột · INSERT thiếu id/updated_at (23502) · RPC sai tên** | (1) client `db = createClient<Database>` (`npm run db:types` sau migration) · (3) `untyped_supabase_import_files` (file mới phải dùng `db`) · `db_types_unknown_table` | 11/09: kiểu sinh lộ 8 bảng code gọi mà staging không có (xem ghi chú) | — |
| C7 | **Cắt âm thầm trần 1000 / URL quá dài** | (6) `CAP_TRUNCATED` runtime · (3) `unpaginated_in_query` (144) · `fetchAllRowsParallel`/chunk 300 | 03/08 badge DO | 03/08 |
| C8 | **Đua ghi trên bộ đếm dùng chung** (khoá đúng mà đếm sai tập · CAS không jitter) | (5) gói 02/17/27/46/56 race · RPC row-lock | 09/09 QA 56 [6a] 5 xe 1 suất → 2 lọt | 09/09 |
| C9 | **Cache "kỳ đã qua" không làm mới khi dữ liệu về muộn** | (5) gói 55 [4k] oracle hai đường | 09/09 | 09/09 |
| C10 | **Hai tên cho một trạng thái** (`DONE` vs `COMPLETED`) | (5) gói 50 [29b][29c] danh sách đóng · CHECK ở DB · **chưa có tầng 1** (cột là text, kiểu sinh không thành enum — đổi cột sang enum Postgres mới lên tầng tsc) | 07/09 3.173 lệnh | 07/09 |
| C12 | **Bước CI xanh máy dev, đỏ runner** (Windows/Node 24/npm 11 ≠ Ubuntu/Node 22/npm 10: npm-audit 03/09 · vitest 11/09 vì supabase-js đòi WebSocket native trên Node 20) — mỗi lần là một chuỗi email đỏ | quy trình: bước CI mới chạy thử dưới đúng Node runner TRƯỚC khi push: `npx -y -p node@22 -- node …` + `env -u SUPABASE_URL …` mô phỏng không `.env`; sau push đọc kết luận qua API công khai (`/actions/runs?branch=dev` → `/jobs` → `/check-runs/{id}/annotations`) | 11/09: tái hiện đúng lỗi bằng `npx -p node@20` | 11/09 |
| C11 | **Phép kiểm QA khoá chính lỗi lại** (kiểm khớp hằng số cùng hiểu nhầm với code) | quy trình: phép kiểm mới phải ĐỎ trên bản lỗi (`git stash`) trước khi tin · oracle hai đường | 06/09 gói 22 [17] | 06/09 |
| C13 | **Một CỘT hai NGHĨA, mỗi cửa hiểu một kiểu** — `qa_status_id` có mã `OK` = ĐÃ DUYỆT, nhưng cửa quét hiểu đúng còn 5 chỗ chỉ-đường + 3 RPC coi "có giá trị = đang giữ" ⇒ quét thì xuất được mà kế hoạch bảo "hết hàng". **LẶP 14/09 ở FE**: 5 màn tra tồn viết `!!e.qa_status` ⇒ pallet đã duyệt OK cũng đeo nhãn tím "QA giữ" — ratchet cũ chỉ bắt `qa_status_id` phía BE nên mù với FE ⇒ sửa LƯỚI | (3) ratchet `qa_hold_rule_hand_rolled` baseline 0 — 14/09 bắt thêm dạng FE `!!x.qa_status` / `qa_status !== null`, miễn `utils/qaHold.ts` (bản FE của luật, `isQaHeld`) + (2) `tests/unit/qaHold.test.ts` + (1) `isPickEligible` BẮT BUỘC tham số bộ id giữ | 13/09 diễn tập: 8.760 pallet "kẹt" (thật 5) · 83 mã không chốt nổi %Date · 14/09 ratchet mở rộng ĐỎ 5 trên bản cũ | 14/09 |
| C14 | **State dẫn xuất không đặt lại khi nguồn đổi** — `useColumnResize` giữ mảng độ rộng của TAB CŨ, `<colgroup>` thiếu `<col>`, `table-fixed` cho cột cuối rộng 0 ⇒ **nút thao tác văng ra ngoài màn, cuộn hết cỡ không tới** | (1)+(khuôn) hook tự nạp lại khi `storageKey`/số cột đổi — bảo đảm `widths.length === defaults.length` cho MỌI khoá động về sau | 13/09 Việc cần làm tab Cần hạ ở 1280 px | 13/09 |
| C16 | **Máy chấm người theo chi tiết máy tự chọn TUỲ Ý** — kế hoạch ghim một tem pallet chỉ để giữ chỗ mềm, nhưng bảng in tem như mệnh lệnh và cửa quét chấm "lấy pallet khác" cho mọi pallet không phải cái ghim, dù cùng ô cùng date (Ba Vì: 14/17 việc có pallet tương đương). Chỉ số tuân thủ trừ điểm người làm đúng | (5) gói 57 [22c–22d] "quét pallet tương đương ⇒ DONE, không SKIPPED" + [22f] chiều ngược "khác date ⇒ vẫn OTHER_PALLET" · (khuôn) `sameKey()` một định nghĩa tương đương cho cả cửa quét lẫn RPC bảng · UI: lệnh chỉ nói "N pallet ở ô X", chi tiết sau kính lúp | 14/09 user: "43 pallet chung một date thì pallet nào cũng được mà?" | 14/09 |
| C17 | **Nút "làm lại" không làm lại vì trạng thái cũ ăn hết nhu cầu** — bộ sinh việc idempotent (nhu cầu = đặt − đã quét − việc treo) là ĐÚNG cho lượt gọi tự động, nhưng nút "Sắp lại kế hoạch" bấm tay cũng gọi thẳng nó ⇒ luôn "0 việc mới", ghim cũ (lập bằng bộ lọc QA lỗi 12/09, hoặc trước khi hàng date ngắn hơn về) sống mãi sau khi luật/tồn đã đổi. Cùng họ: `resetUntouchedTasksOfItems` đã phải sinh ra cho ca đổi %Date (10/09) mà nút Sắp lại không đi qua. Kèm hai điểm mù cùng đợt: dòng THIẾU MỘT PHẦN không báo (chỉ báo khi pool rỗng) · màn chốt so TỪNG DÒNG với TOÀN kho nên 5 đơn cùng ngày đều thấy "đủ" | (5) gói 57 [6] (tồn không đổi ⇒ cùng bộ pallet, việc cũ CANCELLED/REPLANNED) + [23a] (tồn đổi ⇒ ghim theo FEFO mới) + [23a2] (việc đã hạ SỐNG qua sắp lại) + [23b] (thiếu một phần có cảnh báo) + [23c/23c3] (tranh chấp đo theo NGÀY XUẤT) · (khuôn) hàm bấm-tay = "bỏ việc chưa ai đụng" + hàm idempotent, đừng đưa hàm idempotent thẳng lên nút · **LẶP 16/09 ở Fill hàng**: dòng máy đặt lô 66 % (đơn chốt ≥ 60 %) vẫn treo sau khi người sửa thành ≥ 80 %, "đang có lệnh" che hết nhu cầu ⇒ máy không đặt lô đúng, xe nâng đi hạ pallet đơn không lấy được — lưới: `withLotCheck` bỏ dòng máy mang NSX ngoài tập đạt mức khỏi "đang có lệnh" (`stale_dates`), bộ đối chiếu thu hồi ngay trong lượt với lý do máy `MACHINE_LOT_REASON` (không bị hiểu thành "người bác"); gói 18 **[22d2][22d3]** · **LẶP 16/09 ở Slotting — màn GIAO VIỆC đếm bằng cột ĐÓNG BĂNG**: Hộp việc lấy `SlottingPlan.n_lines` (số dòng lúc tạo kế hoạch) nên giục "54 dòng chuyển pallet đang mở", trong khi chính trang kế hoạch suy sống từ vị trí pallet và chỉ còn **2 dòng làm được** (103/106 pallet đã xuất hết hoặc không còn bản ghi; kế hoạch thứ hai: 3 → 1). Hai màn cùng app nói hai số, số sai nằm ở chỗ giao việc ⇒ người đi làm đi tìm 52 dòng hàng không còn. Lưới: migration `20260916b` — nhánh SLOTTING của `work_inbox` đếm dòng còn ít nhất một pallet SỐNG chưa ở đích (đúng định nghĩa PENDING/PARTIAL của `deriveLineStatuses`), hết dòng thì không hiện nữa | 14/09 user: "tại sao không đòi mốc lại chỉ định 80 % khi date ngắn hơn vẫn còn?" · 16/09 user: "nếu tôi chỉnh 60 % thành 80 % thì điều gì xảy ra" — chạy thật: dòng lô 27/06 đứng nguyên · 16/09 user: "xem lại phần Sắp xếp tối ưu vị trí đã ổn chưa?" — đo: 54 vs 2 | 16/09 |
| C18 | **Cổng CI kêu oan vì chính bộ kiểm** — staging là sân chung: CI chạy sau MỖI push, người phát triển chạy gói QA hàng chục phút ⇒ bất biến toàn-DB thấy fixture của gói khác (kho `QA57_*` thiếu cấu hình loại vì ghi thẳng PostgREST, chuyến/DO mồ côi lúc dọn dở) và đỏ. Đo 30 lượt: **5 lượt đỏ 13–14/09, 0 lượt là lỗi sản phẩm** — mỗi lượt một email, user hết tin vào cổng. Kèm điểm mù: gói NGÃ trước `finish()` thì không `::error` nào được in | (5) bất biến chỉ đếm dữ liệu NGHIỆP VỤ (bỏ mã `QA*`), lưới cho đường thật chuyển sang gói 49 `[46b]` · `finish()` hạ `::notice` khi MỌI phép hỏng đều nêu mã mang dấu bộ kiểm (`looksFixture`, cố ý không khớp "QA giữ"/`qa_status`) · phép kiểm mồ côi BẮT BUỘC nêu mã trong `detail` · `lib.mjs` bắt `uncaughtException`/`unhandledRejection` in `::error` kèm câu lỗi | 14/09: dựng kho `QA00PROBE` thiếu cấu hình ⇒ 31/31 PASS "bỏ qua 1 kho fixture"; đổi mã thành `ZZ00PROBE` ⇒ ĐỎ 765/770 đúng dòng | 15/09 |
| C21 | **Hai đường cùng làm MỘT việc mà mù với nhau — tính năng NGỦ ĐÔNG che lỗi tích hợp** — hạ hàng xuống vị trí nhặt lẻ có hai đường: lệnh `FillTask` (trang Fill, 05/08) và việc `LOOSE_FEED` bộ lập kế hoạch đặt lúc Bắt đầu chuyến (10/09). `fill_demand` chỉ đếm `FillTask`; `planGdoTasks` không đọc `FillTask` ⇒ **cùng một nhu cầu bị hạ hai lần**. Không ai thấy suốt 6 tuần **CHỈ VÌ đường thứ nhất có 0 lượt dùng** — bug nằm im chờ ngày tính năng được bật. Cùng khuôn: một cửa mới chạm sổ mà cửa cũ đang ngủ. Câu hỏi bắt buộc trước khi BẬT hay tự-động-hoá một đường: *"đường nào khác cũng ghi vào sổ này, và hai bên có trừ của nhau không?"* — và đừng lấy "chưa có sự cố" làm bằng chứng đúng khi đếm lượt dùng bằng 0 | (5) gói 18 **[22e]** (việc `LOOSE_FEED` treo phải trừ vào "đang có lệnh" của Fill) + **[22d]** (chạy lại không đẻ lệnh đôi) · (khuôn) trước khi bật/tự-động-hoá: đếm LƯỢT DÙNG THẬT của đường cũ; 0 lượt = chưa từng kiểm tích hợp, không phải "chạy ổn" | 15/09: [22e] ĐỎ trên bản chưa vá ("thiếu 100→100, kỳ vọng 70") | 15/09 |
| C20 | **Vá luật vào MỘT cửa trong khi còn cửa thứ hai cùng chức năng** — luật "job đỏ phải tự khai hỏng ở đâu" (10/09) và bản vá fixture (14/09) đều nằm trong `finish()`, nhưng **9/59 gói QA tự đếm rồi đặt `process.exitCode`** nên không hưởng gì: gói 07 đỏ 2 lượt liền 14–15/09, chú thích công khai chỉ có "xem log" (log đòi đăng nhập), chạy tay ngay sau đó 94/94 PASS. Cùng khuôn "hai cửa cùng một sổ mà khác luật". **LẶP 18/09 vòng 3 — ba lần trong một ngày**: (a) sổ Chuyển vị trí dựng 17/09 với lời hứa "MỌI cửa đổi ô của pallet gọi CHUNG `logPalletMoves`" nhưng chỉ nối **3/5 cửa**, bỏ đúng hai lần đổi ô THƯỜNG XUYÊN NHẤT trong ca (quét lệnh fill hạ xuống ô nhặt lẻ · đặt phần dư khi quét xuất); (b) vá xong lại lộ **cửa thứ SÁU** (dồn pallet — ghi thẳng `location_id` nên ratchet mới, vốn chỉ soi lời gọi RPC, không nhìn thấy); (c) cùng ngày lặp ở FIXTURE: cờ vai trò `is_forklift_driver` đổi điều kiện của MỌI gói bịa ra vai lái xe nâng, nhưng chỉ gói 57 được vá ⇒ gói 46 đỏ 9/15 với thông báo lạc đề ("Chuyến chưa Bắt đầu"). **Khuôn rút ra: lưới phải hỏi theo HÀNH VI, đừng hỏi theo CÁCH VIẾT** — câu đúng là "cửa này có ĐỔI Ô pallet không", không phải "cửa này có gọi RPC không"; và vá một lớp thì phải QUÉT HẾT thể hiện của nó, kể cả trong bộ kiểm | (3) ratchet `move_without_ledger` baseline 0 — bắt CẢ lời gọi RPC LẪN ghi thẳng `location_id` · ratchet `qa_pack_without_annotated_finish` baseline 6 — gói MỚI phải kết thúc bằng `finish()` hoặc `tally()` · `tally()` mang đủ annotation + retry + nhận diện fixture cho gói tự đếm · (khuôn) vá luật vào một hàm kết thúc ⇒ hỏi ngay "gói/đường nào KHÔNG đi qua hàm này?" | 15/09: probe `tally` 3 chiều (đỏ ⇒ ::error đúng tên · xanh ⇒ im · chỉ-fixture ⇒ ::notice + exit 0); chạy thật gói 07 thấy đúng ca lần-đầu-hỏng-lần-hai-sạch; 18/09 `move_without_ledger` ĐỎ 2 trên bản 5-cửa rồi ĐỎ 1 trên bản 6-cửa, xanh sau vá · gói 18 [9f] xác nhận vết ghi đúng ô nguồn + có tên người · gói 46 từ 6/15 lên 15/15 sau khi sửa fixture | 18/09 |
| C19 | **Luật nghiệp vụ MỚI chỉ gắn vào ĐƯỜNG MỚI — cửa CHỈ ĐƯỜNG cũ vẫn trả lời theo luật cũ** — mức %Date chốt tay (10–11/09) được bộ sinh việc đọc qua `matchesRule`, nhưng gợi ý "Vị trí lấy" (có từ trước) và lộ trình nhặt lẻ (14/09, dựng TRÊN chính gợi ý đó) đi FEFO thuần ⇒ **cùng một dòng hàng: bảng "Việc cần làm" chỉ ô đạt mức, bảng "Theo vị trí" chỉ ô date 9 %**. Không lỗi nào nổ vì cửa quét chỉ soi `date_required` của VL06O ⇒ hàng dưới mức đã cam kết với khách đi ra khỏi kho không dấu vết. Câu hỏi bắt buộc khi thêm một luật nghiệp vụ: *"cửa CHỈ ĐƯỜNG nào đang trả lời cùng câu hỏi này mà chưa biết luật mới?"* — đường chỉ đường im lặng nên không ai phát hiện bằng cách dùng | (5) gói 57 **[25f1–25f3]** (lộ trình và bảng việc phải chỉ CÙNG một ô · %Date in ra là của pallet ở ô đó) + **[25g]** (không pallet nào đạt ⇒ không chỉ bừa, nêu đúng lý do "còn hàng nhưng không đạt mức") · (khuôn) `palletMeetsDateRule` = cửa DÙNG CHUNG với bộ sinh việc; gợi ý khoá theo **DÒNG ĐƠN** chứ không theo MÃ (mức date thuộc về dòng — hai dòng cùng mã có thể đòi hai mức) | 14/09: 4 phép ĐỎ đúng dòng trên bản chưa vá đang chạy Preview (lộ trình→`KA_01_T1` date 3 %, việc→`KB_02_T3`, `reason=undefined`) · **15/09 TÁI PHÁT 2 ca cùng khuôn**: (a) mô hình **VỊ TRÍ NHẶT LẺ** sống ở bộ sinh việc + Fill hàng mà hai cửa chỉ đường không biết ⇒ đo Ba Vì **13/13 điểm ghé đều trên kệ (5 điểm tầng 4)** trong khi kho lẻ có đúng mã đó; (b) cửa gác LUÂN CHUYỂN so "pallet tốt nhất" trên TOÀN tồn, chỉ biết `date_required` của VL06O ⇒ dòng chốt tay "≥ 80 %" ở kho bắt buộc bị 422 vì một pallet 2,7 % mà chính dòng đó KHÔNG được lấy — cửa gác chỉ vào thứ không lấy được. Lưới thêm: gói 57 **[25i–25o]** (cảnh báo ↔ chặn ↔ kho chưa khai ô lẻ giữ nguyên hành vi · **[25o] bộ sinh việc phải ĐẶT đúng việc fill mà lộ trình đang đòi**) · `services/loosePickFace.ts` = một chỗ giữ luật + câu chữ; `rotationCheckOf` nhận `DateRule` thay vì `minPctDate`. ĐỎ 6/6 trên Preview chưa vá (`need_fill=KHÔNG CÓ`, `422 ROTATION_REASON_REQUIRED … pallet QA57-ROT_LOW`); (c) **cùng ngày, cửa thứ BA**: Fill hàng đếm SỐ LƯỢNG ở kho lẻ nên báo "thiếu 0" và KHÔNG ra được lệnh fill trong khi lộ trình đang đòi fill — user bắt bằng câu "kiểm tra lệnh fill hàng thì lại k có nhỉ?"; vá xong lại lộ nấc thứ hai của cùng lớp: đọc luật mới nhưng đọc THIẾU (lấy FEFO trần thay vì mức %Date của dòng) ⇒ đòi hạ lô mà chính đơn không được phép lấy. Lưới: gói 18 **[1c] viết lại theo luật mới (bản cũ đang KHOÁ luật cũ lại) + [1d] + [1e]**. **Bài học thêm vào khuôn:** sửa một cửa chỉ đường thì phải đi hết DÂY — cửa kế tiếp mà người dùng bấm sang (nút "Fill hàng ›" tôi vừa gắn) là nơi lỗi lộ ra ngay | 15/09 · **20/09 TÁI PHÁT lần 2 (ca đêm mô phỏng, chuyến BHX chốt ≥ 80 % theo kênh)**: CỬA QUÉT — cửa duy nhất trừ tồn — vẫn chỉ soi `date_required` của VL06O (trống 100 %), thủ kho soạn hàng lẻ từ pallet **78 %** → 200 + xác nhận + trừ tồn, `rotation_violation=false` vì kiểm luân chuyển chỉ hỏi "có lột thứ tự pallet tốt nhất không" (pallet KHÔNG ĐẠT sớm hơn nên "đúng thứ tự"); comment :5444 khẳng định sai rằng bước %Date đã chặn. Vá: `dateRuleBelowError` gọi `palletMeetsDateRule` ở CẢ check-scan lẫn scan → 400 `DATE_RULE_BELOW` nêu số đo + mức + nguồn mức; lưới gói 57 **[25p1][25p2]** (xem trước + ghi, oracle đọc lại tồn). Khuôn bổ sung: vá CHỈ ĐƯỜNG rồi thì hỏi ngay *"cửa TRỪ TỒN đã đọc luật này chưa?"* |
| C15 | **Đi ra được mà không về được** — trang giao việc link sang trang chuyên môn, mũi tên quay lại của trang đích viết cứng về DANH SÁCH module đó, trang danh sách không có nút nào; PDA dạng ứng dụng cài đặt không có back trình duyệt ⇒ về chỗ vừa đứng mất 3 nhát qua menu | (khuôn) `lib/returnTo.ts`: link rời trang gọi `setReturnTo` (mẫu `anchorDirected`), Shell hiện thanh "‹ Về …" ở MỌI trang đích, mũi tên trang chi tiết dùng `backTarget(mặc_định)`, menu gọi `clearReturnTo`. Chưa có tầng máy — trang giao việc MỚI phải nhớ neo | 14/09 user bắt: "chưa có nút back để quay lại nhỉ?" | 14/09 |
| C26 | **NHÓM CẤU HÌNH MỚI NỐI VÀO CUỐI FORM THAY VÌ VÀO KHU VỰC CỦA NÓ** — form Kho ở Cài đặt WMS xếp nhóm theo khu XUẤT rồi NHẬP, nhưng "XUẤT — Quy định date theo khách hàng" (thêm 11/09) chui xuống dưới cả khu NHẬP, và hai ô chỉ tầng kho (xe hạ riêng · nhặt dọc đường) đứng thành hộp rời ngoài nhóm "Chỉ dẫn công việc" mà chúng phụ thuộc. User 16/09: *"đưa các hạng mục setting giống nhau về 1 khu vực chứ — rule phải là theo khu vực kể cả khi xoá, thêm mới"*. Không lỗi nào nổ, tsc/build/QA đều xanh — chỉ người đọc form mới thấy tiền tố "XUẤT —" xuất hiện hai lần cách nhau một khu. Khuôn: **thêm một nhóm cấu hình là trả lời "nó thuộc KHU nào" trước, rồi đặt vào lưới của khu đó — không nối vào cuối form.** Nhãn khu in từ `SettingsGroup area=` (một nguồn), không gõ tay vào `title` | (3) ratchet **`settings_area_interleaved`** baseline 0 — trong một `FormSheet`, dãy `area="XUẤT"/"NHẬP"` (kể cả `<OutboundStrategyFields>`/`<InboundStrategyFields>`) phải là các đoạn LIỀN NHAU theo thứ tự XUẤT → NHẬP; khu lặp lại hay NHẬP đứng trước XUẤT = ĐỎ | 16/09: bản WMSSettings cũ ⇒ `settings_area_interleaved: 1`; sắp lại ⇒ 0 | 16/09 |
| C27 | **HAI ĐẦU HIỂU NGƯỢC MỘT THAM SỐ RỖNG** — FilterBar coi bộ lọc nhiều-chọn `selected=[]` là "Tất cả" (chip không active, nút "Xóa tất cả" đưa mọi chip về rỗng), còn BE `parseListParam` coi `?status=` RỖNG là "KHÔNG trạng thái nào" (hợp đồng gói 07, cố ý để `?codes=` rỗng không thành "mọi mã"). Tab Lệnh fill gửi thẳng `status: f.status.join(',')` ⇒ bỏ tick "Chờ làm" hay bấm "Xóa tất cả" là **bảng TRỐNG** với câu "Chưa có lệnh fill nào khớp bộ lọc" — user 16/09 hỏi "lệnh fill tự động đã hoạt động chưa, tôi thấy Lệnh fill ko có dữ liệu?" trong khi DB có lệnh F260916-01 đang mở và API với bộ lọc mặc định trả đúng. Không lỗi nào nổ (200, `total: 0`), bộ lọc lại NHỚ theo user nên trống dai dẳng qua mọi lần mở lại. 22 chỗ khác trong `pages/` đều đã `|| undefined`/ternary — đúng một dòng thiếu. Khuôn: **mảng lọc lên query = `arr.join(',') \|\| undefined`**; mọi bộ lọc mới hỏi "rỗng ở FE nghĩa là gì, rỗng ở BE nghĩa là gì, có trùng không?" | (3) ratchet **`list_param_join_without_empty_guard`** baseline 0 — dòng `key: arr.join(',')` không guard trong `frontend/src/pages` = ĐỎ | 16/09: bản FillPicking chưa sửa ⇒ `1 > baseline 0` đúng luật; sửa ⇒ XANH | 16/09 |
| C28 | **THANH THAO TÁC CHỌN-NHIỀU CHÈN VÀO LUỒNG — bảng co lại đúng lúc đang tick** — tick dòng đầu là một `<div>` "Đã chọn n · Giao cho · Hủy" chui vào giữa band và bảng, vùng bảng mất ~36 px, dòng nhảy dưới con trỏ, tick tiếp là trúng dòng khác; bỏ tick hết thì bảng lại giãn ra. Không lỗi nào nổ, tsc/build/QA đều xanh. Đo 16/09: 4 màn cùng khuôn (chi tiết lệnh fill · Kết nối ERP · Kế hoạch VC · Khách hàng) trong khi Tồn kho/Mã hàng/Vị trí đã dùng pill nổi và Xuất kho/DO SAP đặt nút h-7 sẵn trên header. User 16/09: *"tick multi là hiện action lên, table không được resize — action lên header hoặc ở giữa như tồn kho"*. Khuôn: **thứ chỉ hiện khi CÓ CHỌN thì không được chiếm chỗ trong luồng** — `FloatingActionBar` (`components/shared`) hoặc nút cố định trên header | (3) ratchet **`bulk_bar_inline_reflows_table`** baseline 0 — khối `<tậpChọn>.size > 0 && (` mà thẻ mở đầu là `<div>` không `fixed` = ĐỎ | 16/09: bản cũ ⇒ 3 vi phạm đúng 3 file; chuyển sang pill nổi ⇒ 0 | 16/09 |
| C25 | **QUÁ TẢI bị ghi như APP HỎNG ở ĐƯỜNG NỀN — chuông kêu oan, email gửi về người dùng** — `fail()` đã dịch 57014 → 503 từ 07/09, nhưng việc NỀN (quét cảnh báo · lập kế hoạch lấy hàng · đồng bộ việc) không đi qua `fail()` mà ghi thẳng `recordServerError(..., 500, ...)` ⇒ DB bận là digest dựng cờ đỏ "lỗi BE 24h" và **gửi email báo hỏng trong khi app chạy bình thường**. Đo 15/09 (kiểm app, đúng lúc bậc full chạy): 2 dòng `ALERT_RULE_FAILED` status 500, nội dung `canceling statement due to statement timeout`. Cùng họ với C18/C20 (cổng kêu oan) và memory `soft-5xx-message-and-alarm` — LẶP ở một đường chưa được phủ. Khuôn: **vá một luật vào `fail()` thì hỏi ngay "đường nào ghi error_logs mà KHÔNG đi qua fail?"** | (3) ratchet **`background_error_500_hardcoded`** baseline 0 — service ghi 500 cứng = ĐỎ, phải dùng `recordBackgroundFailure()` (tự phân biệt quá tải ⇒ 503 + hậu tố `_OVERLOAD`, hỏng thật ⇒ 500). Miễn trừ: `utils/response.ts` (định nghĩa) và `app.ts` (lưới UNCAUGHT) | 15/09: mồi một file service gọi `recordServerError(…, 500, …)` ⇒ cổng tĩnh **mã thoát 1** kèm đúng tên luật; gỡ mồi ⇒ mã thoát 0 | 15/09 |
| C24 | **Điều kiện bật một luật hỏi theo KHO trong khi luật đó chỉ phục vụ được theo LOẠI KHO** — `hasPickFace(warehouse)` chỉ hỏi "kho có khai ô nhặt lẻ nào không", nên một kho khai ô lẻ cho FG01 là MỌI loại hàng khác cũng bị giục "fill xuống ô lẻ" — trong khi Fill hàng LỌC HẲN mã đó khỏi Đề xuất (không đích nào nhận loại ấy). Người dùng đi vào **NGÕ CỤT**: bảng "Theo vị trí" ghi "⚠ nên fill xuống ô lẻ" + nút "Fill hàng ›", bấm sang thì trang Fill im lặng hoàn toàn về mã đó. Đo Ba Vì 15/09 (kiểm app): 3/3 ô nhặt lẻ khai FG01, mã 510000306 (FG02) cần nhặt lẻ 60 thùng ⇒ lộ trình gắn `need_fill_from=B_TP3_32_T1`, Đề xuất chỉ hiện 8/9 mã. Ở kho tích "bắt buộc lấy đúng thứ tự" còn nặng hơn: cửa quét 422 mà KHÔNG có đường fill ⇒ loại hàng đó không ai lấy được. Khuôn: **điều kiện bật một luật phải hỏi ĐÚNG CHIỀU mà luật đó thi hành được** — luật thi hành theo (kho × loại) thì điều kiện cũng phải theo (kho × loại), không được hỏi theo kho rồi áp cho mọi loại | (5) gói 18 **[25a–25c]** (mã không ô lẻ nào nhận loại ⇒ không giục fill · kho tích bắt buộc cũng không chặn loại đó · mã CÓ ô nhận thì luật cũ giữ nguyên) · `pickFaceAcceptor(kho).accepts(loại)` = MỘT cửa hỏi cho cả lộ trình lẫn cửa quét, luật nhận loại giống hệt đích fill (`takePickFace`) · **LẶP 16/09 — cùng nút, lý do khác**: mã ĐÃ CÓ lệnh fill treo vẫn bị giục "nên fill xuống ô lẻ", mà Fill hàng trừ phần đang treo khỏi "thiếu" nên tab Đề xuất KHÔNG hiện mã đó (đo Ba Vì: 510000219 treo 2.280 trong F260916-01). Lưới thêm: gói 57 **[25i2][25i3]** — lộ trình trả `fill_order_code`/`fill_pending_base`, màn nói "⏳ đã có lệnh fill … chờ hạ" + băng riêng "Xem lệnh ›"; huỷ lệnh thì quay lại "chưa ai lo" | 15/09: dựng lại trên dữ liệu thật qua API (`need_fill_from` có ở mã FG02 trong khi `/fill/demand` không trả mã đó) và thấy nguyên văn trên giao diện 360 px: băng "1 mã nên fill xuống vị trí nhặt lẻ — Fill hàng ›" · 16/09: [25i2] ĐỎ trên Preview chưa vá | 16/09 |
| C23 | **Mốc "HÔM NAY" lấy từ THAM SỐ người gọi truyền vào** — chốt lười lệnh fill nhận `day` (ngày đang đối chiếu) rồi đóng mọi lệnh `target_date < day`; chạy tay cho NGÀY MAI ⇒ đóng luôn lệnh HÔM NAY và huỷ hết dòng chưa làm. Đo 15/09: **chính gói QA 18** (DAY = 21/12 để tách fixture khỏi dữ liệu thật) làm hai lệnh thật F260915-01/-02 của Ba Vì bị "Hệ thống" chốt lúc 15:17 và 15:23 — và người viết đọc nhầm thành "đường mở lệnh mới sau khi chốt hoạt động". Không lỗi nào nổ: 200 OK, `closed: 1`. Khuôn: mọi phép "quá hạn / đã qua / cũ hơn" phải so với **đồng hồ của kho** (`vnToday()` / `now() AT TIME ZONE 'Asia/Ho_Chi_Minh'`), KHÔNG so với ngày người gọi đang xem hay đang xử lý — hai thứ trùng nhau ở ca thường nên bug nằm im tới lúc ai đó chọn ngày khác | (5) gói 18 **[24a]** — mồi một lệnh ĐANG MỞ của hôm nay, chạy tay cho ngày tương lai, lệnh mồi phải còn PENDING · (khuôn) hàm chốt/dọn KHÔNG nhận tham số ngày, tự lấy hôm nay bên trong | 15/09: [24a] ĐỎ trên Preview chưa deploy (`status=DONE closed_by=Hệ thống`), xanh sau deploy | 15/09 |
| C22 | **Cổng gác chỉ đứng ở RUNNER, không đứng ở cửa máy mình** — cùng bộ kiểm rẻ (cổng tĩnh · npm test · ratchet độ phủ · audit) chạy tại máy hết **34 giây** và KHÔNG cần DB, nhưng chưa từng có git hook nào gọi nó ⇒ mỗi commit còn dở đẩy lên là một job `static` đỏ = **một email gửi về hộp thư người dùng**. Đo 15/09: 3 lượt đỏ liên tiếp (`b82c3465` · `962fc063` · `b757baf8`), **cả ba cùng một dòng** `write_route_without_validate: 259 > 258`, và cả ba đã tự xanh lại ở commit kế tiếp — tức cổng bắt ĐÚNG, chỉ bắt ở sai CHỖ. Khác hẳn C18/C20 (cổng kêu oan): ở đây không có gì để nới, chỉ có chỗ đứng để dời. Câu hỏi khuôn: *"phép kiểm này có cần DB không? Không ⇒ vì sao nó chỉ chạy sau khi đã push?"* | (0 — tầng RẺ HƠN cả tsc) `.githooks/pre-push` chạy `run-all.mjs --tier fast --offline`, đỏ thì CHẶN push · versioned trong repo, bật bằng `git config core.hooksPath .githooks` · bỏ dải chỉ `.md`/`docs/` (cổng tĩnh chỉ đọc `backend/src` · `frontend/src` · `scripts/qa` · `package*.json` nên tài liệu không đổi được kết quả) · van xả `git push --no-verify` / `QA_SKIP_PREPUSH=1`. **Bước cần DB vẫn nằm ngoài** — hook không được đụng staging, và `--tier fast` đầy đủ vẫn là việc phải nhớ chạy trước khi push commit tính năng | 15/09: thử ngược 3 chiều — thêm `router.post('/qa-probe-delete-me', …)` vào `routes/auth.ts` ⇒ **mã thoát 1** kèm đúng dòng `259 > baseline 258`; bản sạch ⇒ mã thoát 0; dải `54966b66` (chỉ `CLAUDE.md` + `BUG_CLASSES.md`) ⇒ bỏ qua cổng, mã thoát 0 | 15/09 |
| C28 | **Phép kiểm đỏ vì HẾT GIỜ, không phải vì sai** — phép kiểm mirror chạy N=3000 input ngẫu nhiên tốn 1–3 giây mỗi file, trong khi trần mặc định của vitest là **5 giây**; các file chạy song song nên lúc máy bận là một file bất kỳ vượt trần. Đo 16/09: chạy trọn bộ 3 lượt thì **1 lượt ĐỎ** ở `qtyUnits.mirror`, mà chạy riêng file đó lại XANH — dấu hiệu nhận ra: thông điệp là `Test timed out in 5000ms` chứ không phải `AssertionError`. Lên CI thì thành email báo lỗi OAN, cùng họ C18/C22 nhưng gốc nằm ở **cấu hình bộ chạy kiểm**, không phải ở dữ liệu hay chỗ đặt cổng. Bẫy đi kèm: dễ đổ tại thay đổi vừa làm (lượt này nó đỏ ngay sau khi gỡ 5 thư viện chết, trông như do gỡ mà ra) | (2) `testTimeout: 30_000` trong `backend/vitest.config.ts` — **nới trần, KHÔNG giảm N**: số input ngẫu nhiên chính là giá trị của phép kiểm, cắt N là cắt độ phủ để đổi lấy màu xanh · (khuôn) phép kiểm đỏ thì đọc THÔNG ĐIỆP trước khi đọc thay đổi của mình: "hết giờ" và "sai kết quả" là hai chuyện khác hẳn; chạy riêng file đó — xanh ⇒ chập chờn, đỏ ⇒ lỗi thật | 16/09: ép `--testTimeout=1` ⇒ ĐỎ đúng dòng "Test timed out in 1ms" (chứng minh đúng núm điều khiển); nới lên 30 s rồi chạy trọn bộ **5/5 lượt XANH** | 16/09 |

| C29 | **ÁP THAM SỐ URL MỘT LẦN — nhưng khoá theo GIÁ TRỊ tham số, nên bấm lại chính link đang mở là không có gì xảy ra** — trang Việc cần làm nhớ "đã áp `?tab=&trip=`" bằng chuỗi tham số để khỏi kéo ngược người dùng khi họ bấm tab khác (bản vá 12/09). Nhưng React Router `<Link>` trỏ tới CÙNG URL đang mở thì điều hướng bằng **replace**: URL không đổi ⇒ chuỗi tham số không đổi ⇒ effect bỏ qua ⇒ **dòng Hộp việc đó chết** cho tới khi bấm dòng khác. User 16/09: *"bấm vào Cần đưa ra, sau đó quay về hộp việc thì bấm tiếp k đc"*. Không lỗi nào nổ (không console, không 4xx) — chỉ người bấm mới thấy. Cùng lớp ở `LoosePickingDetail` (`?route=1`). Khuôn: **khoá theo `useLocation().key`** — sinh mới ở MỌI lượt điều hướng kể cả replace cùng URL, và đứng yên khi người dùng bấm tab trong trang; đừng khoá theo giá trị tham số, cũng đừng khoá theo state đang hiển thị | (3) ratchet **`url_param_applied_by_value`** baseline 0 — file FE có `useSearchParams` + ref tên `*applied*` mà KHÔNG dùng `useLocation()` = ĐỎ | 16/09: tái hiện trên Preview bằng Playwright (bấm dòng → tab Hộp việc → bấm lại dòng đó: tab đứng yên, `history.state.key` đổi mà effect không chạy); ratchet ĐỎ 2 trên bản lỗi, 0 sau vá | 16/09 |
| C30 | **LỌC Ở Ô CHỌN BỊ TƯỞNG LÀ CỬA GÁC — máy chủ không kiểm lại thứ màn hình vừa lọc** — ô "Lái xe nâng" lúc Bắt đầu chuyến lọc đúng ở FE (`job_title.includes('lái xe nâng')`), nên người dùng bình thường không bao giờ chọn sai; nhưng `validForkliftIds` chỉ gác **người còn làm việc + thuộc kho của chuyến**, không gác VAI TRÒ ⇒ mọi đường ghi KHÔNG qua màn (bộ nạp mô phỏng, script, bundle cũ) vẫn ghi được. Đo 18/09: **Admin** và **SIMDAY Bot** (cả hai KHÔNG có chức danh) đứng tên lái xe nâng của 3 chuyến ĐANG XUẤT ở Ba Vì ⇒ bảng "Cần đưa ra" của Admin hiện việc không phải của mình, khối Giám sát → "theo người" đếm sai công cả kho. Không lỗi nào nổ — **lỗ chỉ lộ qua DỮ LIỆU, không lộ qua thao tác**. Kèm lớp con cũ (C-14/08): luật vai trò chép ở FE bằng so TÊN tiếng Việt. Khuôn: *"máy chủ có gác đúng cái màn hình vừa lọc không?"* và cho **cả hai đọc CÙNG một nguồn** (ở đây route nuôi ô chọn trả thẳng cờ) | (3) ratchet **`role_by_vietnamese_name`** mở rộng bắt `.includes('lái xe nâng')` / `=== 'Lái xe nâng'` — CHỈ dạng SO SÁNH, không bắt chữ hiển thị · (5) gói 57 **[3c]** người thật không mang cờ → 400 nêu tên | 18/09: ratchet ĐỎ 1 trên bản cũ (`git stash` OutboundDetail), 0 sau vá; gói 57 [3c] chạy thật trên Preview ⇒ 400 "Trần Hoàng Lãm không phải chức danh lái xe nâng"; 186/186 | 18/09 |

| C31 | **"AI LÀM" LẤY TỪ THÂN REQUEST — đường nào không gửi thì bản ghi mất tên** — controller đọc `actor_name` / `employee_id` / `scanned_by` từ `req.body` rồi ghi thẳng vào cột audit. Form trên màn có gửi nên **thử tay không bao giờ thấy lỗi**; mọi đường khác (bundle PWA cũ, script nạp dữ liệu, tích hợp ERP, chính bộ kiểm) ghi ra dòng KHÔNG NGƯỜI. Đo: `OutboundScanEntry.scanned_by` **288/288 rỗng** (vá 17/09) · `InventoryAdjustmentLog.actor_name` rỗng ở mọi lời gọi không qua form (vá 18/09 — và điều chỉnh tồn là thao tác đổi THẲNG số tồn, chỗ cần biết "ai làm" nhất). **LẶP lần 3 (18/09, nặng hơn hai lần trước)**: `ProductionImport.updated_by` ghi `req.body.updated_by ?? null` trong khi nút Hoàn thành / Bỏ hoàn thành gọi API **KHÔNG kèm thân request** ⇒ vừa không bao giờ ghi được tên, vừa **GHI NULL ĐÈ** lên tên đã có. Đo: **0/22.986 phiếu ĐÃ HOÀN THÀNH có người** trong khi `created_by` đầy 22.983 — cột này là khoá ngoại Employee và được SELECT ra `updated_by_emp(id, name)` để hiện "Người sửa", tức vốn được thiết kế để hiện tên. Ngay trong CÙNG một patch của `setOrderLocation`, `location_history` đọc `req.user` còn `updated_by` đọc thân request — hai vết của cùng một thao tác lấy từ hai nguồn. **LẶP lần 4 — và chỉ lộ vì user HỎI "còn gì chưa fix"**: sau 3 lần vá bằng mắt vẫn còn **8 cửa** lấy người làm thẳng từ thân request (4 thao tác HÀNG LOẠT của Tồn kho · chuyển vị trí hàng loạt · kiểm kho `stocktake_by`+`updated_by` — căn cứ của tab Luân phiên ABC · dòng sổ kiểm kho lấy id theo thân request trong khi TÊN lấy theo JWT · log điều chỉnh lúc quét nhập GỘP, chỗ này còn để `actor_name` NULL CỨNG). Lỗ chỉ lộ qua DỮ LIỆU, không lộ qua thao tác. Khuôn: **thân request nói AI LÀM thì chỉ là ưu tiên, JWT mới là nguồn đáy** — client gửi thì tin client (màn cho ghi hộ người khác), không gửi thì rơi về người đang đăng nhập | (5) gói QA gọi API **không gửi** trường actor rồi đòi vết có tên: gói 10 `[ai-quét]` (xuất) · gói 47 `[12h]` (điều chỉnh tồn) — lưới cũ của 47 kiểm `actor` cho dòng MOVED nhưng bỏ qua dòng ADJUSTED, đúng chỗ lọt · gói 48 `[22b]` (hoàn thành phiếu nhập KHÔNG kèm thân request) · **(3) ratchet `actor_column_from_body` baseline 0** — cột vết gán thẳng từ trường thân request là ĐỎ; đây mới là tầng đúng, vì 4 lần trước đều vá bằng mắt rồi quên quét phần còn lại. Bằng chứng cần máy: ratchet ĐỎ **7** trên bản cũ, còn **2** sau lượt vá TAY — tức nó bắt đúng hai chỗ người vá vẫn sót · **(2) test đơn vị `tests/unit/actor.test.ts` trên MỘT nguồn `utils/actor.ts`** — sau 2 lần vá là 2 bản chép tay khác nhau, tới bản thứ 3 thì gom lại thay vì chép tiếp (đúng lớp C2 "luật chép tay") | 17/09 gói 10 ĐỎ trên bản cũ · 18/09 gói 47 [12h] ĐỎ "ai=TRỐNG" trên Preview chưa vá, xanh sau deploy; 24 lượt ghi tranh chấp đồng thời ⇒ 24/24 dòng có tên; 18/09 vòng 3: gói 48 [22b] xanh với `updated_by` là UUID người đăng nhập, test đơn vị 6/6; vòng 4 gói 47 [4b] (chuyển vị trí hàng loạt không gửi employee_id vẫn ghi được người ở CẢ pallet LẪN sổ) | 18/09 |

| C32 | **KÝ TỰ ĐIỀU KHIỂN TRONG THAM SỐ → 500 ở tầng driver** — Postgres KHÔNG lưu nổi ` ` trong `text`, nên mọi tham số mang `%00` nổ trước khi controller kịp làm gì (đo 18/09: `pallet-ledger?pallet_code=%00abc` → 500). Không phải lỗ bảo mật, nhưng đổ rác vào `error_logs` ⇒ rule "lỗi BE 24h" kêu oan — **tự làm hỏng tai mắt**, y hệt lớp injection 21/08. Bẫy đi kèm: cùng một `%00` mà đường này 500 còn đường kia trả 200 (đo: `/masterdata/materials?search=a%00b` = 200) ⇒ vá từng route là sót | (lưới CHUNG, không ratchet) middleware `/api` trong `app.ts` chặn `[ --]` ở **CẢ query LẪN body** — chừa `\t`/`\n` để ghi chú nhiều dòng vẫn qua · (5) gói 07 mục 7b (chạy MỖI PUSH) | 18/09: gói 07 ĐỎ trên Preview chưa vá (`%00` → 500 và `%00` không bị chặn ở đường khác), xanh sau deploy | 18/09 |
| C33 | **VIỆC NỀN NGÃ VÌ ĐUA DỮ LIỆU, bị ghi thành 500 "hỏng"** — bộ lập kế hoạch đang tạo việc thì dòng hàng nó trỏ tới bị XOÁ+TẠO LẠI (đường derive của Kế hoạch xuất làm thế mỗi lần dữ liệu ngoài dội xuống) ⇒ `23503 violates foreign key constraint`. Lượt sau tự đúng, app vẫn chạy — nhưng digest dựng cờ đỏ và **gửi email báo hỏng**. Cùng bệnh với C25 (quá tải bị xử như hỏng), chỉ khác nguyên nhân: ở đây dữ liệu đổi dưới chân chứ không phải DB bận | (2) `classifyBackgroundFailure` tách thành hàm THUẦN + `tests/unit/backgroundFailure.test.ts` 6 ca — gồm ca "lỗi chỉ có CÂU CHỮ, không có mã" và **2 ca gác chiều ngược** (TypeError · lỗi rỗng vẫn phải 500, cổng không được nới). Vá ở `recordBackgroundFailure` nên phủ MỌI đường nền, không riêng chỗ vừa nổ | 18/09: bỏ bản vá ⇒ 6/6 ĐỎ | 18/09 |
| C34 | **SỐ TOÀN KHO lấy từ truy vấn ĐÃ LỌC THEO NGỮ CẢNH — badge đổi nghĩa theo chỗ đang đứng** — badge tab "Cần hạ 21 / Cần đưa ra 23" và band của Việc cần làm lấy `totals` từ chính board đang xem; vào tab Sắp quét (board theo CHUYẾN) chưa chọn chuyến thì RPC trả toàn 0 ⇒ badge BIẾN MẤT và band in "Việc còn lại 0" ngay trên danh sách 7 chuyến đang chạy; chọn một chuyến thì badge nhảy 21 → 5. Luật 17/09 "số không đổi theo chỗ đang đứng" đã viết mà tab thứ tư không ai bấm thử. Khuôn: **con số nói về TOÀN KHO phải lấy từ truy vấn toàn kho, không mượn truy vấn của màn đang mở** | (6) soi bằng mắt theo vai → **(5) từ 23/09: gói QA cộng lại từ CHÍNH các dòng endpoint trả về** (mẫu 59 `[2i2]`) | 19/09 vai xe nâng 360 px · 23/09 gói 59 [2i2] ĐỎ trên bản chưa vá (`band={pallet:0,kg:0}` vs cộng tay `{0.2, 50}`) | 23/09 |
| C35 | **HAI CỘT NÓI HAI CHUYỆN — `status = EXPORTED` mà `cartons_remaining > 0`** — Dashboard đếm pallet theo số còn (9.178), Tồn kho/Sức chứa đếm theo status (9.092): hai màn cạnh nhau ra hai con số cho CÙNG kho. 129 pallet ở Ba Vì + Bàu Bàng còn nguyên 6.720 hộp mang EXPORTED; không đường ghi nào của app tạo ra (mọi RPC tự suy status từ số còn) — là rác seed 08–12/09, nhưng app **không có lưới nào phát hiện** nên hai màn lệch nhau một tuần không ai thấy | (5) gói 00 bất biến 1b "EXPORTED thì hết số" — nêu mã pallet để `finish()` phân biệt rác fixture | 19/09: ĐỎ 129 trước khi sửa dữ liệu, 0 sau | 19/09 |
| C36 | **"DỮ LIỆU THÔ NẰM TRONG raw" LÀ LỜI HỨA KHÔNG CÓ PHÉP KIỂM — cột file không khai trong bộ đọc là RƠI HẲN** — `parseSheetByHeader` chỉ dựng object từ cột ĐÃ MAP, nên `raw` của ZSD02 chỉ giữ 54/79 header suốt 2 ngày trong khi plan viết "cột không có vai thì nằm trong raw"; không lỗi, không cảnh báo, chỉ lộ khi user hỏi "không thấy SO, route". Cùng lượt: cột Batch đọc `batch` (VL06O) nên dòng ZSD02 luôn "—" dù có `batch_so`. Khuôn: hai nguồn ghi cùng một cột hiển thị mà FE chỉ đọc một; và "lưu đủ" phải đo bằng số header file ↔ số key dòng | (2) test `zsd02Parse.test.ts`: số header file mẫu (79) = `Object.keys(rows[0]).length`; (5) QA 59 [2c2] `raw._v` + ngày chuẩn hoá | 24/09: ĐỎ trên bản cũ (54 ≠ 79) | 24/09 |

| C37 | **TÍNH NĂNG RA MÁY MÀ 0 CHỨC DANH CÓ QUYỀN — cả một TRANG không nhân viên nào mở được** — `directed_work` lên máy 10/09 với 0/9 chức danh kho; `dispatch` (24/09) + `freight` (23/09) lên máy với **0/19** chức danh, 41 nhân sự đang làm việc không ai thấy mục menu. Cả hai lần chỉ lộ ra vì có người đi ĐO BẰNG TAY. Luật "thêm action = đủ 4 nơi" của CLAUDE.md phủ FE config · BE config · gate nút · route — **thiếu nơi thứ NĂM: cấp cho chức danh nào**, mà không có nơi thứ năm thì bốn nơi kia chỉ phục vụ superadmin. Gói 08 tầng 2 CÓ kêu cả hai lần, nhưng kêu bằng ⚠️ giữa một lượt chạy XANH nên không ai đọc | (5) gói 08 tầng **2b**: module mà KHÔNG action nào có người nắm ⇒ **FAIL**, không warn (action LẺ chưa ai được cấp vẫn warn — đó là quyết định quản trị); ngoại lệ khai ở `MODULE_ADMIN_ONLY` kèm lý do. Migration cấp quyền tự gác bằng `DO $ RAISE` | 24/09: gỡ tạm key `dispatch`+`freight` khỏi mọi chức danh ⇒ gói 08 ĐỎ nêu đúng 2 module (13/14), áp lại migration ⇒ 15/15 và khớp bản sao lưu tuyệt đối | 24/09 |
| C38 | **HAI CỬA CÙNG MỘT SỔ MÀ TÍNH KHÁC NHAU — POST bỏ chính mình ra khỏi Σ, PUT thì không** — `createShare` loại ĐVVT đang thêm ra khỏi tổng tỷ trọng (đúng cho SỬA, sai cho THÊM), trong khi unique key là (kho, ĐVVT, **hiệu lực TỪ**) nên dòng thứ hai cho cùng ĐVVT khác ngày là hợp lệ ⇒ phần nó đang giữ biến mất khỏi phép tính, Σ vượt 100 mà cửa trả 201, engine gặp hai mục tiêu cho một ĐVVT và lấy đại dòng đầu. `updateShare` vốn loại theo ID nên làm đúng — chênh lệch giữa hai cửa là chỗ lỗ nằm. Khuôn: **viết cửa thứ hai cho cùng một bảng thì đọc lại cửa thứ nhất, và hỏi unique key cho phép mấy dòng** | (5) gói 60 `[4c5]` thêm dòng thứ hai cho cùng ĐVVT → phải 400 | 24/09: ĐỎ trên Preview chưa vá (201 thay vì 400), 50/50 sau deploy | 24/09 |

**Ghi chú C38 (24/09) — nạn nhân thứ hai là chính BỘ KIỂM:** gói 60 khai tỷ trọng lên kho Ba Vì (kho THẬT) bằng
hai con số gõ tay 60 % + 40 %, tức giả định kho luôn trống 100 %. Hôm có người khai tỷ trọng thật (85 %) là gói
đỏ 4 phép, **0 phép là lỗi sản phẩm**. Nay gói ĐO chỗ trống rồi kiểm chính LUẬT Σ ≤ 100, và chọn ĐVVT fixture
trong số ĐVVT CHƯA có dòng thật ở kho đó. Khuôn: **gói QA ghi vào bảng CẤU HÌNH của kho thật thì oracle phải là
LUẬT, không phải con số tuyệt đối** — cùng họ với bất biến 11b đếm cả kho fixture (14/09).
| C39 | **CỔNG TĨNH KÊU OAN VÌ ĐỌC `if` MỘT DÒNG THÀNH MỞ KHỐI** — `countHookAfterEarlyReturn` bật cờ `inIf` cho MỌI dòng khớp `^ {2}ifs*(`, mà cờ đó chỉ tắt khi gặp `  }` — thứ một-dòng (`if (cond) foo()`) không bao giờ có ⇒ cờ kẹt bật tới hết hàm, rồi `return` thụt ≥4 dấu cách của một callback (useMemo/map/sort) bị đọc thành RETURN SỚM và **mọi hook sau đó đều đỏ**. Đo 24/09 ở `Dispatch.tsx`: 5 vi phạm ma, 0 cái thật. Cổng kêu oan còn tệ hơn không có cổng — nó dạy người ta viết vòng tránh cổng thay vì đọc cổng | (3) chính luật đó: chỉ theo dõi khi thân `if` nằm ở DÒNG SAU (`ifTailOf` tìm dấu đóng ngoặc rồi soi phần đuôi) | 24/09: file tạm 4 ca — bắt ĐỦ 3 ca THẬT (return một dòng · return trong khối · return ở dòng kế), bỏ qua đúng ca `if (cond) stmt()` | 24/09 |
**Ghi chú C34 (23/09) — TÁI PHÁT, chiều ngược lại:** lần 19/09 là con số TOÀN KHO mượn truy vấn của màn đang
mở; lần này là ô tổng của MỘT DANH SÁCH cộng cứng một tập con. `erp_so_lines_summary` cộng `sap_pallets`/`kg`
với `FILTER (WHERE status='OPEN')` trong khi 5 ô còn lại đi theo mệnh đề WHERE ⇒ lọc "Đã có OD" cho **bảng
1.489 dòng đứng cạnh "PALLET SAP 0 · TẤN 0"**, ngay trên cột Pallet SAP của từng dòng đang in 0,68 — và chính
trạng thái rỗng của tab mời người dùng đổi sang bộ lọc đó. Cùng một câu hỏi cho cả hai chiều: **con số này
tóm tắt TẬP NÀO, tập đó có đúng là thứ đang hiện không?** Lưới lên tầng (5): gói QA cộng lại từ chính các
dòng endpoint vừa trả (`59 [2i2]`), không so với hằng số gõ tay. Migration `20260923c`.

**Ghi chú C31 (18/09):** lớp này đã gặp **2 lần trong 2 ngày** ở hai module khác nhau — dấu hiệu lưới đang ở
TẦNG SAI (mỗi lần vá lại thêm một phép kiểm cho đúng một cửa). Tầng rẻ hơn đáng cân nhắc nếu tái phát lần
ba: một helper `actorOf(req, body)` dùng chung + ratchet bắt cột audit được gán thẳng từ biến body. Chưa làm
vì `employee_id` trong thân là hợp lệ ở nhiều chỗ (ghi hộ người khác), regex dễ báo oan.

**Ghi chú C30 (18/09) — siết cửa ghi là đổi điều kiện của FIXTURE:** gói 57 mượn một tài khoản **phạm vi
toàn quốc** làm lái xe nâng (để qua cửa kiểm kho của kho fixture vừa tạo). Đo: **18/18 lái xe nâng đều
ASSIGNED**, không ai toàn quốc ⇒ cờ mới làm ĐỎ TOÀN GÓI chứ không đỏ một phép. Vá: [0e] chọn người CÓ CỜ
rồi cấp tạm `UserWarehouseAccess` cho kho fixture, cleanup xoá lại. **Thêm một điều kiện vào cửa ghi thì
chạy ngay gói QA của module trước khi tin** — fixture cũ dựng theo luật CŨ và thường mượn bản ghi "tiện
nhất", không phải "đúng vai".

**Ghi chú C6 (11/09):** 8 tên bảng trong code không có trong kiểu sinh từ staging — phải soi từng tên: bảng
chỉ có ở production, bảng đã đổi tên, hay mã chết (`DeliveryBooking` của `tms/bookingController.ts` đã biết
là mã chết). Baseline ratchet đặt bằng số hiện tại; giảm được thì `--update-baseline`.

**Cách đọc "có hồi kết":** cột *Tái phát cuối* của mọi lớp có lưới tầng 1–3 phải đứng yên qua các đợt
kiểm; đợt kiểm chỉ còn thêm dòng MỚI. Khi một đợt check-app ra ≥ 3 lỗi LẶP cùng lớp ⇒ dừng vá lẻ, sửa lưới.
